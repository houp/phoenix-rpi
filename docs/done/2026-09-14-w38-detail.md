# W38 working detail (2026-09-14)

Moved out of `docs/inprogress/WEEK-2026-W38.md` to keep the weekly log to what the owner reads.
Live summary stays in the weekly log; the `premain-hang` and `#67` rows in
[`docs/KNOWN-ISSUES.md`](../KNOWN-ISSUES.md) are the single source of truth for issue status.

## 2b. ✅ SIX-APP SHOWCASE GATE PASSES ON THE CURRENT TREE (2026-09-14)

The tree had not been gated since the AF_UNIX upstream merge, the Mesa BO-ownership fix and the
pthread revert. It passes — **6/6 mechanical** (launched, prompt, rc=0, **0 faults** each) **and 6/6
content-graded from the HDMI captures**, which is the half a clean log cannot tell you:

| app | non-black (best) | colours | vs baseline |
|---|---|---|---|
| X desktop | 89.5% | 30385 | 89.6% ✓ |
| QuakeSpasm | 97.9% | 67396 | ≥ 62% ✓ |
| Quake III | 61.5% | 47507 | in range ✓ |
| Quake II | 47.2% | 11729 | 47.2% ✓ |
| vkQuake | 56.8% | 26065 | 62–83%, low-normal |
| SuperTuxKart | 98.9% | 104551 | 99.5% ✓ |

ⓘ vkQuake's best frame is at the low end of its band; these are uncontrolled viewpoints (attract
loops), so frame-to-frame variance is expected and this is not by itself a regression signal.
Logs `artifacts/rpi4b-uart/*w38gate-*`, frames `artifacts/hdmi/*w38gate*`.

## 2c. ✅ DEMO-RISK SOAK on the gated tree — the two failure modes that would break a live demo

Qualifying the build the image was cut from, against the things that would visibly fail on stage:

| soak | trials | result |
|---|---|---|
| **Quake III on `q3dm7`** (the documented V3D binner-wedge reproducer) | 6 | **0 wedges, 0 faults**, all 6 rendered |
| **SuperTuxKart** (`hacienda`, 4 karts, 2 laps) | 6 | **0 corruption, 0 faults, 0 allocator events**; 5 of 6 rendered |

★ `q3dm7` is the map that historically wedged the binner; 6 clean runs on this tree, every one
reaching a steady frame stream. No `RENDER TIMEOUT` / `DROPPED job` anywhere.
⚠ **One STK launch (1 of 6) was a `premain-hang`** — command echoed, then nothing for 200 s, which
clears the ~68 s cold-exec threshold, so it counts. On the **clean** tree this is now 1/52
(quakespasm) + 1/6 (STK) = **2 in 58 launches**. The relaunch workaround applies and the rest of the
run was clean; it remains the known residual risk, not a new one.

## 3. ⚠⚠ `premain-hang` — THE REGRESSION CLAIM IS WITHDRAWN, and the real rate is ~2%

**I compared instrumented builds against clean ones and called the difference a regression. That was
wrong.** Re-scored every arm from the logs, tagged by what instrumentation it carried:

| build arm | silent | trials | rate |
|---|---|---|---|
| 2026-09-09 rolled back (clean) | 0 | 27 | 0% |
| 2026-09-12 restored (clean) | 3 | 26 | 11.5% |
| **current, CLEAN (what ships)** | **1** | **52** | **1.9%** |
| current + `LIBC_STARTUP_TRACE=min` | 6 | 44 | 13.6% |
| current + exec-entry marker | 0 | 33 | 0% |

★★ **My diagnostic instrument raises the failure rate ~7×** (1/52 clean vs 6/44 traced,
**Fisher p = 0.045**). Every headline rate I have quoted — the "18% of launches", the
"cold+heavy 3.7%" — came off instrumented builds.
❌ **So the rollback result is void:** it compared rolled-back **clean** (0/27) against current
**instrumented** (6/44). Controlling for that, rolled-back clean vs current clean is **0/27 vs 1/52,
Fisher p = 1.00 — no difference whatsoever.** The 2026-09-09 window is **not** implicated by my data.
Witold's regression hypothesis is neither confirmed nor refuted; my evidence simply did not test it.
ⓘ The one arm that still looks odd is 09-12 clean (3/26) against current clean (1/52), p = 0.11 —
suggestive of nothing at this sample size.

★ **What this means for the demo (the standing goal): the risk is much smaller than reported.** On
the build that actually ships, a cold app launch fails to start about **1 in 52**, not 1 in 6, and
the workaround (relaunch) is unchanged. This does not need to block a recording.

⏭ **Method rule going forward:** never compare a rate across builds that differ in instrumentation.
Measure the clean build, or instrument both arms identically.
⏭ The defect is real and still open — it did occur once in 52 clean runs — but at ~2% it needs a
reproducer that does not perturb it, and every instrument I have tried either raises the rate (the
libc trace) or suppresses it (the exec marker). That, not more benching, is the blocker.
ⓘ Live fact worth keeping: the board stays **fully alive on the network** through the 300 s hang
(156 consecutive pings), so it is a localised process hang, not a console or scheduler wedge.

## 4b. `unlock on not locked lock` — three causes eliminated; caller still unnamed

`_proc_lockClear()` prints that line from two places, and every plausible source has now been tested:

- ❌ **Not the condition-wait path** — `LOCKWAIT-UNOWNED` marker (kernel `3be6f38c`): QuakeSpasm shows
  **1 `lockClear`, 0 `LOCKWAIT-UNOWNED`**.
- ❌ **Not `pthread_mutex_unlock`** (so not SDL's `SDL_UnlockMutex`) — libphoenix probe `d30ab73`,
  both probes live in one run: kernel `lockClear=1`, libc probe **0**.
- ❌ **Not `pthread_join`** — that "fix" was wrong and was reverted.
⇒ It is a **direct `mutexUnlock()`** somewhere (86 call sites across 10 libphoenix files, or the app).

**Attempt to name the caller from the kernel (`39dda776`, pushed) — inconclusive, with two findings
worth more than the attempt:**
- ⛔ **A probe placed AFTER `LIB_ASSERT_ALWAYS` on the same condition is DELETED by the compiler.**
  The assert does not return on failure, so downstream `lock->owner != NULL` is known and the branch
  is dead code. My first version compiled and linked cleanly and its format string was simply
  **absent from `threads.o`** — a silent no-op that reads exactly like "it did not reproduce".
- ⛔ **`thread->context` is whatever the SCHEDULER last saved, not the syscall-entry frame.** Three
  runs gave a nonsense value, one genuine userspace address, and a pair of kernel addresses. The one
  good sample resolved to `mutexUnlock` in `libphoenix/arch/aarch64/syscalls.S` — the syscall
  **stub**, not its caller.
⏭ Naming the caller needs the syscall-entry user frame, which that pointer does not provide.
ⓘ Priority **low**: benign while uncontended, fires at process exit, no demo impact. The probe is
committed with both caveats so the next attempt does not repeat them.
✅ Clean rebuild verified: all four diagnostic strings **0** in the loader; HW **38/0 `OK`**, 0 faults,
`Host_Init` **2.219 s**. Manifest `2026-09-14-straypc-probe-clean.md`.

## 4c. ✅ `#67` vkQuake torches — MEASURED AT LAST: 6/6 LIT. And my last entry was wrong.

⚠ **RETRACTION.** Yesterday I wrote that the `+map` fix "does not work" and that **both** forks ignore
`+map start`. Both halves were wrong:
- **quakespasm honours `+map start`** — the log shows the start map's own level name (`Introduction`),
  `FITZQUAKE 0.85 SERVER`, `player entered the game`, ~24 fps. I had read `3 demo(s) in loop` as proof
  the demo loop won; that line is printed **unconditionally** by `Host_Startdemos_f`, *before* the
  `if (!sv.active && ...)` that actually decides. I graded on a message that says nothing.
- **vkQuake ignores it for an unrelated reason**: our own glue gives that port **no argv path at
  all** — the boot map/demo come from `id1/phoenix-map.cfg` / `phoenix-demo.cfg`. The cmdline-cvar fix
  was never the obstacle there.

🐞 **Real bug found and fixed** (ports `6a639ab`, pushed): `read_boot_map()` applied the same `"start"`
fallback to the **demo** config as to the map config. The demo config's job is to be *absent* most of
the time, so a missing file produced `playdemo start` — a demo that does not exist — instead of
falling through to the map path. **The demo override could not be turned off by deleting the file**,
which is exactly what made `#67` unmeasurable.

✅ **With that fixed, the torch question is answerable for the first time:**
`check-torch-rois.py --rate` → **6/6 gradeable trials PRESENT, 0 absent, 0 inconclusive (100%)**, at
**599/553 lit pixels** against a threshold of 8. Before the fix: 5/5 INCONCLUSIVE, 0 at-viewpoint
frames. Scored by pass rate at a fixed viewpoint, never a screenshot — the instrument exists because
screenshots produced five false closures.
ⓘ `id1/phoenix-demo.cfg` is hand-staged (nothing generates it) and is now **absent**, so vkQuake boots
`map start` by default — which also gives the showcase gate a controlled viewpoint to grade. Re-create
the file with a demo name to get the attract-loop footage back; a copy is saved outside the tree.

---

# Moved out of the W38 log on 2026-09-15 (done, kept for the record)

## 2c. ✅ Earlier: gated, soaked and shipped

- **Six-app showcase gate: 6/6 mechanical and 6/6 content-graded** from the HDMI captures (X 89.5%,
  QuakeSpasm 97.9%, Quake III 61.5%, Quake II 47.2%, vkQuake 56.8%, STK 98.9% non-black).
- **Demo-risk soak on the same tree:** Quake III on `q3dm7` — the documented V3D binner-wedge
  reproducer — **6 runs, 0 wedges, 0 faults**; SuperTuxKart **6 runs, 0 corruption, 0 faults, 0
  allocator events**. One STK launch of the six was a `premain-hang` (§3).
- **`#67` vkQuake torches — scored properly for the first time: 6/6 gradeable trials LIT**, 599/553
  lit px against a threshold of 8 (it had been 5/5 INCONCLUSIVE, 0 at-viewpoint frames). Unblocked by
  a real bug fix in our port glue, ports **`6a639ab`**: `read_boot_map()` defaulted the *demo* config
  to `"start"`, so deleting `id1/phoenix-demo.cfg` ran `playdemo start` — a demo that does not exist —
  instead of booting the map the ROI checker needs.
  ⓘ That file is hand-staged and now **absent**, so vkQuake boots `map start`, which also gives the
  gate a controlled viewpoint. Re-create it with a demo name to get attract-loop footage back.
  ⓘ The shipped image `4099f657` predates this and still carries the demo config — unaffected.

## 2d. ✅ Docs and gate brought in line with this week's evidence

- **`docs/KNOWN-ISSUES.md` corrected** — it is the single source of truth and two rows were wrong.
  `premain-hang` still quoted the withdrawn **~17%** rate, the cold+heavy grid and the `p = 0.022`
  regression claim; it now carries the clean-build rate (**~1 in 52**) and the instrumentation
  confound. `#67`'s recipe told the reader to run **`vkquake +map start`**, which cannot work — that
  port has no argv path — and is now the correct bare-`vkquake` recipe plus the 6/6 result.
- **The `#67` torch check now runs inside the showcase gate** (`run-showcase-gate.sh`). vkQuake boots
  a fixed viewpoint, so it costs no extra Pi time. Exercised both ways on existing frames: a start-map
  cycle exits 0 (19 of 22 frames at the reference viewpoint), a demo cycle exits 2. That history is
  five false closures on screenshots, so it should not depend on anyone remembering to check.
- Swept the live docs for the retracted figures — only history under `docs/done/` still carries them.
- Reviewed `q3-qvm-recipe`, `shader-cache-speckle`, `xterm-resize-artefacts`: all three still genuinely
  open, nothing to remove.

---

## 2c. ✅ Earlier: re-gated after the vkQuake fix — 6/6 content-graded (2026-09-14)

| app | non-black (best) | colours |
|---|---|---|
| X desktop | 89.8% | 34882 |
| QuakeSpasm | 66.3% | 90998 |
| Quake III | 61.3% | 54880 |
| Quake II | 47.3% | 11722 |
| vkQuake | 55.0% | 21850 |
| SuperTuxKart | 99.8% | 109834 |

★ vkQuake's number is now **meaningful**: it boots `map start`, so it is the same view every run
instead of wherever an attract demo happened to be. The band I twice wrote off as "variance" was that
missing viewpoint.
🐞 **Harness fix (`run-showcase-gate.sh`): prompt deadline 150 s → 220 s.** The first cycle of a
session boots slower; twice today it missed the `(psh)%` marker, and the app then scores **rc=2 with
"(no log)"** — indistinguishable from the app being broken. X "failed" that way and passed a 220 s
retry with **0 faults** and an 89.8% desktop.
