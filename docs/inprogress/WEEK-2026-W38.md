# Week 2026-W38 (Mon 2026-09-14 → Sun 2026-09-20)

Phoenix-RTOS RPi4 — weekly working log. Kept **short on purpose**: this is the file you read.
Analyses: `docs/misc/`. History: `docs/done/`. W37: `docs/done/WEEK-2026-W37.md`.
This week's working detail: [`docs/done/2026-09-14-w38-detail.md`](../done/2026-09-14-w38-detail.md).

**★ OWNER GOAL (2026-09-06):** a stable build with the games and X11 good enough for a public
presentation / a screen recording published online. Everything below is ranked against that.

---

## 1. STATE OF PLAY — is anything blocking? **No.**

The build is **6/6 gate-verified** (all five games + the X desktop render, 0 faults), the demo image is
cut and gated, and every bug that could stop a presentation was closed this week. **The only thing
between us and a presentation is flashing the image** (§2) — and that is also the only risk I cannot
retire from here, because this host has no card reader.

Every open bug, honestly rated:

| bug | what it does | live with it? |
|---|---|---|
| "page reads back zero" family — `libc-uninit-main`, `freebin-corruption`, `stk-highbits-pointer`, and the 09-15 HEAP-CANARY | an app occasionally dies at startup or mid-run | **Yes.** Relaunch it. No *fault* from this family since 2026-09-12; the canary hit once and has not repeated in **19** runs since. |
| `V3D-binner-wedge` | GPU binner stalls, auto-recovers via reset | **Yes.** 9 q3dm7 runs / ~85 700 frames, 0 wedges; last seen 2026-08-22. |
| `atexit-null-head` | a `.data` word read back NULL at load | **Yes.** Root cause found and fixed in-tree; 0 events in ~3645 processes. |
| `unlock on not locked lock` | a warning | **Non-issue** — diagnostic builds only, does not exist in the shipped image. |
| `q3-qvm-recipe` · `shader-cache-speckle` · `xterm-resize-artefacts` | reproducibility gap · a known cosmetic tell · an unreproduced drag artefact | **Yes**, all three. |

⚠ **Course correction (owner, 2026-09-15):** *"since a week you are basically solving some esoteric,
complex, deep bugs."* Fair. Once the blocking bugs were gone, "pick the highest-priority remaining
item" kept walking me down the list instead of prompting the question of whether anything left was
worth the time. **The deep-bug hunt is stopped.** The corruption family stays instrumented — the
canary now reports its own damage extent and says whether the parent's copy survived, so a recurrence
diagnoses itself at zero cost — and I will only go back to it if it actually bites.
🔧 **Demo-guide accuracy, three passes (2026-09-15).** README + the launcher said **twm** where every
session mode runs **Window Maker**, `xeyes` where the code runs `xlogo`, omitted `browse`/`action`, told
the reader to `rm -f /tmp/.X0-lock` by hand (`#66` fixed that), and offered an STK command that still
needs a human to drive. All corrected against the code, `startx term` re-verified on hardware. The vein
is spent — the remaining commands were checked and are right. Detail in `docs/done/`.

## 1b. 🎬 Showcase reel re-record — IN PROGRESS (owner asked 2026-09-15)

Owner: *"try to regen the showcase video with the current system version."* Nine clips recorded off the
current build. Owner reviewed them and asked for three things:

| owner's note | status |
|---|---|
| "Quake 2 … is just standing at one place doing nothing" | ✅ **fixed and in the reel** — `+demomap`, cut 97\|22 |
| "Quake 3 … camera is static around player which is not moving" | ✅ **fixed with `+devmap`** — now beats the shipped clip on every metric |
| "vkQuake — some lightmaps on walls are wrong … **this is a regression**" | ✅ **explained + re-recorded on the right map** — see below |
| "Quakespasm looks perfect ❤ / STK ~9 fps, 1/3 better" | — no action |

**Quake II ✅.** The complaint is one number: over any 10 s of the rejected clip the upper two-thirds of
the picture was *bit-identical* (translation metric **0.02**) — only the weapon bobbed. `+demomap`
scores **19.54**, and 16.30 mean frame-to-frame vs 0.55 — ~30× the rejected clip and ~22% better than
the segment already in the reel. Reel now cuts `20260915-164244-q2demo|97|22` (0% still frames).
ⓘ That demo ends at 152 s and the rest of the 300 s clip is a frozen console — 180 s is enough next time.

**Quake III ❌ — my re-record was a regression, and the cause was my command line.**
`scripts/stage-game-data.sh` stages an autoexec with `cg_thirdPerson 1` / `cg_cameraOrbit 1`, and
**both are `CVAR_CHEAT`** — launching with `+map` lets cgame init reset them, so the orbit never
engaged. That script's own header says to use **`+devmap`**. I then compounded it with
`+set cg_cameraOrbit 3 +set cg_cameraOrbitDelay 50`, replacing the tuned 1/1 orbit with the coarse
stepping this project had already measured and rejected. Result: **4× less dynamic than the clip
already in the reel** (lag-1 mean 1.84 vs 7.65; 73% still frames vs 0%), no clean 24 s window in it.
⇒ Re-recorded as **`quake3 +devmap q3dm1`**, nothing appended, and the orbit engaged: lag-1 mean
**7.94** / median 7.61 / **0% still** / translation **11.36** — better than the clip that was already
in the reel (7.65 / 7.07 / 0% / 9.70) and unrecognisable next to the failed attempt (1.84 / 0.25 /
73%). Third-person orbit confirmed on a decoded frame (player model from behind, 100 health, a bot in
shot, 36 fps). Reel now cuts `20260915-172704-q3orbit|122|24` — no scoreboard or death flash anywhere
in the window.

↩ **RETRACTED (2026-09-15): the claim that three reel segments could not be reproduced.** I had
reported python3, dillo and mc as missing from the netboot export and from every recent gated image.
They are **all present in both** — `python3` 58.8 MB, `dillo` 4.6 MB, `mc` 2.5 MB, `top` 229 kB,
`xbill` 3.7 MB, byte-identical sizes in the live export and inside `16ad56f9`'s ext2 (read with
`debugfs` at the p2 offset). The earlier reading came from `ls a b`, which **fails if *either* operand
is missing** — so a present binary reported ABSENT. All eleven reel segments are reproducible; nothing
needs an owner decision here.

### ★ vkQuake "lightmap regression" — ROOT CAUSE FOUND: it is a **different map**, not a render bug

**The two clips do not show the same level.** Straight from the UART logs:

| clip | `phoenix-demo.cfg` | demo | level |
|---|---|---|---|
| 09-08 — *"the previous version looked better"* | `demo2` | demo2.dem | **the Grisly Grotto** (e1m2) |
| 09-15 — *"some lightmaps on walls are wrong"* | `demo1` | demo1.dem | **the Necropolis** (e1m3) |

e1m3 is one of the darkest, harshest-contrast levels id shipped — large unlit recesses with hard
straight boundaries against brightly-lit stone. That is exactly what "suddenly some specific walls are
too dark or too lit" describes, and it is what the map looks like **when rendered correctly**.

`id1/phoenix-demo.cfg` is **hand-staged — nothing generates it** — and it is the single switch that
picks the level. It got re-staged as `demo1` for this week's recordings; the 09-08 reel was captured
with `demo2`. So the change in appearance came from my own staging, not from the graphics stack.

**Fix for the reel: stage `demo2` and re-record vkQuake**, which puts back the exact level the owner
liked. ⚠ Stage it *after* a cycle's prepare step — the export's `usr/share` is re-synced from the
project rootfs-overlay, which silently removed the file between this afternoon's runs and now.
⚠ It must be **absent** for the #67 torch gate, which needs `map start`.

**✅ RESOLVED — and the renderer is exonerated by measurement, not by argument.** I re-recorded
vkQuake with `demo2` staged, so there is now a **same-engine, same-demo, same-map** pair one week
apart. The status bar is pixel-identical art at a given demo tick, which makes an exact frame anchor
(it failed across engines only because QuakeSpasm scales its HUD differently): **17 exact matches**.

| vkQuake, demo2 / Grisly Grotto | seam mean | median | p90 |
|---|---|---|---|
| 09-08 build | 0.104 | 0.101 | 0.160 |
| 09-15 build | 0.117 | 0.109 | 0.168 |

Indistinguishable, and the best-matched frame pair (90/92/97 armour/health/ammo, 56 FPS on both) is
visually identical. **No rendering regression exists.** The reel's vkQuake segment is now cut from the
new clip — `20260915-171943-vkq-demo2|112|22`, the Grisly Grotto on the current build, 0% still frames.

⚠ Two measurement traps caught in the act while grading it, both of which had already produced a wrong
reading earlier today: a **frozen console tail** scores 4.10% on the seam metric on 27 consecutive
frames (white text on black is a luminance step with no colour step — a textbook false positive), and
an **ffmpeg contact sheet's last tile did not hold the time I computed for it**, which made a
prime-gameplay frame look like the console. Per-second scans of the decoded stream settled both; the
tile did not.

ⓘ Also worth knowing for the reel: **vkQuake plays ONE demo and drops to the console** (~60 s of
gameplay per run) because the glue issues a single `playdemo`; QuakeSpasm loops demo1→2→3. The
vkQuake clip therefore has a much shorter usable window than the others.

**What the metrics said** (kept, because they are why I did not stop at "looks different"): *two*
independent pixel metrics fail to separate the 09-08 and 09-15 clips — pure-black coverage reads
**0.00% on both**, and a lightmap-seam detector (strong relative luminance step with no matching
chromaticity step, which is what a wrong lightmap block looks like and a geometry edge does not) reads
median **0.102% vs 0.105%**. Two null results against an owner-visible difference meant the difference
was not in the pixels' *lighting* — it was in the *content*. The contact sheets then showed it at once.

Cleared along the way, by reading rather than guessing — all still true and worth not re-testing:
`r_gpulightmapupdate 1` has been forced since the port landed **2026-08-26** (so it was on for the good
clip too) · ports `bec6d0d` only affects `pr_edict.c` edict sorting, and libphoenix's `copysign` is
correct · mesa `274ee5abea9` is GL-path-only *and* a fix · the 09-11 V3D clock/reset family logged
**zero** retry/reset/wedge/timeout lines in either of today's UART logs · the `#29` `imageExtent`
rewrite's premise does not survive reading the code (`mipwidth == glt->width` at that point).

⏳ Still open, and a **separate** question from the regression: the owner also compared vkQuake against
QuakeSpasm *on the same demo* and preferred QuakeSpasm. Both engines do play demo1 first, so that
comparison is fair — but QuakeSpasm draws its status bar at a much smaller scale, so there is no cheap
pixel anchor to align the two engines frame-for-frame without OCR. Parked until the reel is done.

✅ **Dillo works end-to-end and the proof is independent of the picture:** the host's own HTTP access
log shows the request arriving *from the Pi* — `10.42.0.12 - - "GET / HTTP/1.1" 200` — and the frame
shows the page rendered with `http://10.42.0.1:8000/` in the URL bar under Window Maker on glamor X.

🔧 **Game of Life: my first take was bad and is being re-recorded.** I passed `--ansi`, which makes
`life.py` erase and repaint the *whole* screen each generation; on the HDMI fbcon that repaint eats
most of the ~1.25 s generation interval, so the capture catches a **half-painted screen in 93% of
frames** (median 48.5% of the screen painted). The segment already in the reel is fully painted in
**100%** of frames — because the 09-08 capture ran `life.py 400` with **no flag**, letting curses
update only the cells that changed. ✅ **Re-recorded without it: 99.3% median, full in 100% of frames**,
matching the old segment exactly. ⓘ Caught only by comparing against the segment it replaces — the bad
clip looked perfectly plausible on its own, because sparse `#` on black is what Life should look like.

### ✅ `vkq-tile-flicker` FIXED — vkQuake was rendering into the buffer being displayed

The one-frame black tile blocks were **not** a lightmap bug. vkQuake has no WSI swapchain, so it
rendered into a single VkImage backed by the fb0 pages and `storeOp=STORE` *was* the present — writing
the buffer the display was scanning out, while it was scanned out. V3D stores tiles one at a time, so a
refresh landing mid-store showed some tiles of the new frame and some of the old.

★ **Port-only fix — the winsys already had everything and the GL path already used it**, which is
exactly why QuakeSpasm was the clean control. vkQuake called `v3d_phoenix_set_scanout()` (records pa and
size) instead of `v3d_phoenix_scanout_init()` (probes the firmware's granted virtual height and sets up
the 2nd/3rd buffers), so `v3d_phoenix_flip()` was a **silent no-op** in that process. It now creates one
image/view/framebuffer per granted buffer, renders off-screen, and flips once the frame is complete.

**HW-verified:** `scanout init … virt_h=3240 -> 3 buffer(s) TRIPLE-BUFFER+page-flip`, three images bound
at distinct physical addresses, **0 faults**, and the render is unchanged (lum 27.4 vs 27.3, colours
1099 vs 1103, non-black 91.9% vs 91.5%). Escape hatch: `VKQ_SINGLE_BUFFER=1`.
⚠ **Honest limit: the flicker RATE is unmeasured.** The mechanism is gone by construction, but no
validated detector exists (7+ variants failed — they fire on fast camera motion or miss a frame
confirmed by eye), so I am not quoting a before/after number.
🎬 The reel's vkQuake segment is re-cut from the **fixed** build.

### 🎬 Reel regeneration status — 9 of 11 segments now from the CURRENT build

## ★ THE REGENERATED REEL IS BUILT AND VERIFIED

`artifacts/hdmi-video/20260915-191652-phoenix-rtos-rpi4-showcase.mp4` — **239 s, 147 MB, all 11
segments captured today (2026-09-15)** on the current build. This is what you asked for: *"regen the
showcase video with the current system version to make it most up-to-date as possible."*

Boot · Shell · Game of Life · X11 desktop · Dillo · H.265 · QuakeSpasm · Quake II · vkQuake ·
Quake III · SuperTuxKart. Every cut chosen on measurements taken on the *exact* cut — and the three
you flagged are fixed: **Quake II** now plays the demo (30× the motion of the clip you rejected),
**Quake III** orbits (the earlier attempt failed because `+map` silently resets the `CVAR_CHEAT` orbit
cvars — it needs `+devmap`), and **vkQuake** is back on demo2/the Grisly Grotto.

🔒 **Gated**: `scripts/verify-demo-reel.py` → **PASS on all eleven** (live signal, content, and motion
where motion is expected). It exists because the first build shipped a Shell cut that ran 6 s past the
end of its source run into the capture card's **no-signal** field — a uniform RGB(7,7,7) that is a dead
input, not a black picture, and is indistinguishable from a broken segment once published.

⚠ **Two things still need your call before this goes public** — see the H.265 privacy note below, and
`vkq-tile-flicker` (cosmetic, absent from the chosen vkQuake window).

### 🐞 NEW BUG, and a correction to what I said above: `vkq-tile-flicker`

My "different map" finding stands for the *lighting*, but it was **not the whole report**. There is a
real, separate defect, and I verified it myself on three **consecutive frames from a single decode**:

**A single frame (1/30 s) shows hard-edged black rectangles; the frames immediately before and after,
at the same viewpoint, are clean.** Evidence: `artifacts/vkq-lightmap-dating/` (saved by the analysis)
plus my own `flicker3.png` triple.

**It is not a lightmap bug.** The blocks are axis-aligned in *screen* space, and one band crosses the
left wall, the arch recess, the right pillar and the right wall at identical screen rows — four
surfaces with independent lightmaps in texture space. Nothing lightmap-shaped can do that. V3D is a
**tile-based** renderer and the blocks are tile-shaped ⇒ suspect the **tile-store / present path**,
not the compute-lightmap shader. Prior art: [[project_pi4_quake_flicker_vcmbox]] (Quake flicker was
single-buffering).

**Control:** QuakeSpasm on the *same demo, same day, same capture card* is clean over 72/72
consecutive frames where vkQuake blocks. So it is not the grabber and not H.264.

⚠ **The rate is NOT yet measurable, and I am not quoting one.** Seven detector variants have now
failed (mine: three — two fire on fast camera motion, one misses the frame I confirmed by eye; the
analysis: four). Every verdict so far is contiguous-window visual review.
ⓘ Reported present on 09-08 at much lower amplitude, i.e. probably **pre-existing**, with amplitude or
rate having changed rather than the defect appearing — **I have not independently verified that half**.
ⓘ For the reel: the worst single-frame black spike inside the chosen vkQuake window (`112|22`) is the
camera passing a dark doorway, not a glitch. That is reassurance, not proof.

★ **ROOT-CAUSED the same day, from source, no Pi time:** vkQuake has **no WSI swapchain** — it renders
into ONE VkImage whose memory the winsys backs with the **fb0 physical pages**, and the render pass's
`storeOp=STORE` *is* the present. So **the GPU writes the buffer the display is scanning out, while it
is being scanned out**, and V3D stores tiles one at a time — a refresh landing mid-store shows some
tiles new and some still holding the previous frame. That is precisely a screen-space, tile-shaped,
one-frame artefact. ⚠ `v3d_phoenix_set_next_scanout()` is **not** a page flip despite the name (it
flags the next BO as scanout-backed so it gets RASTER tiling); vkQuake calls it once, at image
creation. **There is no per-frame flip in the Vulkan path at all.**

That also explains the control: the GL path **page-flips** (`scanout_nbuf` / `scanout_disp_off`), so
QuakeSpasm only ever displays completed frames. Same class as the earlier Quake dynamic-model flicker,
which was also single-buffering.

**Fix shape (not yet written):** give the Vulkan path the double-buffering the GL path already has —
render into the back buffer, then move the displayed offset. The winsys already detects whether a
second buffer was granted; what is missing is a per-frame flip entry point the Vulkan side can call.
Analysis: [`docs/misc/2026-09-15-vkq-tile-flicker-root-cause.md`](../misc/2026-09-15-vkq-tile-flicker-root-cause.md).
Cosmetic meanwhile, and absent from the reel's chosen vkQuake window.

### 🎬 Reel regeneration status — 9 of 11 segments now from the CURRENT build

**10 of 11 segments now come from the 2026-09-15 build.** Each cut was chosen on measurements taken
on the *exact* cut, not on the whole clip: Boot `48|17` (0 black frames, vs **7** in the line it
replaced), X11 `139|26` (0 frozen steps, Life live at 17.4 gen/s matching its own readout),
QuakeSpasm `116|22` (entirely inside demo1 — the demo1→demo2 change is at t=140.5), STK `161|24`
(**2.1×** the motion of the old line, which contained a frozen step), plus the Q2/vkQuake/Q3, Dillo and
Game of Life cuts.

⏳ **Only Shell is still from 09-08.** ↩ **And the `fbcon-freeze` bug I filed does not exist — retracted.**
I read a static HDMI screen as a frozen console. It was static because **nothing more had been sent**:
`record-showcase-clip.sh` sets `--idle-secs` to *recording length + 30 s*, and idle-secs is how long the
cycle waits for quiet **after each command** — so with six commands the first alone outlasted the whole
240 s recording. The "frozen" frame's **bottom** shows `uname -a`, its output and a **live cursor at the
prompt**; I had cropped only the top and middle of a 1080-row frame. Everything I built on that — the
takeover timing, the `/dev` re-bind lead, the inherited-stdio hypothesis, a "reproduced 2/2" — was
explaining an artefact of my own harness settings. The console is fine.
🔧 **Fixed in the tool:** `REC_IDLE_SECS` now overrides the pacing, and the script warns when
idle-secs × commands exceeds the recording. Re-recording Shell with it.
⚠ Worth taking seriously beyond the reel: a frozen HDMI console during a **live presentation** would
look identical to a hang.

⚠ **PRIVACY — needs your call.** The H.265 segment decodes `IMG_8331-phoenix.265`, **your own phone
recording of your desk**. In the 09-15 capture a laptop screen is legible at **t≈80 and t≈129** showing
an internal Claude Code transcript, and the reel encodes at full **1920×1080 with no downscale**, so
anything readable there is readable in the published file. My chosen window (`85|24`) deliberately
avoids both, and shows a file manager, the previous showcase mp4, the Phoenix-RTOS page in Dillo and a
keyboard — but the room and screen are still yours. ⓘ The same footage is in the **currently shipped**
reel too, so this is not new; I am flagging it because publication makes it matter.
✅ **A non-personal alternative is recording now**: the same `hevc-play` decoding
`showcase1080.265` — the Pi hardware-decoding a 1080p video that contains no personal content. You can
take whichever you prefer; nothing is published either way until you say so.

The last two are recording: `python3 /usr/share/demo/life.py 6000 --ansi` and `startx_gpu browse`.
🔧 The Dillo one needed a fix first: the Pi's `dillorc` points `home=` at `http://10.42.0.1:8000/` —
the dev host on the netboot network — but **nothing in the tree ever served it**, so that capture
silently depended on someone starting a server by hand and would otherwise have recorded an empty
browser window. The page and the one-line command that serves it are now committed
(`tools/demo-apps/webroot/`).

## 2. ⏸ YOUR ONE ACTION — flash the new demo image `da752ac1`

`artifacts/rpi4b/rpi4b-sd-2part-gated-da752ac1.img` · SHA256
`da752ac1b8a902420feca8d85a03c2d3416e2062a5844ddb353062fc6358d857` · 1.6 GB ·
manifest `manifests/2026-09-15-demo-image-da752ac1.md`

```
udisksctl unmount -b /dev/sda1 2>/dev/null || true
sudo dd if=artifacts/rpi4b/rpi4b-sd-2part-gated-da752ac1.img of=/dev/sda bs=4M conv=fsync status=progress && sync
```

**Supersedes `16ad56f9`, which you had not flashed yet.** One delta: the **vkQuake page-flip fix**
(ports `7ed2949`). `16ad56f9` was cut at ports `d7a3740`, so a live vkQuake demo off that card would
have shown the one-frame tile flicker. Everything else is identical.

**Gates — all run against the image, not the build tree:**
- **contents PASS** (`verify-sd-image-contents.sh --expect showcase`), incl. the FAT boot partition
- **filesystem + boot target**: `e2fsck -fn` clean (10513 files), `loader.disk` boots
  `/dev/mmcblk0p2:ext2` with zero `nfs;/` refs, plo starts **usb** and **lwip** (no usb = no keyboard)
- ★ **the fix is verified INSIDE the image's own ext2**, read out with `debugfs` — the page-flip and
  scanout-count markers are present **and the old single-image marker is absent** (a negative control,
  so this cannot be a stale binary).

★ Before presenting or recording: run each GPU app **twice** to warm up — 1 cold start in 12 spends its
whole window compiling shaders.
⚠ **You are the first to boot this from a card** — this host has no SD reader, so SD boot cannot be
exercised here; the gates above are the closest stand-in.
↩ Fallbacks, newest first: `-gated-16ad56f9.img`, `-gated-4099f657.img`, `-gated-afd534a7.img`.
ⓘ Don't flash `rpi4b-sd.img` (FAT-boot-only) or the rolling `rpi4b-sd-2part.img`.

## 2b. ✅ Re-gated after today's three core changes (`w38-postdebt`)

**6/6 mechanical · 6/6 content-graded · 0 faults · 6/6 launched · torches PRESENT** — the first full
six-app pass *after* the USB-stack, `KERNEL_BOOT_TRACE` and klog-mirror changes, which is the check a
2-command smoke test does not give.

| app | non-black (best) | colours |
|---|---|---|
| X desktop | 89.5% | 29646 |
| QuakeSpasm | 66.9% | 65796 |
| Quake III | 54.8% | 51167 |
| Quake II | 47.2% | 11722 |
| vkQuake | 56.4% | 21755 |
| SuperTuxKart | 99.9% | 111034 |

ⓘ QuakeSpasm and Quake III read a little lower than the 09-15 morning run (76.2 / 63.1). Both play a
**demo**, so the best frame depends on where the attract camera is — the historical band is 61–76 and
61–65. Not read as a regression; both launched, rendered 22 ticks and faulted 0 times.
Manifest `manifests/2026-09-15-w38-postdebt-gate.md`.

⏸ **The image is NOT re-cut.** Those three changes remove silent-corruption margin and publication
debt; none fixes anything the owner would see. `16ad56f9` stays the one to flash — a third SHA to
track would be churn. Say the word (or wait for something material) and it is a ~10-minute re-cut.

## 2c. ✅ Earlier: gated on the exact tree the image came from (2026-09-15, `w38-final`)

**6/6 mechanical · 6/6 content-graded · 0 faults · 6/6 launched · torches PRESENT.**

| app | non-black (best) | colours |
|---|---|---|
| X desktop | 89.8% | 35938 |
| QuakeSpasm | 76.2% | 114802 |
| Quake III | 63.1% | 45082 |
| Quake II | 47.2% | 11746 |
| vkQuake | 56.7% | 22204 |
| SuperTuxKart | 99.9% | 116048 |

Every figure matches or beats the 09-14 baseline. This is the run that matters: it is the first
six-app pass *after* today's core changes (the `_file_init` refactor touches every process's stdio and
the pipes cache touches every `open()`), and it is the tree `16ad56f9` was cut from.
Manifest `manifests/2026-09-15-w38-final-gate.md`.

## 5. Upstream

- **2026-09-13 sweep (7th). Nothing behind, nothing merged.** All 16 siblings **0 behind** upstream;
  all four game-fork patches (quakespasm / vkquake / yquake2 / quake3) **OK**.
- **2026-09-14 sweep (8th). Nothing behind, nothing merged.** All 16 siblings **0 behind**; all four
  patches **OK**. ⓘ Housekeeping: four repos (devices, ports, project, tests) were left in detached
  HEAD by an earlier `restore-integration-state.sh` and are reattached to `master`.

## 6. Open bugs

`atexit-null-head` · `libc-uninit-main` · `V3D-binner-wedge` ·
`allocator-double-free` · `stk-highbits-pointer` · `freebin-corruption` (first cause
believed found: the Mesa/winsys BO-ownership pair fix).
Single source of truth: [`docs/KNOWN-ISSUES.md`](../KNOWN-ISSUES.md).
