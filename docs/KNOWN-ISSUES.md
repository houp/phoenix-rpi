# Known issues and limitations

Open items in the Phoenix-RTOS Raspberry Pi 4 port, as of 2026-09-17. This is
the user-facing summary; the exhaustive engineering registries are:

- [docs/pi4-hardware-support-matrix.md](pi4-hardware-support-matrix.md)
  — per-peripheral status with evidence.
- [docs/TEMPORARY-FIXES-AND-FUTURE-CLEANUP.md](TEMPORARY-FIXES-AND-FUTURE-CLEANUP.md)
  — the `TD-NN` transitional-debt registry (its bottom "Tracking Checklist" is
  authoritative; most `TD` items are already resolved).

**This file is the single source of truth for open issues in this port.** Nothing else should carry
its own list — [docs/PHOENIX-RTOS-RPI4-CHANGES.md](PHOENIX-RTOS-RPI4-CHANGES.md) links here rather
than duplicating, because the two had already drifted. Resolved items are **struck through and kept**,
not deleted (corrected 2026-09-17 — the six `~~struck~~` rows below contradicted the old "removed as
soon as they stop being issues" wording). A closed row is what stops a fixed bug being re-litigated,
and several here were declared fixed more than once before they actually were; the git history keeps
the fuller record of what
was fixed, and `docs/done/` keeps the analyses.

> **Editing note.** Each row below must be **one line with exactly three `|`
> cells**, and the table must contain **no blank lines** — a blank line ends the
> table and everything after it renders as plain text. Long analysis belongs in
> `docs/misc/` or `docs/done/`, linked from the row, not inlined here.

## Application bugs

These affect the showcase apps, not the base system.

| ID | Symptom | Status / workaround |
|---|---|---|
| ~~#67~~ | **vkQuake wall torches (`progs/flame.mdl`) — no intermittency survives a correct measurement.** The historical reports were never scored against a controlled viewpoint: the check had 0 at-viewpoint frames because our own port glue ran `playdemo start` instead of booting the map (ports `6a639ab`). ⚠ **Never close this on a screenshot** — it was declared fixed five times historically, each on a single frame, and one "proof" was a moving lavaball mistaken for a wall torch. Verify only with a pass *rate*: `./scripts/test-cycle-bench.sh 6 <label> -- "vkquake"` then `./scripts/check-torch-rois.py --rate <label>`. ⚠ **Bare `vkquake`, NOT `+map start`** — this port has **no argv path at all**; the boot map comes from the hand-staged `id1/phoenix-map.cfg` (default `start`), and `id1/phoenix-demo.cfg` must be **absent** or a demo overrides it. Refuted: submit serialization, the alpha/scanout theory, `blend==0`, a stale shader cache, non-deterministic GPU archives. History: [`docs/misc/2026-09-03-quake-torch-regression-archaeology.md`](misc/2026-09-03-quake-torch-regression-archaeology.md). | **Closed 2026-09-15 — 6/6 by rate, twice.** `torch67`: **6 of 6 gradeable trials PRESENT, 0 absent, 0 inconclusive**, 16–19 at-viewpoint frames per trial, **599/553 lit px against a threshold of 8**, 0 faults in all six. That repeats the 09-14 6/6 on a different build, so **12 trials across two builds, no absence**. The torch ROI check runs inside `run-showcase-gate.sh` on every gate, so a regression is caught without anyone remembering to look. |
| ~~#64~~ | "SD-side filesystem stack pressure under load (deep fs call chains)". **The SD/ext2 half was already fixed and is the reference fix**: `#120` raised the storage pool to `storage_run(2, 16*_PAGE_SIZE)` = 64 KB, and the 2026-06-08 pool-thread audit ([`docs/done/2026-06-08-pool-thread-stack-audit.md`](done/2026-06-08-pool-thread-stack-audit.md)) re-checked every Pi 4 server against it. ★ That audit left exactly **one RISK row unapplied — the USB msg and status threads at 2 KB**, running msgRecv/status → enumeration → in-process class drivers (usbkbd/usbmouse) → DMA pool, on adjacent `.bss` with no guard page, so an overflow silently clobbers the neighbour instead of faulting (the `#120` shape). | **Closed 2026-09-15.** The last open row is applied: USB thread stacks **2 KB → 32 KB** (usb `f9f5723`), the top of the audit's 16–32 KB range, ~60 KB of `.bss`. Verified on hardware: `usbkbd` and `usbmouse` both enumerate (`/dev/kbd0`, `/dev/mouse0`), 0 faults, unix-socket 38/0, quakespasm `Host_Init` 2.228 s, every non-debug boot-stage row green. |
| ~~#66~~ | Stale `/tmp/.X0-lock` prevented the X server restarting after an unclean exit. **Not a broken liveness probe** — the new `test-libc-signal` `liveness` group proves `kill(pid, 0)` answers ESRCH for a reaped pid and 0 for a live one without delivering anything (5/5 on hardware). It is **PID REUSE**: the kernel hands out the *lowest* free pid, so the pid a dead X server released is likely already held by something else when the next launch reads the lock file, and Xorg's `LockServer()` then refuses with "Server is already active". | **Fixed.** `xlaunch` removes a leftover lock before starting the server (coord `ce5f50873`) — it is the only launcher, on a board with one display, about to start a fresh server. Verified: with a planted lock it logs `removed leftover /tmp/.X0-lock`, X comes up; with no lock the unlink is a silent no-op and two back-to-back sessions in one boot both start. |
| V3D-binner-wedge | Intermittent V3D binner wedge on long GPU runs, auto-recovered by a GPU reset that reports loudly (`RENDER TIMEOUT` / `DROPPED job`). Seen under both GL and Vulkan; `q3dm7` is the documented reproducer, and it is workload-specific (q3dm1's 1942 faces never wedge, q3dm7's 5823 do). ★ **It is NOT an MMU fault** — with the real fault-report registers instrumented (`MMU_VIO_ADDR` 0x1234, `MMU_VIO_ID` 0x122c, offsets confirmed against Linux `v3d_regs.h`), a wedged trial reported `vio_addr=0`, `vio_id=0`, `int_sts=0`, no binner overflow, and `ct0ca` parked **inside its own valid BCL BO**: a binner front-end pipeline stall with zero error status. ⚠ The earlier "`mmu_ill=0x8000886x` ⇒ stale PTE" reading was a red herring — `MMU_ILLEGAL_ADDR` is the scratch-page redirect *we program*, so reading it back only echoes `scratch_pa`. `r_mergeLightmaps 0` is **not** a fix (it wedged with the cvar set, after one lucky clean run). One contributing cause was found and fixed in the interim: `MAP_CONTIGUOUS` returns **non-zeroed** DRAM, so unpopulated binner-overflow-pool slots held cold-boot garbage that CT1 followed as a wild pointer; the pool is now zeroed. | Open; a reset+drop mitigation ships. Not reproduced recently — **9 q3dm7 runs, ~85 700 frames, 0 wedges** (6 more on 2026-09-15: 58 123 frames, 0 wedges, 0 faults), plus 18 six-app gate runs at 0; ⊕ **2026-09-17: 6 more `q3dm7` trials, 37 057 frames, 0 wedge signatures and 0 faults** (`q3dm7hunt`, deliberately run on the documented reproducer rather than waiting for it to turn up); last observed on a gate/shipping build **2026-08-22** (⚠ scoped 2026-09-17: the row's own `RENDER TIMEOUT`/`DROPPED job` signature does appear in later logs, up to 2026-09-09, but every one is an experiment-labelled run and the driver tags those drops `<-- TINY CL: likely a one-shot upload/meta-copy, NOT a redrawable frame`. Nothing at all after 09-09). ⚠ Kept open deliberately: clean runs also predate the pool fix, so for an intermittent this bounds the rate rather than proving it gone. ★ **That diff is DONE (2026-09-15) and the previous "named next step" was already stale** — [`docs/misc/2026-08-22-q3dm7-binner-stall-analysis.md`](misc/2026-08-22-q3dm7-binner-stall-analysis.md) had run it on 08-22 and gone further. Re-verified independently against today's tree: **`TILE_BINNING_MODE_CFG` cannot differ — we emit no field of it**; the packet comes from upstream Mesa (`v3dx_draw.c:86-103`) and no Phoenix commit touches `v3dx_job.c`/`v3d_job.c`/`v3d_util.c`, and the `TILE_BINNING_MODE_CFG` block itself (`v3dx_draw.c:86-103`) is upstream-authored line for line. ⚠ **Corrected 2026-09-17:** this used to say no Phoenix commit touches `v3dx_draw.c` *either*, which is false — two do, and one is `2728620c216` "v3d: force EZ off on Phoenix 26.2 — avoids constant depth-drain render wedge", a Phoenix-only change to that file explicitly about a render wedge. It does not touch the binning-config packet, so the conclusion stands, but the sentence as written was an over-claim in exactly the place a reader would check. **tile_alloc/tile_state use Mesa's own formula** (`v3d_util.c:272-313`, called at submit time with the real draw count) and its headroom term **saturates at 512 KiB by ~16 draws**, so q3dm1 and q3dm7 get the *same* ~570 KiB — face-independent by construction, and refuted anyway by the wedge dump (`bpcs=0x7f000`, i.e. 520 KiB still free, `bpoa=bpos=0`, `ovf_armed=0`). **The CT0 kick order matches Linux** (`PTB_BPOS`, QMA/QMS, QTS, QBA, QEA last). **No unflushed CPU→GPU write exists** ⚠ *scoped 2026-09-18: true of the **CL** submit path, which is what the evidence below is about. The **TFU** path had no barrier at all until `2bc4dd6` — but that is not a candidate here: TFU output is sampled later by CT1's TMU, is not fetched by CT0, and this row's signature is a CT0 front-end stall with `ct0ca` parked inside a valid BCL BO. See [`2026-09-18-dma-barrier-audit.md`](misc/2026-09-18-dma-barrier-audit.md).*: every binner-input BO is Normal-NC (no **caller** ever passes `V3D_CREATE_BO_CACHEABLE` — `v3d_resource.c:141-144` passes `V3D_CREATE_BO_SCANOUT` or `0` and nothing else (⚠ corrected 2026-09-17: this used to say the flag "has no user anywhere in Mesa's `src/`", which is false — it is consumed at `v3d_bufmgr.c:130`; the point is that no allocation requests it)), the per-frame BCL and the late in-place `TILE_BINNING_MODE_CFG` rewrite are drained by `dsb sy` before the first MMIO poke, and `9bac0f7`→`6cdf597` are consistent because `pmap.c:314-315` clean+invalidates any page mapped `MAIR_IDX_NONCACHED` at map time. ⓵ **One real divergence, new:** we write `CTL_MISCCFG = 0x05` (`V3D_QRMAXCNT=2`, `OVRTMUOUT`) at init (`v3d_gpu.c:553-555`); **Linux writes MISCCFG only for ver < 41, i.e. never on 4.2**, and our own cold probe reads the silicon default as `0x06` (QRMAXCNT=3, OVRTMUOUT=0). `QRMAXCNT=2` was A/B-tuned; the default 3 has never been benched, and the "OVRTMUOUT required by our Mesa build" comment is unverified. ⚠ **Do not change it now** — the bug has not reproduced, so an A/B would destroy the only signal there is. ✅ **That observation is now ARMED (2026-09-18, devices `560c398` + `663fd4d`).** A wedge prints where `ct0ca`/`ct1ca` parked relative to the submitted CL (`inside` / `at-end` / `before` / `after` / **`foreign`**), which BO covers that address, a byte dump around it clamped to the BO, and the opcode **at** `ca`. ⚠ **Two historical fields were reading the wrong byte** and old logs must be read with that in mind: `BIN … op=` sampled `ct0ca & ~3` (trustworthy only when `ct0ca & 3 == 0`) and `RENDER DBG … wedge_op=` sampled `ct1ca & ~0xf` (trustworthy only when `ct1ca & 0xf == 0`). ⓘ It had to be done **twice**: the first implementation went into the `rpi4-v3d` server, which is built but **not auto-launched** — the shipping games link the in-process winsys, so that is where the live path is. ⏭ **At the next recurrence, read `CL@ct0ca`.** Foreign content ⇒ BO aliasing survived the three fixes that landed after the last sighting (handle recycling `6502f67`, stale dirty lines `9bac0f7`/`6cdf597`, single-owner mappings `7a1e3db` + mesa `274ee5abea9`) — then re-run `V3D_BO_TRACE=1` + `scripts/analyze-bo-trace.py` and look for *temporal* overlap. A valid draw item with `FDBGS` stall bits and `int_qpu=0` ⇒ a genuine front-end starve, and *then* MISCCFG parity (`V3D_QRMAXCNT = -1`) is the A/B, scored by the q3dm7 `BIN TIMEOUT` rate. |
| freebin-corruption | **The allocator is the victim, not the origin — mechanism now identified (2026-09-12).** A free bin ends up holding a pointer to memory that is no longer what it thinks, and a live heap header's `size` field reads back with its **high 32 bits overwritten** (`0x80000001_0000d000` where the true size is `0xd000`). The process then dies computing `heap + heap->size`. The corrupting values decode as **RGBA8 pixels** (`A=128`, exactly 50% alpha, twice), and the identified route is a BO's CPU mapping having **two live owners**, so a page the GPU still uses is handed to `malloc`. ★ **Fixed and now MEASURED, not inferred (2026-09-15).** The pair devices `7a1e3db` + mesa `274ee5abea9` made the winsys the single owner of a BO mapping, and a `V3D_BO_TRACE=1` SuperTuxKart run on the current build scores **0 overlapping reuses in 2880 BO events** (`scripts/analyze-bo-trace.py`). ⚠ **Read the right number:** the same run shows **117 CPU addresses reused by more than one handle**, and that is *expected* once closed BOs are really unmapped — the historical "56" was measured with unmapping OFF, where any reuse meant two live owners. Only *temporal overlap* is a defect. Contained meanwhile: `heap->size` is now bounds-checked from above (libphoenix `11286c0`), and both bin hand-out paths validate their result. Eliminated, do not re-walk: insertion of a heap base, writes into a poisoned freed chunk (8 trials, 0 fires), intra-struct overflow, STK's drive-graph index, and a userspace buffer overflow (11/11 page-aligned siblings, p≈1e-27). | **Believed fixed, watching.** The last signature-A fault in the archive is 2026-09-12 16:38; the ownership fix landed the same day at 20:51, and **23 SuperTuxKart runs since show 0 faults** — which is evidence for *this* fix, not for the font patch 0019 (see `stk-highbits-pointer`). Not closed: the mechanism check is one run, and signature B (the stale-bin hand-out, `hbase?=0`) still fires and is a separate, contained thing. ⊕ **Measured 2026-09-17 — signature B is still LIVE and the BO mechanism does not explain it.** Across the 1 239 non-probe logs since the fix, **4** fired a containment guard with no fault at all (3× `free() of a corrupt chunk header` in SuperTuxKart on 09-15/09-16/09-17, 1× `chunk handed out twice` in vkQuake on 09-16) — every one of those runs kept rendering to the end of its window, so nothing looked wrong. ↩ *And the tool was not blind:* `uart-summary.sh` has carried these patterns since 2026-09-10 and re-grading those four logs with it now reports 23/1/5/71 — none of the four had ever been put through it (three ad-hoc verification runs, one graded only by `analyze-bo-trace.py`, whose PASS was reported without checking the log for faults). The showcase gate's own narrower regex genuinely could not see them; that is fixed separately. The 09-17 run is `botrace-now`: **71 guard fires in the same run that scored 0 overlapping BO reuses in 2 827 events**, so the residue is not the double-ownership route. Symbolised against the unstripped `prog/supertuxkart` (the shipped copy is stripped), three callers land in **Bullet physics** (`btCompoundCollisionAlgorithm::~btCompoundCollisionAlgorithm`, `btUnionFind::reset`, `btSimulationIslandManager::buildAndProcessIslands`); the other 71 resolve to `operator delete(void*)`, i.e. the guard records the C++ runtime rather than the site. ⛔ **That idea is dead, and it is worth knowing why:** the aarch64 target builds with `-fomit-frame-pointer` (`phoenix-rtos-build/target/aarch64.mk:20`), and the shipped `free()` proves it — its prologue is `stp x30, x19, [sp, #-64]!`, with **no frame record** (`stp x29, x30` + `mov x29, sp`). So `__builtin_return_address(1)` and any x29 walk have nothing to walk: level 0 is the only reliable caller. Getting the real site needs either frame pointers across libstdc++ and the port, or `.eh_frame` unwinding — neither is a one-line change. Do not re-propose "record one more return address" (measured 2026-09-18).  [`2026-09-17-stk-rate-and-allocator-guard-fires.md`](misc/2026-09-17-stk-rate-and-allocator-guard-fires.md). ⚠ **Corrected 2026-09-17 — it is NOT SuperTuxKart-specific**, which is how the "~2 runs in 23" framing reads. A census of the log archive for `large-bin lookup returned a non-chunk` puts it across **four different workloads**: the X desktop (`refresh-x`), **Quake II** (`refresh-q2`), **Quake III** (`soakq3-T2`) and STK (`stksoak-T1`, `stk-720p`) — so anyone hunting it in STK's allocator churn is looking in the wrong place. Latest occurrence **2026-09-16**, i.e. later than the "page reads back zero" family's 09-12 cutoff. Non-fatal in every case (frames continue immediately after). ⊕ **2026-09-17 census: 0 occurrences in 31 GPU runs** on the current build (runs where the winsys actually presented frames; 20 of them Quake II, a workload that has hit this signature before). ⚠ Weak evidence, stated as a bound: against the historical ~2-in-23 a clean 31 has roughly a 1-in-16 chance of happening anyway, so this narrows the rate rather than showing it gone. ⓘ The naive denominator here is 47 runs-reaching-psh — the wrong one, since libc-test and boot-only cycles do no GPU work. Re-score any suspicious build with `V3D_BO_TRACE=1` + `scripts/analyze-bo-trace.py`. ⊕ **Re-scored 2026-09-17 on the current tree** (first trace since the upstream merge and the two core changes): **0 overlapping reuses in 2 827 BO events** — 1 405 CREATE / 1 207 MMAP / 215 CLOSE over 1 220 cpu addresses, with 120 address reuses all occurring after the previous owner closed. So the double-ownership mechanism this family is hypothesised to run on is **not** present in the shipping build. ⓘ `export V3D_BO_TRACE=1` does work from psh, despite psh's other env-var limitations. Detail: [`docs/misc/2026-09-12-two-corruption-signatures.md`](misc/2026-09-12-two-corruption-signatures.md). |
| stk-highbits-pointer | ⊕ **RE-MEASURED 2026-09-17 — the rate below is historical: 0 in 143 STK engine starts since the 2026-09-12 20:51 BO-ownership fix (12 of them on the post-sweep tree), against 30 exceptions in the 242 pre-fix runs (12.4%) and 8 carrying this signature (3.3%), same detector both sides. p ≈ 3e-8 for the broad class, ≈0.012 for the signature.** [`2026-09-17-stk-rate-and-allocator-guard-fires.md`](misc/2026-09-17-stk-rate-and-allocator-guard-fires.md). Historically: SuperTuxKart died ~1 run in 10 with `far = 0x80000001_<valid heap address>` — a pointer whose **upper 32 bits are garbage** (bit 63 set is never a valid aarch64 userspace pointer). Symbolised to `FontWithFace::render`, and the signature **predates and survived** the drive-graph guards (seen 09-08, 09-09, 09-11, 09-12), so it is distinct from the fixed AI crash. The faulting `{_M_start,_M_finish}` pair is **self-consistent** (finish−start equals the element count, both words share the same bogus high half), so the pointer *arrived* bad rather than being overwritten in place. | Open. ⛔ **The STK font path is CLEARED as the origin (2026-09-15, source review).** The faulting `ldrb` is `font_with_face.cpp:787`, in the **collect** loop (630-805); the loops ports `b5ce28f` patches start at **834**, so that fix cannot have caused or cured this. There is **no out-of-bounds 4-byte store anywhere in `render()` or its callees** — every indexed store was checked and is in bounds, and `grab()/drop()` write an 8-aligned `ReferenceCounter`, not "8-aligned +4". The `m_cached_gls` dangling-reference theory is dead too (no `erase` anywhere, no re-entry into `getCachedLayouts`). ⇒ If a font-path store produced signature A, the store was **legitimate and the buffer was wrong** — the allocator handed the same memory to two owners, which also explains the self-consistent `{_M_start,_M_finish}` pair (another live object's coherent 16 bytes). ⚠ **23** STK runs since `b5ce28f` show 0 faults, but that is only **p ≈ 0.09** against the old rate and, per the above, would not be evidence even if it were significant. ★ Settled empirically too: `STK-FONT-OOB` — 0019's own guard — has fired **0 times in 28 post-fix STK logs**, so the fallback-bank bug it fixes was never live in this workload. 0019 is a correct fix for a real bug and is unrelated to this crash. ⚠ The "RGBA8 pixel" reading of `0x80000001` is **unconfirmed** — `SColor(alpha=128,0,0,0)` is `0x80000000`; nothing in the path produces `b=1`. **Same root cause as `freebin-corruption`** — and that cause is now identified and measured: a BO's CPU mapping with **two live owners**, fixed by devices `7a1e3db` + mesa `274ee5abea9` on 2026-09-12 at 20:51. ★ The 23 clean STK runs attach to **that** fix, not to the font patch: the last fault in the archive is 2026-09-12 **16:38**, four hours before it landed. ⊕ **2026-09-18 hunt with the new diagnostics in place (frozen, gated build): 10 long STK races (6-lap profiles, **31 806 frames**, the workload 3 of the 4 archived fires came from) — **0 guard fires, 0 faults**.** ⚠ A bound with its exposure stated, not a clearance: the archived rate is 4 in ~1 239 logs, so ten races cannot clear it — what it does establish is that the shipping build has been sampled with `why=`/`lheap?`/`freed?` in place, so the next occurrence arrives self-explaining instead of as another line of inference. ⏭ Make the next occurrence decisive: extend the 0016 guard to re-read `dfv[0]/dfv[1]` through a **`volatile`** pointer (a plain re-read is optimised away) and dump `gl[i-1..i+1]` plus `&gl[0]`, `size()`, `capacity()`. Neighbours foreign ⇒ whole buffer aliased; only the post-page-boundary half foreign ⇒ page-level. Review: [`docs/misc/2026-09-15-stk-font-path-cleared.md`](misc/2026-09-15-stk-font-path-cleared.md). Analysis: [`docs/misc/2026-09-12-stk-highbits-pointer-analysis.md`](misc/2026-09-12-stk-highbits-pointer-analysis.md). |
| allocator-double-free | libphoenix's allocator reports a double free and the process exits **70** (`EX_SOFTWARE`). **Not AF_UNIX-specific**: the report appears in 21 logs from 2026-06-28 → 09-09 across xcalc (×7), Dillo, vkQuake (×2), the glamor desktop (×5) and `test-libc-unix-socket` (×4). A generic heap-corruption symptom with several causes, some already fixed separately (the `vasprintf` overflow behind glib2/mc/Dillo; `free()` of `.rodata` in X11). ⚠ Only **one** occurrence in the whole archive captured block detail, so inferences from it rest on a sample of one — and two readings this row used to assert were wrong and have been corrected against the allocator source. | Open, rare; **no field occurrence since 2026-09-09** (⊕ re-confirmed 2026-09-17 against the whole archive: **0** of the 1 239 non-probe logs since 2026-09-12 carry this row's own signature — but 4 of them fired a SIBLING guard without faulting, see `freebin-corruption`, so read this as "the fatal path is quiet", not "the heap is clean") — plus a dedicated 10-run SuperTuxKart soak on 2026-09-16 (`stksoak2`) with **0 faults, 0 allocator events, 0 guard fires** and frame counts inside ±2% (1607–1672), so no trial degraded silently either. ⚠ That bounds the rate, it does not clear it. The report now names its caller plus both footers, which discriminate the mechanism on sight: `foot == size & ~3` means the allocator wrote the header (a genuine double free); a mismatch means an overrun or a planted footer. Resolve `caller=` with `addr2line` against the unstripped `prog/` copy. ★ **2026-09-18, source+archive review ([`2026-09-18-allocator-guard-residue.md`](misc/2026-09-18-allocator-guard-residue.md)): the failing blocks form an INTACT chunk grid, so the application did not smash them.** Per heap the 99 residue events are non-overlapping and 73 of 95 consecutive pairs are exactly adjacent, with valid sizes and flags — blocks this allocator really carved. That retires the whole application-corruption class (header mismatch, overrun, alignment-adjusted free, foreign-allocator free) and redirects the hunt to the heap's own recorded extent: the failing code is **6/8**, "the chunk is outside the range its heap claims". All four residue heaps are **`0xd000` (bin-14)** — the same class as the 09-12 fatal signature-A event. ⚠ Premise correction: the `why=` guard postdates every field fire, so **it has never fired outside the self-test**; the codes above are deduced from the printed fields. ⏭ Next: the corrupt-header branch now prints `hsize`/`hfree`/`hend?` for codes ≥ 5 (libphoenix, 2026-09-18, **unbuilt**) — `hend?=1` means a smaller heap was mapped over a released larger one, `hend?=0` with `hfree` inconsistent means the extent shrank in place. ⛔ Do **not** spend another pass naming the `operator delete` caller: it is aimed at the half the grid evidence just cleared, and `-fomit-frame-pointer` makes it impossible here anyway. |
| libc-uninit-main | **Two globals set by libc init were both unset at once** (**at least two** observations, not one: `rpi4b-uart-20260915-001244-opbench-T9.log` and, found 2026-09-17, `rpi4b-uart-20260907-115130-acq3-T1.log:142` eight days earlier with the *same* signature in the *same* process — `/bin/ntpclient`, PID 24 / thread 45, `esr=...92000007`, `far=0x30`, `pc=0x41bfb4` vs `0x41bff4`, i.e. a different build of the same binary). Previously filed as an "ntpclient Data Abort, stack-overflow shape" — that reading is **wrong**. Exact diagnosis: `far=0x30`, `esr=0x92000007` (read), `pc` resolves to `mutexLock(stream->lock)` in `fwrite`, libphoenix `stdio/file.c:777`; `0x30` is the `lock` offset in `FILE`, so the stream pointer was **NULL**. The stack dump carries the formatted string verbatim (`psh: ntpclient: unknown command\n`), which is `psh.c:211` `fprintf(stderr, ...)`. ⇒ **two** globals were unset at once: `stderr` (assigned only in `_file_init`) and psh's applet list (filled only by `__attribute__((constructor))` entries, one of which is `ntpclient` itself — `/bin/ntpclient` is the same 229 264-byte psh binary). Neither `_file_init()` nor the constructors had run. ★ **2026-09-15, from the ELF: the two zero globals share ONE 4 KiB page.** `psh_common` is at `0x438198` and `stderr` at `0x4383c8` — both in page `0x438000` of `.bss` (`stdin`/`stdout` too). And the fault's own register file was working on it: `x24=0x438198` (exactly `psh_common`), `x25=0x438000` (exactly that page base). ⇒ **one page reading back zero explains both at once**, which is a far smaller claim than "libc init never ran" — `_file_init()` and the constructors write into that page, so a zero read means those writes are not there. ⛔ **2026-09-15, source review: the mapping reading gets a CLEAN NEGATIVE, and this is NOT the same bug as `atexit-null-head`.** The two globals are not even on the same *kind* of page: psh's RW `PT_LOAD` splits into a file-backed COW page (`0x436000–0x437000`, where `.data` and `atexit_common` live) and a 3-page anonymous demand-zero mapping (`0x437000–0x43a000`, where `psh_common` and `stderr` live) — different populate paths, different data sources. And no kernel path was found that loses a write to that anon page: `process->lazy` is 0 on MMU builds so exec eagerly `_map_force`s every page; `pmap_switch` precedes `process_load`, so the BSS memset writes through the final mapping; every fault serializes on `map->lock` and a populated amap slot returns early, so two faults cannot yield two pages; there is no pageout/reclaim to drop a filled slot; and `_pmap_cacheOpAfterChange`'s flush-by-VA only fires for non-cached/executable descriptors, never for a cached RW data page. ⇒ **"those writes definitely execute" is the assumption the next occurrence must TEST, not inherit** — crt0 / `_libc_init` ordering is back on the table. ⓘ `atexit-null-head` is separately attributed **in-tree and the attribution holds**: `vm/object.c:285-309` records that `object_fetchCluster` silently zero-filled on a short/EOF NFS read and names `atexit_common.head` as the casualty; the fix added bounded retries plus the reporter that has seen 0 events in ~3645 processes. | Open, **two sightings** (2026-09-07 and 2026-09-15), no demo impact (ntpclient only sets the clock). ⏭ A recurrence now arrives **labelled**: psh's unknown-command path reports via `write(2)` instead of `fprintf` — it could previously die on the error itself — and says explicitly when the applet list is empty, which means lost `.bss` rather than a genuinely unknown command (utils). The era it comes from is the `premain-hang` bench, now fixed, so also re-check whether it recurs at all. ⓘ Hardened on the way past: `_file_init()` never checked its three `calloc()`s and dereferenced them immediately, so an allocation failure there was an instant NULL-deref — libphoenix now builds each standard stream through one checked helper. |
| ~~premain-hang~~ | **FIXED 2026-09-15 — and it was never a libc/exec/vfork problem.** `socket()` (kernel `posix/inet.c`) and libphoenix's `socksrvcall()` (reached via `getaddrinfo`) resolved `/dev/netsocket` with a **path lookup on every call**. A path lookup is answered by the filesystem that owns `/` — which `nfs-fs` **is**, and it is single-threaded — so its own loop thread sent the lookup to the port it was supposed to be servicing and deadlocked. libnfs opens a fresh socket whenever it must reconnect, so a dropped server stopped the whole filesystem and every process blocked on its first request; a launch hung while opening its own binary. ⓘ **Netboot/nfsroot only** — the SD demo image has an ext2 root and no `nfs-fs`. ⓘ An **old** defect, made reachable by the NFS-root takeover (2026-06-28) and turned from rare into periodic by the NFSv4 lease keepalive thread (2026-08-03); not a regression from the recent merges. | **Closed.** Fixes: kernel `7c021702`, libphoenix `47dde32` (cache the oid), ports `a44623a` (libnfs sync-call deadline, defence in depth), filesystems `427aabd` (reclaim on `-ETIMEDOUT`). Gate: **`./scripts/test-nfs-recovery.sh`** — restarts the host nfsd under a running Pi and grades reclaim + relaunch; **PASS** on the clean build (`noticed=1 reclaimed=1 launched=1 wedged=0 faults=0`), where it wedged 2/2 before. Manifest `manifests/2026-09-15-premain-hang-fixed.md`. ⚠ The withdrawn rate/regression claims recorded here earlier stand withdrawn; the method that worked was a `volatile` mark re-reported by a watchdog thread, not a pass-rate bench. |
| q3-qvm-recipe | Quake III's `pak1.pk3` (three QVMs built from **ioquake3**, needed because the free demo's 1999 QVMs report UI API 3 while quake3e requires 6) is **staged from `assets/quake3-qvm/` rather than rebuilt from source** — the QVM build recipe **is** in the repo. The game itself needs no retail content and no CD key; see `assets/quake3-qvm/README.md`. | ✅ **CLOSED 2026-09-17 — the gap was already fixed and this row had not caught up.** `tools/quake3-vm/build-quake3-vms.sh` (added 2026-09-03) builds the QVMs from ioquake3 pinned at `5883936`, including the whole host toolchain (`lburg` → `q3rcc`, `q3cpp`, `q3lcc`, `q3asm`). **Run end-to-end and verified against the committed pak:** all three QVMs build with the right sizes and magic `0x12721445`, the UI/GAME/CGAME API cross-checks pass, and against `assets/quake3-qvm/pak1-ioq3-vms.pk3` — `cgame.qvm` and `ui.qvm` are **byte-identical**, `qagame.qvm` differs in exactly **5 bytes**, which are the embedded `gamedate` string (ioquake3's `__DATE__`: `Sep 17 2026` vs `Aug  5 2026`). So the build is **bit-for-bit deterministic apart from the build date** and the staged asset is reproducible from source. ⓘ The stale `build/vm/` from an earlier interrupted run was empty, which is what made this look unfixed. |
| q2-sdl-openaudio-hang | **Quake II hangs at startup inside `SDL_OpenAudio()`** — the app prints `SDL audio driver is "phoenix".` and then produces **nothing** for the rest of the run. It never reaches yquake2's next line (`SDL audio initialized.`) **and never prints `SDL_OpenAudio() failed`** either, so the call did not fail — it never returned. Window is exactly `SDL_OpenAudio(&spec, NULL)` (yquake2 `src/client/sound/sdl.c:1614`) → `PHOENIXAUDIO_OpenDevice` (`sdl2/overlay/src/audio/phoenix/SDL_phoenixaudio.c:112`), whose only blocking syscall is `open("/dev/audio0", O_WRONLY)` at `:126`, plus SDL's audio-thread spawn. The screen stays on the fbcon console; the app never switches to video mode. | **CONTAINED 2026-09-18 — the symptom a demo would notice is gone; the mechanism is still open.** The driver graded the channel by `ACTIVE` with no `ERROR`, which the stall satisfies, so it called a **parked** channel started and then blocked in the first write. It now grades by **progress**: the ring plays free-running silence, so across the 20 ms arm settle a healthy channel walks `SOURCE_AD` **1 784-2 352 words** (measured, 10/10 boots) while a parked one moves at most the FIFO depth; the threshold is 64. On a parked channel it prints the full engine state (`DMA_DEBUG`, `CONBLK`, `SRC`, `DEST`, `LEN` — never captured before), re-arms the PWM up to 3x, and if it still will not stream degrades `/dev/audio0` to a **paced null sink**: writes are accepted at the real playback rate and dropped, so the app opens audio, runs at full speed and is silent instead of blocking. Not PIO (that spins on `STA.FULL1`, and a FIFO nothing drains is the fault) and not an instant return (that spins the caller's audio thread). Negative control 10/10 boots: arm 1, 0 re-arms, 0 null sinks, self-test 8960 samples. Whether a re-arm un-sticks a real stall is itself the next measurement, and the print says so. ↩ **Correction, and it invalidates this row's own headline evidence:** `DMA_CS=0x21` is **not** a stall signature — **9 of 10 healthy boots read exactly `0x21` while streaming**. The channel is DREQ-paced, so between DREQs it is always held (Linux: bit 5 = `ISHELD`, "held by DREQ flow control", `bcm2835-dma.c:131-137`; and "the ACTIVE flag in the CS register is not a reliable indicator", `:682-683`). Of the captured registers only *0 samples* and the full ring ever carried information. ⊕ The PWM **clock** start was also brought into line with `clk-bcm2835.c` (source and enable were changing on one bus cycle; `CM_GATE` was never set) — a race by construction and the top surviving candidate, but landed as upstream conformance, **not** claimed as the cure. **MITIGATED, not fixed — still open.** ⚠ An earlier edit of this row (mine) headed the status "✅ ROOT-CAUSED AND FIXED" while the same cell said "Open, rare" and the body said the DMA stall is not fixed. Retracted: what is fixed is the **blast radius**, not the defect. The stall itself is untouched, and the new short-write abort path has **never fired on hardware** (⊕ 2026-09-17: **44 consecutive boots** since the fix all completed the self-test — `self-test fed 8960 samples`, 0 aborts — including a deliberate 20-trial Quake II startup hunt (`q2hunt`) in which **20/20** reached `SDL audio initialized`, 0 stalled, frames 3988-4190 — which bounds the underlying stall rate and shows the change does not disturb the healthy path across many boots, not just the one verification run) — it is supported by code reasoning plus a healthy-path regression (self-test fed 8960 samples, Quake II rendered 6331 frames), not by an observed abort. Treat "relaunch it" as still the operative workaround. ⊕⊕ **2026-09-18 — the stall was CAPTURED three times (first ever), four hypotheses retired on evidence, and a separate 100%-of-boots defect found and fixed on the way.** What the captures say, identically each time: `DMA_CS=0x21` (**ACTIVE\|DREQ_STOPPED** — the channel is alive, parked for a DREQ that never comes), `PWM_STA=0x100`, FIFO **not empty**, ring **full** (`w=15 r=16`), `0 samples` moved. Retired: **the clock had not stopped** (`CM_PWMCTL=0x91`, BUSY, at the abort); **writes are not dropped** (`bin/pwmwrite`: 0 of 205 000 on the unused PWM0, at three spacings); **the period is not lost** (`RNG1=612` on 30/30 boots, and its reset value is 32, not 0, so a lost write could not stall it); **the bus error is not the trigger** (see below). ✅ **Fixed (devices `11f182a`): `audio_pwmInit()` latched a BUS ERROR on EVERY boot** — entry `PWM_STA=0x2` (BERR clear) vs ready `0x102` (BERR set) — because it wrote `PWM_CTL` three times back to back, which the BCM2835 doc names as the BERR trigger ("successive cycles to the same set of registers"). Now paced (read-back + 10 µs), BERR cleared, and `RNG1`/`CTL` verified with a named complaint if either does not take. **Verified 12/12 bench boots + 6/6 gate boots + 60/60 rate-run boots: BERR clear, `RNG1=612`, clean init.** ⊕ **And the rate run itself (2026-09-18, 60 boots on the FINAL gated build with no code changes mid-run): 0 stalls.** Against the pre-pacing 3-in-44 that is **p ≈ 0.015**, so this build is measurably quieter. ⚠ **That is a BOUND, not a cure, and the confound is named:** pacing the PWM writes changed init timing, and a rate that moves when timing moves is exactly what makes this a Heisenbug — the same reason 3-in-44 became 0-in-127 earlier across a *functionally neutral* relink. The mechanism stays open on the strength of the three captures. ⚠ **The stall itself is NOT claimed fixed and is not settleable by short runs: it is timing-sensitive.** Its rate moved **3 in 44 boots → 0 in 127** across a relink whose only audio change was a print inside the abort branch (unreachable when healthy), Fisher p ≈ 0.01 — so every instrument perturbs it. ★ `PWM_STA` bit 8 is **BERR**, bit 9 is **STA1**; swapping them (which I did for an hour) turns "a write failed" into "transmitting". Probe: `tools/pwm-write-probe`. **Rate: 2 of 136** Quake II runs that reached driver selection (~1.5%) — ⊕ corroborated 2026-09-17 by an independent measure: of the **41** Quake II runs since 2026-09-13 (the point from which yquake2 reliably emits `flipstat`), exactly **1 drew zero frames** — the same `sdgate2` run — i.e. 1/41, ~2.4%. Before that date a zero-frame reading means only that the build did not instrument the counter, so it is not a hang detector for older logs: 2026-09-04 (`q2fix1`, **netboot**) and 2026-09-17 (`sdgate2-q2`, **SD card**) — both lanes, so it is not SD-specific and not new. ⓘ Denominator basis: logs whose name marks them a Quake II run and which reached `SDL audio driver is "phoenix"`. The same string appears in ~336 logs across all engines; against that population the rate would be ~0.6%, but the hang has only ever been seen in Quake II, so the narrower denominator is the honest one to quote. Mitigation depends on the build. **Fixed builds** (devices `efac488`+): the stall is bounded to ~10 s, so a relaunch works. **Unfixed builds — including the `b95e983a` SD card** — must **REBOOT**: the self-test blocks the message loop for up to ~350 s, so `/dev/audio0` stays unopenable and an immediate relaunch hangs identically. It also clears on its own after ~6 min. ⚠ "just relaunch it" was the advice here and is wrong for an unfixed build. ⚠ **It passes the gate**: rc=0, psh prompt, 0 faults, `launched=yes` (psh echoes the command), and HDMI snapshots still count. The only signal is **zero flipstat frames** — grade GPU apps by frames, never by the gate's mechanical columns. ✅ **ROOT-CAUSED AND FIXED 2026-09-17 (devices `efac488`)** — and it was never an infinite hang. `rpi4-audio`'s boot self-test runs **before** `audio_thread()`, so no message is served until it finishes, while `/dev/audio0` already exists and has printed `ready` — hence a *blocking* open rather than a failing one. `audio_write()` bounds a stuck DMA at ~10 s and returns short, and the self-test feeds **35** chunks (the healthy log's `fed 8960 samples` = 35 x 256), so a stalled DMA costs up to **~350 s** of a device that answers nothing — longer than the 300 s capture, which is why it read as a hang. Fix: stop at the first short write, log it, and start the message loop; worst case ~350 s → ~10 s. HW-verified: healthy path unchanged (8960 samples, underruns=0, DMA) and Quake II reaches `SDL audio initialized` and renders 6331 frames. ⓘ The underlying DMA stall is NOT fixed — it is now bounded and announced (`self-test ABORTED ... write path stalled`) instead of silent. ★ **What the next occurrence will tell you, for free:** the abort prints `PWM_STA`, and its low bits discriminate the two directions — `FULL1` (bit 0) stuck set means the FIFO is full, i.e. the **PWM is not consuming** (clock/DREQ side); `EMPT1` (bit 1) means the FIFO is empty, i.e. the **DMA is not feeding** (ring/CB side). Healthy values for reference: `STA=0x102` at `ready`, `STA=0x700` after a good self-test. Static review found no unchecked-result race of the [[V3D clock]] kind: the PWM clock is stopped/div-set/re-enabled with bounded BUSY waits and returns -1 on failure, and the DMA start is confirmed ACTIVE-and-no-ERROR after a 20 ms settle before `dma_active` is set — so the trigger is a later runtime stall, not a bad init. ✅ **Done 2026-09-17 (devices `d13778d`)**: the abort now prints `DMA_CS` and the ring write/read cursors alongside `PWM_STA`, so a single occurrence decides which half stalled — `ACTIVE` clear = the channel stopped, `ERROR` = bus error, `write == read+1` = the producer is blocked on a full ring whose read cursor is not advancing. The PIO path gets its own message, since `ad.dma` is meaningless there. Printf-only in a branch that does not execute on a healthy boot; HW-verified that the healthy path is unchanged. |
| shader-cache-speckle | A **stale Mesa shader-cache blob** renders as green speckle over an otherwise-valid frame. Phoenix has no ELF build-id, so the on-disk cache keys on shader source only and a host toolchain change invalidates nothing: after the gcc-16.2 promotion the cache still held gcc-14-era blobs, the GPU executed stale QPU binaries, and SuperTuxKart's loading screen came up speckled. ⓘ Useful diagnostic tell — coloured speckle **over a valid frame** means stale-blob execution, whereas a real GPU wedge drops whole frames and logs one. | **Mitigated 2026-09-08 (netboot).** Two controls, and both are real: (1) `V3D_PHX_CACHE_VERSION` (`sources/phoenix-rtos-devices/gpu/rpi4-v3d/mesa/v3d_phoenix_stubs.c:44`, currently **1**) segments the cache dir as `<base>/v<N>`, so bumping it invalidates every blob; (2) host-side, `sync-netboot-tree.sh` hashes `libv3d-phoenix.a` / `libGL-phoenix.a` / `libv3dv-phoenix.a` and clears `<export>/.mesa-shader-cache` **only when the GPU driver actually changed** (before 2026-09-08 it cleared unconditionally). 🐞 **That optimisation was inert until 2026-09-16**: `prepare-buildroot.sh`'s `rsync --delete` deleted the fingerprint file on every prepare, so the next sync took its "(or first run)" branch and wiped the cache anyway — fixed by excluding it. ⚠ **What the cache is actually worth, per app:** vkQuake **~67 s** (67 shader modules, a black screen that reads as a hang); SuperTuxKart only **~5.5 s** — the oft-quoted "55.5 s cold vs 20.0 s warm ⇒ ~35 s of recompilation" is a **misattribution**, refuted by a controlled probe ([`2026-09-08-stk-time-to-race.md`](misc/2026-09-08-stk-time-to-race.md):84-102): 50 shaders "compiled" with **zero new cache entries** (all hits) and the run still took 49.8 s. ~27 s of STK's first-run cost is something else; leading hypothesis is contiguous-BO allocator warm-up. Cache dir is `$MESA_SHADER_CACHE_DIR`, else `$MESA_GLSL_CACHE_DIR`, else cwd-relative `./.mesa-shader-cache`. Manual escape hatch: `sudo rm -rf <export>/.mesa-shader-cache`. ⚠ **The `shader cache COLD` line is a DIRECTORY check, not a per-app one** (`v3d_phoenix_stubs.c:128`): it fires only when the cache dir is absent, so the first GL app ever run on a fresh root prints it and every later app stays silent even while compiling all of its own shaders. Measured 2026-09-17 — a six-app gate on a freshly flashed card printed it **0** times. Do not read silence as "warm". ⚠ The SD image ships **no** warm cache, so the first run of each GL app on a freshly flashed card pays the full recompile. |
| xterm-resize-artefacts | Owner-reported artefacts while dragging an xterm resize on the GPU X desktop. **Not reproduced** by a sound measurement; resize *correctness* is verified (server and client agree once settled). Most likely intermittent frames during a fast drag — this stack has no compositing and presents by GPU readback. | Open, unreproduced; re-check with the drag *released*. |
| ~~`vkq-tile-flicker`~~ | **vkQuake showed one-frame, screen-axis-aligned black tile blocks** because it rendered straight into the buffer the display was scanning out: no WSI swapchain, one VkImage backed by the fb0 pages, `storeOp=STORE` as the "present". V3D stores tiles one at a time, so a refresh landing mid-store mixed new and old tiles. Not a lightmap bug — a band crossed four surfaces with independent lightmaps at identical screen rows. | **FIXED 2026-09-15, ports `7ed2949`** (port-only; the winsys already had the machinery and the GL path already used it, which is why QuakeSpasm never showed this). vkQuake called `v3d_phoenix_set_scanout()` (records pa/bytes) instead of `v3d_phoenix_scanout_init()` (probes the granted virtual height, sets up buf1/buf2), so `v3d_phoenix_flip()` was a silent no-op. Now it creates one image/view/framebuffer per granted buffer, renders off-screen and flips after the device-idle wait. **HW-verified**: `3 buffer(s) TRIPLE-BUFFER+page-flip`, three images bound at distinct PAs, 0 faults, render unchanged (lum 27.4 vs 27.3, colours 1099 vs 1103). Escape hatch `VKQ_SINGLE_BUFFER=1`. ⚠ **The flicker RATE is still unmeasured** — the mechanism is gone by construction, but no validated detector exists (7+ variants failed), so this is not closed on a before/after number and no percentage should be quoted. |
| ~~`fbcon-freeze`~~ | ↩ **RETRACTED 2026-09-15 — this bug does not exist.** I read a static HDMI screen as a frozen console. It was static because **nothing further had been sent**: `record-showcase-clip.sh` set `--idle-secs` to *recording length + 30 s*, and idle-secs is how long the cycle waits for quiet **after each command** before sending the next — so with six commands the first one alone outlasted the whole 240 s recording. The "frozen" frame's BOTTOM shows `uname -a`, its output and a **live cursor at the prompt**; I had cropped only the top and middle of a 1080-row frame and concluded output stopped at the NFS takeover. Everything built on that — the takeover timing, the `/dev` re-bind lead, the inherited-stdio hypothesis — was explaining an artefact of my own harness settings. **Lesson: look at the WHOLE frame before declaring anything frozen, and check what the harness actually sent before blaming the target.** Fix is in the tool, not the system: `REC_IDLE_SECS` now overrides the pacing and the script warns when idle-secs × commands exceeds the recording. | **Not a bug. Closed 2026-09-15.** |

## System-level limitations

| Area | Limitation | Status |
|---|---|---|
| Board portability (TD-06) | The DTB parser assumes a single interrupt controller; only the 4 GB Pi 4B is validated (1/2/8 GB models **not merely untested — the 2 GB and 8 GB ones are known to be mis-mapped**: plo's syspage memory map is still hardcoded, which `TEMPORARY-FIXES-AND-FUTURE-CLEANUP.md` TD-15 records as "mis-maps 2/8 GiB boards — substantive, deferred to careful work". Corrected 2026-09-17; the earlier wording invited someone to just try an 8 GB board). | Known limitation. |
| Early-console alias — cross-board port (B5) | The generic aarch64 early-console path (`hal/aarch64/generic/console.c`) hardcodes the UART at the fixed VA alias `0xffffffffffe00000`, because it must print *before* the DTB-discovered base is available. On the Pi 4B this alias **equals** the discovered pl011 base, so it is correct and intentionally left as-is. It only matters when porting to another aarch64 board whose console is elsewhere. | By design for the Pi 4; a to-do only for a future cross-board port. |
| SError masked (TD-10) | Asynchronous SError is masked in early kernel paths because a live PCIe/VL805 USB external-abort SError is not yet root-caused; unmasking regresses boot. A dump-and-halt handler is implemented and armed for when the abort is fixed. ⚠ This is why an MMIO read of an unclocked block **hangs silently** instead of aborting. | Known, HW-gated. |
| `hal_memset` DC-ZVA (TD-20) | The `dc zva` fast path is disabled on the Cortex-A72 pending proof of the EL2 DC-ZVA trap state (does not reproduce in QEMU). Performance-only; correctness-safe. | Known limitation. |
| Stack-exhaustion signal DoS | A userspace process that **exhausts its 1 MiB main-thread stack** takes down the kernel: fault-signal delivery (`hal_cpuPushSignal`, aarch64) writes the signal frame to the now-unmapped user stack and double-faults at EL1, with no recovery — so any program overflowing its stack crashes the box (register-confirmed via `tools/stack-bomb/`). Fix = a guarded signal-frame push, minding the demand-paged-stack case. **Attended**: highest-blast-radius kernel path, and invisible to a green-boot smoke. | Known; turnkey work-order in [docs/done/2026-08-22-signal-push-stack-exhaustion-dos-workorder.md](done/2026-08-22-signal-push-stack-exhaustion-dos-workorder.md). |
| Windowed GL in X11 is bandwidth-bound (~10 fps), by architecture | A GL application drawing into a **window** copies its pixels GPU → CPU → socket → CPU → GPU every frame. Measured on the 640×480 demo window: **96 ms/frame (10.4 fps)**, of which `draw` is 2.7 ms and the rest is the round trip (`glReadPixels` 12.5 ms, packing 6.7 ms, `XPutImage` **58 ms** pushing 1.23 MB through the X socket). Full-screen GL is unaffected — the games bypass X and render straight into the scanout buffer (QuakeSpasm ~37–48 fps, Quake III 25–46). **Root cause:** no DRI3/DMA-BUF equivalent, so a client can only hand the server pixels, never a buffer reference. Cheaper mitigations all measured and closed: a larger AF_UNIX ring (+12%), poller contention (refuted), a cached readback alias (refuted), deferred present (no effect). | **By design for now — owner decision, 2026-09-09.** A point fix was designed and half-proven on hardware but closed deliberately: its ceiling was only ~1.7× (≈17 fps) because the DDX present survives the change, and it would leave the port with no general buffer-sharing story. The right investment, if ever needed, is a Phoenix-specific DRI/DRM layer. Reference: [docs/misc/2026-09-09-gl-window-buffer-sharing-work-order.md](misc/2026-09-09-gl-window-buffer-sharing-work-order.md). |
| ~~SD boot of the current image is unverified~~ | **RESOLVED 2026-09-17 — SD boot is now verified end to end, and this row is kept only so the old claim is not re-quoted.** The owner supplied a card, it was written with `b95e983a` (read-back sha256 byte-identical) and booted repeatedly: **6/6 clean boots** (`sdrel5`; kernel banner / fbcon / psh / lwIP / genet link all 6/6, 0 faults, every trial on `mmcblk0p2:ext2`), and the **full six-app showcase ran three times on the card — 17 of 18 app-runs, 0 faults, #67 torches present on every pass**. ⚠ *Corrected 2026-09-17:* this cell said "twice — 12/12". Re-grading every run by flipstat frames shows `sdgate2`'s Quake II drew **nothing** (the `q2-sdl-openaudio-hang` row above records that same run), so the second pass was 5/6. The shader cache also lands on the SD root and survives a reboot (27 blobs after a boot that ran no GPU app). ⓘ Both lanes coexist: dnsmasq up ⇒ netboot, down ⇒ SD boot — a *written* card does not block netboot (a **blank** one stops the Pi booting at all). | **Closed.** Detail: [`docs/done/2026-09-17-w38-sd-lane-and-fixes.md`](done/2026-09-17-w38-sd-lane-and-fixes.md). |
| Ethernet link IRQ (TD-Eth-LinkIRQ (⚠ status mismatch, flagged 2026-09-17: `TEMPORARY-FIXES-AND-FUTURE-CLEANUP.md` marks this **RESOLVED-BY-DECISION** — MDIO polling is the accepted answer — so it is a design choice recorded here, not an open limitation)) | The PHY's `INT_B` line is not routed to a GIC SPI on the Pi 4 board, so link state is MDIO-polled at 1 Hz (as Linux and U-Boot also do). | By design for this board. |
| SD write throughput | Reads use UHS-I **DDR50** (1.8V) + **SDMA** + multi-block CMD18 (~38 MB/s, ~86% of Linux on the same card); writes are multi-block CMD25 but **PIO** (~17 MB/s DDR50 / ~13 MB/s HS50 — a BCM2711 controller quirk corrupts DMA writes, so they stay PIO). Multi-block writes verified 0-corruption. The 1.8V/DDR50 switch is reliable on netboot and best-effort on SD-boot (falls back to HS50). | Reads fast; writes correct but PIO-bound. Remaining (attended) levers: DMA writes, reliable DDR50 on SD-boot. |
| Audio | PWM audio over the 3.5 mm jack works with a streaming DMA ring and a Quakespasm mixer backend; an audible end-to-end sign-off on real headphones is still pending. | Partial. |
| Netboot NFS-root: one narrow boot-order race | A command issued *before* the NFS-root `takeover` completes can still hit the pre-takeover RAM root and report `not found`; the clean fix is a `plo` boot-order gate. The test harness already covers it with `--inter-cmd-secs`. ⓘ The broader "NFS root is unreliable, prefer SD" framing this row used to carry is **not supported by measurement**: across **297 NFS-root boots on 2026-09-11/12, 297 reached the psh prompt (100%)** and there were **zero** `ERANGE` / `exec -12` / `NF4ERR_EXPIRED` lease-reclaim failures. (A host-side `nfs-server` restart is still the fix if a stale export does appear after days of uptime.) | Known, narrow; NFS root is the routine lab configuration and is reliable in practice. |

## Partial / not started

- **WiFi (BCM43455) — data plane proven at FULL MTU in both directions; the
  lwip netif is the remaining integration step.** The driver joins a real
  WPA2-PSK access point, completes the 4-way key handshake, **and carries real
  traffic over the air**: it obtains a full DHCP lease (DISCOVER → OFFER →
  REQUEST → ACK, confirmed by the AP's `DHCPACK`) via `tools/wifi-probe
  jointxcnt`. Beyond that lease the frame path carries **full-size ethernet
  frames both ways**, verified byte-for-byte: the Pi sends 400/1000/**1472**-byte
  UDP payloads that a host socket receives bit-exact, and reads back a host-sent
  1472-byte tagged probe unchanged. Two fixes were needed — SDIO byte-mode CMD53
  caps a transfer at 512 bytes (larger requests silently wrapped the 9-bit count
  field, so 525..2048 was a corruption band), and function 2's block size had
  never been programmed, which made block mode unusable on the data path. The
  driver exposes `/dev/wifidata` (write a frame / read a frame) as the seam for
  an lwip netif. That netif is **not** wired up, so arbitrary sockets do not use
  WiFi — for everyday networking use wired Ethernet (fully working).

  *Measurement note for anyone debugging this:* `tcpdump` on the host's AP
  interface does **not** see frames sent by the Pi — not even the DHCP that the
  host's own dnsmasq answers — so it is useless as an egress detector on this
  rig and has twice produced a false "no egress" conclusion. Use an
  application-level detector (a UDP socket, or dnsmasq's own logs);
  `scripts/wifi-air-monitor.sh` wraps the ones that work. (The earlier "TX
  reaches the firmware but not the air" report was a measurement artifact — a
  link-layer counter that does not count broadcast/pre-lease frames.) The
  proprietary Cypress firmware blobs are **not vendored** here
  (copyright/EULA hygiene); `scripts/stage-bcm43455-firmware.sh` stages them
  into a gitignored `.firmware/`.
- **Bluetooth (BCM43455) — driver-level bring-up only.** `/dev/hci0` comes up,
  the firmware patchram loads (323/323), a real BD_ADDR is read, and an HCI
  Inquiry completes. There is **no host Bluetooth stack** — no pairing,
  profiles, or audio — so it is not usable for real Bluetooth work yet.
- USB mass storage, I²C / SPI / PWM general-purpose drivers, and camera (CSI-2) /
  DSI display are not implemented.

