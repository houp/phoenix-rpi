# Week 2026-W38 (Mon 2026-09-14 → Sun 2026-09-20)

Phoenix-RTOS RPi4 — weekly working log. Kept **short on purpose**: this is the file you read.
Analyses: `docs/misc/`. History: `docs/done/`. W37: `docs/done/WEEK-2026-W37.md`.
Working detail for this week: [`docs/done/2026-09-14-w38-detail.md`](../done/2026-09-14-w38-detail.md),
[`2026-09-15-w38-fixes-detail.md`](../done/2026-09-15-w38-fixes-detail.md),
[`2026-09-15-w38-detail-2.md`](../done/2026-09-15-w38-detail-2.md).

**★ OWNER GOAL (2026-09-06):** a stable build with the games and X11 good enough for a public
presentation / a screen recording published online.

---

## 1. WHERE THINGS STAND — two calls are yours, nothing is blocking

**Everything is green on the current tree**, including the 10th upstream sweep adopted tonight (§5):
six-app gate 6/6 · libc 20 suites / 1 088 tests / 0 failures · endurance X 26 min, vkQuake 28 min /
48 825 frames, STK 6/6 — all 0 faults. **The card is a separate, older artifact** and still boots
`b95e983a` (§2). SD-lane evidence table from the overnight run: archived in
[`2026-09-17-w38-sd-lane-and-fixes.md`](../done/2026-09-17-w38-sd-lane-and-fixes.md) — headline: SD
boot 6/6, showcase **17/18** over three passes, shader cache survives a reboot, three endurance runs
clean.

✅ **…and that includes tonight's thirteen commits, built and gated together at 21:51**
(`barriergate` **6/6**, 0 faults, torches present; manifest `2026-09-18-w38-barriers-gated.md`): five
missing-barrier fixes across **audio / V3D TFU / xHCI event ring / SDHCI read / V3D mailbox**, the
binner-wedge CL dump, the ARMTRIALS degrade, three allocator diagnostics and one kernel one.
★ The barrier work is now **measured rather than argued** — see §3.
✅ **Call #1 is now cheap, and it points at the right build: your card is untouched.**
`artifacts/rpi4b/rpi4b-sd-2part.img`, sha256 **`6012dd0d…52fb597f`**, cut **00:33 from the gated
tree** — i.e. the tree that was gated 6/6, soaked 12/12 and endurance-tested 26 min, including all
thirteen of tonight's commits and the audio containment. Contents check **PASSES**, QEMU boot
**PASS** (plo → kernel entry, `nfs;/` refs=0 / `bcm2711-emmc` refs=4, i.e. a genuine SD blob).
**If you say "re-flash", it is a 3-minute `dd`**, not a 40-minute build. ⓘ Supersedes the 16:22
(`1060fa27…`) and 12:45 (`564a3473…`) images — do not flash either.
✅ **The netboot lane is back**, as planned: `--variant sd` overwrote the TFTP `loader.disk`, so the
loader was rebuilt (00:41) and the lane **re-verified with a real cycle** — `nfs;/;10.42.0.1;/;v4;takeover`
in the syspage list, kernel banner + psh + lwip + 0 faults. ⓘ Graded by the **root arg**, not by
`bcm2711-emmc`, which starts in both lanes and proves nothing.

⏸ **Your two calls, neither urgent:**
1. **Re-flash the card?** It carries `b95e983a`, which now predates the vkQuake demo loop, the audio
   bound, *and* the whole upstream sweep. The demo works as-is; I have not touched your verified card.
   ⓘ Cutting a fresh SD image costs ~20 min and temporarily breaks the netboot lane (§5), so it waits
   for your word rather than being done speculatively.
2. **TD-19 — add the `isb` to the TLBI helpers?** The doc's `dsb; isb` claim was false and is
   retracted; per ARM ARM the sequence is arguably incomplete. I deliberately did **not** touch it: a
   barrier on every TLBI is a global change to a correctness-sensitive path and wants you watching.

ⓘ **Leave the card in.** Bench is on the **netboot** default; both lanes are one command apart
(`netboot-server-down.sh` ⇒ SD boot, `-up` ⇒ netboot).

## 2. ✅ CARD + REEL — both settled, nothing left for you here

**Card:** you put it on `/dev/sda`, I wrote `b95e983a` (read-back byte-identical, `e2fsck` clean) and
you put it in the Pi. It has since booted 6/6 and run the showcase three times (§1). **Leave it in** —
both lanes stay available (`netboot-server-up.sh` ⇒ netboot, `-down` ⇒ SD boot). Fallback images
`da752ac1` and `16ad56f9` in `artifacts/rpi4b/` were verified intact 2026-09-17. 🧹 That directory
holds 20 GB across 13 images, 9 superseded — your artifacts, so I have deleted nothing.
⚠ **If Quake II hangs on the card, REBOOT — do not relaunch.** The card's image predates the audio
bound, so `/dev/audio0` stays unopenable for up to ~350 s (§4).

**Reel:** `artifacts/hdmi-video/20260916-160506-phoenix-rtos-rpi4-showcase.mp4` — 239 s, H.264
High@L4.0 1080p30 bt709, **faststart already set**: upload as-is, no remux. ⊕ **Re-verified 2026-09-18
with the HARDENED checker** (the one that now fails on an empty frame window instead of grading it
"ok"): **11/11 segments, PASS**, file intact (`ff6c3037…`, 146.8 MB). ⓘ It was recorded on the
pre-sweep build; the current tree renders the same scenes at the same rates, so it stays
representative — I have not re-recorded it without your say-so. Captions carry measured frame rates (vkQuake 43 fps average, STK 7–8), method documented above
the segment table in `make-demo-reel.sh`.
📦 **Publish kit ready** — [`2026-09-18-reel-publish-kit.md`](../misc/2026-09-18-reel-publish-kit.md):
exact chapter timestamps (straight cuts, no fades, so they are exact not approximate), a title, a
description, and the three caveats worth carrying (STK's 7-8 fps is the honest number, the reel has
no audio track, Quake III's camera is a spectator). Publishing is a paste job whenever you want it.

ⓘ Detail for both — provenance, the blank-card retraction, the vkQuake fps re-derivation:
[`docs/done/2026-09-17-w38-sd-lane-and-fixes.md`](../done/2026-09-17-w38-sd-lane-and-fixes.md).

## 3. ✅ This week — fixes, verification, publication hygiene

Detail: [`2026-09-17-w38-sd-lane-and-fixes.md`](../done/2026-09-17-w38-sd-lane-and-fixes.md) ·
[`2026-09-16-w38-verification-detail.md`](../done/2026-09-16-w38-verification-detail.md) ·
[`2026-09-17-stk-rate-and-allocator-guard-fires.md`](../misc/2026-09-17-stk-rate-and-allocator-guard-fires.md)

**Fixed 09-14→09-17:** `vkq-tile-flicker` · vkQuake **loops its demos** · every rebuild silently
wiped the shader cache · the audio hang bounded ~350 s → ~10 s (superseded today by containment) ·
upstream merge adopted (17 commits, two regressions caught). **Verified:** libc 20 suites / 1 131
tests / 0 failures · six-app gate 6/6 · X desktop 31.7 min netboot, ~33 min on the card · STK soak
10/10. ⚠ The GL window is bounded at 20 000 frames (~27–33 min) and exits normally — relaunch it.
**Audits:** the heap guard now names which of 8 checks rejected a block, and ~8 M allocator
operations found 0 violations, so the bug is not in its logic or locking · **13 graders that could
not fail** and **14 stale public-doc claims** fixed · both test-cycle scripts now grade their own
log. Detail: [`2026-09-18-w38-earlyweek-detail.md`](../done/2026-09-18-w38-earlyweek-detail.md).

**★★ THE AUDIO STALL: contained, shape known** (2026-09-18). Full record — every register, null,
citation and correction: [`2026-09-18-audio-dma-stall-captures.md`](../misc/2026-09-18-audio-dma-stall-captures.md).
✅ **CONTAINED — the one thing a live demo would notice is gone.** The driver called a **parked**
channel "started"; it now grades by **progress**, re-arms up to 3×, else degrades `/dev/audio0` to a
**paced null sink**, so the app runs silent instead of blocking. Gated 6/6 twice + a 10/10 negative
control; README's on-stage advice says this per build vintage.
✅ **Five real defects fixed on the way:** a bus error latched on 100 % of boots · a clock start that
moved the source mux and the enable in one bus cycle · the firmware's MASH divider inherited over an
exact integer divide · a re-arm check that could not fail · and **no memory barrier at all** between
the DMA control block and the MMIO kick. None is claimed as the cure.
★★ **The shape changed: per-BOOT, not per-arm** — ~14 000 in-process trials, 0 failures, including
4 000 arms of the real PWM1/DREQ 1/ch 5 path through a new diagnostic ioctl.

★★ **That audit found FOUR more, one live on the demo path**
([`2026-09-18-dma-barrier-audit.md`](../misc/2026-09-18-dma-barrier-audit.md)): the **V3D TFU submit**
kicked the engine with nothing ordering its source texels *or* the fresh PTEs — that is every GL
mipmap/blit and every Vulkan image copy — and the CL path's own comment made it *look* covered by
pointing at a barrier inside an `#ifdef` that ships off. Also **xHCI's event ring** read TRB fields
without ordering them against the cycle bit that publishes them (Linux has that barrier, with a
comment), **SDHCI reads** had none between "complete" and reading the payload, and the **V3D
direct-mailbox fallback** had neither direction. All four fixed. Checked and already correct: V3D
CL/CSD, genet TX+RX, xHCI doorbells, rpivid, rpi4-vcmbox. ⚠ Reasoning, not measurement — no observed
defect is attributed to any of them.
✅ **The seven pending commits were pre-flighted before the one build that gates them all**
(a compile error would have cost a 40-minute cycle): **build-safe** — no compile error under the
port's `-Werror`, no arch break (every touched file builds for `aarch64a72-generic` *only*, and
`xhci.c` already carried four unguarded `dsb sy`), every barrier on the correct side, and the new
32-byte `_IOWR` fits the inline `raw` path on both sides of the message ABI. Three "surely this is
already covered" traps were checked and cleared rather than assumed. **Nine reviewer findings, four
fixed now** (devices `HEAD~1`, `HEAD`): the ARMTRIALS printf re-read the registers it had just
latched, so the UART line and the ioctl result could disagree about the very cycle being captured ·
`trials` never reported being clipped · the server and winsys wedge verdicts were **not** identical —
the server called a BO with no CPU view `foreign`, which is the exact branch the observation keys on ·
and the `audio_write()` barrier's comment claimed a guarantee a free-running engine cannot give.
Banked, not fixed: the two CL dumps share a log prefix (grep noted in the code), ~100 lines are
duplicated between the server and the winsys copy, and `RPI4AUDIO_ARMTRIALS` is a permanent
diagnostic in a published header that can block the driver's only thread for ~100 s.

★★ **The allocator's silent guard firings are NOT a smashed header — the application is cleared**
([`2026-09-18-allocator-guard-residue.md`](../misc/2026-09-18-allocator-guard-residue.md)). The 99
residue events form an **intact chunk grid** per heap — non-overlapping, 73 of 95 consecutive pairs
exactly adjacent — so these are blocks the allocator really carved; the heap's recorded **extent**
stopped covering blocks still live in it (code 6/8). ⚠ The `why=` guard **has never fired in the
field**: it postdates every archived event. ⛔ "Name the `operator delete` caller" is retired — it
targets the half the grid evidence cleared, and `-fomit-frame-pointer` makes it impossible anyway.
★ **An unchecked store would produce exactly this:** `_malloc_heapAlloc()` validated the address
`mmap` returned against *released* heaps only, then wrote the new heap's size at that base — so a
region handed back over a **live** heap shrinks its extent in one store. Now checked and reported.
ⓘ **The kernel refuted that route:** `_map_find` returns only exact leaf gaps and every way its
bookkeeping goes stale makes gaps look *too small*; fresh anon pages are eagerly zeroed. ★ What
survives needs no kernel bug — heaps pack **back-to-back**, so a chunk whose `->heap` names the
*preceding* heap reads identically, and `hend?=1` is exactly that adjacency.
🔧 Two discarded return values fixed: the kernel dropped `_map_add`'s `-EEXIST` (*the* overlap
signal), the allocator dropped `munmap`'s. ⚠ The new overlap check itself had the **"grader that
cannot fail"** shape — making it testable found it **skipped the total overlap**, and that a planted
test heap is discarded by `malloc_heapSizeValid`, so three positive cases passed against a check that
never ran. Four harness cases now prove it reports. ✅ **TD-22 fixed** (kernel, **unbuilt**): `_map_find`'s
right-hand leaf return could hand back a non-`MAP_FIXED` hint sitting nearer a gap's end than `size`,
overlapping the next entry. The bound has been in the source all along but **commented out** — and it
could not simply be restored, because on that `if` `rmaxgap` is a *subtree* maximum, so gating the
descent on it would refuse subtrees that do have room. The check went at the **leaf**, where the value
is that node's exact gap; a refusal falls through to the parent walk. ⏸ Gate before believing it:
every process start maps through this path.
✅ **The 25-boot rate run: 25/25 healthy on the FIRST arm**, 1784–1880 ring words, **0 stalls, 0
re-arms, 0 degrades, 0 faults** on the frozen build. ⚠ **Underpowered — a weak bound, not a result:**
against the published rates (~1 boot in 41–70) a clean 25 has probability **54–70 %** even if nothing
had changed; ruling the stall out at 95 % needs ~121 boots (≈ 12 h). What pinned the defect was the
in-process work, not this bench. 💾 The frozen loader is kept as
`artifacts/rpi4b/loader-frozen-stallrate2.disk`; copy it back over the bootfs `loader.disk` (that
directory *is* the TFTP root) to measure that exact build again.
✅ **Built + gated 21:09–21:51** (`--scope core --with-tests --with-ports --with-showcase`): the
thirteen commits above — five barrier fixes, the wedge dump, the ARMTRIALS degrade, the allocator's
extent/overlap/munmap checks, the kernel's overlap report. **Six-app gate 6/6**, 0 faults, torches
present, real flipstat frames on all five games; manifest `2026-09-18-w38-barriers-gated.md`.
✅ **Verified on the artifact, not the stage list:** sysroot `libphoenix.a` and the staged *and*
live-export `supertuxkart` all carry `OVERLAPPING a live heap`, where every one was **0** before the
build — the games are framework ports whose final link is unconditional, so `--scope core
--with-ports` does relink them. ⓘ STK's CMake link error in the build log is the **by-design** stage-2
failure; `M3 complete` is the line that says the real link happened.
ⓘ **No allocator or kernel guard fired anywhere in the gate** — armed and present in the binary, but
not exercised: the residue shows up in a minority of STK runs and this was one run.
✅ **12-run STK soak on the new build: 12/12 clean** (`stkguard`, 22:14–23:18, the gate's 2-lap
hacienda profile) — **0 faults, 0 allocator guards, 0 kernel guards**, audio healthy on the **first**
arm in all twelve, and **flipstat 47 in every single run**. That is the stability evidence the
thirteen commits needed beyond one gate: they touch every `mmap` and four DMA hot paths.
ⓘ **The heap residue did not reproduce**, so `hend?` is still unread — ~21 STK runs since the guard
was armed, 0 fires. It stays armed; nothing more to do until one fires.
✅ **X desktop endurance on the new build: ~26 min, 0 faults** (`xendure`, 23:34–00:01, 102 HDMI
frames) — **0 allocator/kernel guards, 0 render timeouts, 0 wedges**. The V3D **TFU** barrier is the
one fix that is live on the demo path (glamor blits continuously), and duration is what a single gate
cycle cannot cover. ★ **Graded by CONTENT, not by a clean log:** the 00:01 frame shows the V3D GL
window still spinning its 3D scene, `python3` Game of Life at **gen 26762 / 17.5 gen/s**, `top` with
30 live tasks (v3d, glamor daemon, wmaker, xterm, xclock, xbill, nfs, lwip) and `xclock` reading the
correct wall time. 📸 `artifacts/hdmi/20260919-000105-xendure-tick.png` is the best single "whole
system working" still produced so far — worth considering for the reel.
🔧 ⚠ **Two traps caught in the tooling, both mine.** (1) `stkguard` collided with a 2026-09-10 bench,
so the first grep over `*stkguard-T*` returned **seven** logs for a four-trial-old run — two builds
reading as one clean series; only the duplicate `T1/T2/T3` gave it away, and the bench now warns on a
reused label. (2) I edited `test-cycle-bench.sh` **while it was running**: bash reads scripts by byte
offset, so the running copy resumed mid-token, printed a line from an unrelated branch (`lane: SD
CARD`) and died with a syntax error `bash -n` cannot reproduce. Only the bench's own summary was
lost — all 12 logs are intact and the lane is still `nfsroot`.
★★★ **The barrier race is MEASURED, not argued** — `pwmdma --cb-race`, 5000 trials per arm:
**with `dsb sy` 5000/5000 correct, 0 stale; without it 146 stale** (2.9 %, first on trial 0). A
"stale" fetch is the DMA engine reading a control block's *previous* contents — bytes the CPU had
already overwritten. That is exactly the Normal-NC → Device hazard the five barrier fixes were landed
against, so the audit stops being reasoning and becomes a result. ⚠ The control arm's null **bounds**
the rate rather than proving the ordering is architecturally guaranteed, and neither arm covers a
first-ever fetch of freshly `mmap`'d memory (the probe reuses one CB).
⏳ **A 100-boot rate bench is running on the barriered build to test it** (`barrierrate`, from 01:15,
~5.8 h). This is the only instrument that can: the hypothesis predicts the stall rate goes to **zero**
now that `audio_dmaArm()` orders its control-block stores. 100 boots puts P(0 | unchanged) at
**8–24 %** against the published ~1-in-41-to-70 — not proof, but the first run with real power behind
it (the 25-boot run had 54–70 %). ⛔ **No rebuild until it finishes** — that would overwrite the TFTP
`loader.disk` under the measurement. 💾 The build under test is preserved as
`artifacts/rpi4b/loader-barriers-21_09.disk` (sha256 `58fd8641…`).
🔎 **Does that race explain the audio stall? Plausible, untested — and I checked rather than
assumed.** The arm wrote a control block and kicked with **no barrier** until tonight, happens **once
per boot**, and `MAP_CONTIGUOUS` is not zeroed — a first fetch that missed our stores hands the
channel a garbage `SRC`/`PERMAP` and parks it exactly as captured. ⛔ But the archive **cannot**
settle it: all three captures predate the `CONBLK`/`SRC`/`DEST`/`LEN` fields, and what they do show
(`PWM_DMAC=0x80000804`, `PWM_CTL=0xa1a1`) are CPU-written MMIO registers, not CB-loaded ones.
⚠ **And the 14 000-arm null does not argue against it:** `audio_dmaArm()` rewrites **nothing** in the
control block, so every re-arm fetches identical bytes — a stale fetch returns the right values and
streams fine. **ARMTRIALS is structurally blind to this**; only the first arm on a fresh page can
fail. ⓘ Rates do not transfer either (2.9 % is a CB-fetch rate on another channel at one preload
depth) — the overlap with ~1-boot-in-41-to-70 is *consistent with*, not *predicted by*. ⏭ It now
tests itself: both the barrier and the CB fields ship, so the next stall on this build is informative
either way.
🔧 **It took three fixes to the probe to get there, all one fact:** `abort+RESET` clears `TXFR_LEN`
and `CONBLK_AD` on BCM2711 but **not** `SOURCE_AD`. That broke the sentinel (refused every run), the
readback poll (exited before polling — 4969/5000 "not fetched", ungradeable) and, worst, the
classifier, which treated "not fetched" as `len==0 && src==0` — so a retained address would have
landed in a variant window and scored as a **stale fetch**, manufacturing the very result the probe
exists to test for. ⓘ Both of the probe's own refusals fired correctly and stopped a bad number being
reported; only the third run graded.

↩ Corrections banked: `DMA_CS=0x21` is the **healthy** state (9/10 boots read it while streaming), so
the captures' headline evidence was worthless · `CM_GATE` does not read back on this clock · the CL
dump added for the binner wedge landed on `rpi4-v3d`, which is **not auto-launched** — the shipping
games use the in-process winsys. ✅ Now armed there too (devices `663fd4d`), and it exposed that **two
existing fields read the wrong byte**: `BIN op=` sampled `ct0ca & ~3` and `RENDER wedge_op=` sampled
`ct1ca & ~0xf`, so old readings only hold when those were aligned.
✅ **Also fixed:** picocom kills its process group — that had been killing every boot-only bench at
trial 1 *and* hiding the netboot stage table (capture `a7514505f`).
📣 **Public docs caught up:** README + hardware matrix carry the containment; the reel has a publish
kit (chapters, title, description, caveats) at [`2026-09-18-reel-publish-kit.md`](../misc/2026-09-18-reel-publish-kit.md).
**📊 A clean day, measured not assumed:** all **238 UART logs / 56 MB** from 2026-09-18 through the
full fault regex: **0 fault patterns** (positive controls fire, so the detector is not vacuously
clean). ⓘ Quake III's gate frames moved 10 918 → 9 498 today; **not** a regression — fourteen archived
gates span 8 993-10 918 with everything else flat.
⏳ **Nothing built yet, deliberately:** a rebuild overwrites the TFTP `loader.disk` and the rate run is
measuring the *frozen* build. Everything above builds when it finishes.


**Same method, next defect — no capture yet:** 10 long STK races on the frozen build (6-lap profiles,
**31 806 frames**, the workload 3 of the 4 archived heap-guard fires came from) — **0 fires, 0
faults**. A bound with its exposure stated, not a clearance. What is banked: the shipping build is now
sampled with `why=`/`lheap?`/`freed?` in place, so the next fire says in one line whether a block
outlived its heap, a stale pointer's address got reused, or `->heap` was smashed.

## 4. Open bugs — all rated livable

| bug | what it does | live with it? |
|---|---|---|
| "page reads back zero" family — `libc-uninit-main`, `stk-highbits-pointer` | an app occasionally dies at startup or mid-run | **Yes.** Relaunch it. ⊕ **STK re-measured 2026-09-17: 0 crashes in 143 engine starts** since the 2026-09-12 BO fix, against **30 in the 242 runs before** (identical detector both sides, p ≈ 3e-8) — the "~1 run in 10" figure is historical. ⚠ *Corrected 2026-09-17:* this cell used to say "no fault since 2026-09-12", which its own membership contradicts — `libc-uninit-main`'s sighting is **2026-09-15**, and it now has a second, earlier one (2026-09-07, identical signature). The `far=0x8000…` signature proper is clean since 09-12. |
| `V3D-binner-wedge` | GPU binner stalls, auto-recovers via reset | **Yes.** Hunted 2026-09-17 on its own reproducer (`q3dm7`): **0/6, 37 057 frames, 0 faults**. Cumulative 15 runs / ~123 k frames, 0 wedges; last seen 2026-08-22. ⚠ A bound on an intermittent, not a clearance. |
| `q2-sdl-openaudio-hang` | Quake II hangs at startup in `SDL_OpenAudio` | **Yes.** 2 of 136 Quake II runs (~1.5%), both lanes. ⊕ **CONTAINED 2026-09-18 — the symptom a demo would notice is gone.** The driver now grades the DMA channel by *progress* (healthy 1784–2352 ring words per 20 ms against a 64-word threshold) instead of by `ACTIVE`, re-arms the PWM up to 3× printing full engine state, and otherwise degrades `/dev/audio0` to a paced null sink, so a stalled channel costs silence rather than a blocking write. Gated 6/6 twice plus a 10/10 negative control. **The mechanism is still open**, but its shape is pinned: ~14 000 in-process arm trials — including 4 000 on the real PWM1/DREQ 1/channel 5 — produced **0 failures**, so it is **per-BOOT, not per-arm**. Five real driver defects fixed on the way (BERR on 100 % of boots, a one-bus-cycle clock SRC\|ENAB, an inherited MASH bit, a re-arm check that could not fail, and no memory barrier at all). |
| `allocator-double-free` + `freebin-corruption` | a heap guard fires; the fatal variant exits 70 | **Yes.** ⊕ **2026-09-17 census:** 0 of the 1 239 non-probe logs since the BO fix carry the fatal double-free signature — but **4 fired a containment guard** (3× corrupt chunk header in STK, 1× chunk-handed-out-twice in vkQuake) and kept rendering to the end, so nothing looked wrong. ↩ `uart-summary.sh` would have flagged all four (23/1/5/71) — none of them was ever run through it, including one I reported on the same morning from its BO trace alone. Three callers symbolise into **Bullet physics** teardown; the 71-fire run is the same one that scored **0 BO overlaps**, so the double-ownership route does not explain the residue. ⏭ the guard records `operator delete`, not the site. ★ **2026-09-18 — the application is cleared.** The 99 residue events form an **intact chunk grid** per heap: non-overlapping, 73 of 95 consecutive pairs exactly adjacent, valid sizes and flags. Those are blocks this allocator carved, so no header was smashed — the heap's recorded **extent** stopped covering blocks still live in it (code 6/8). ⚠ And the `why=` guard **has never fired in the field**: it postdates every archived event. Next reading comes from `hsize`/`hfree`/`hend?`, now printed (unbuilt). [`residue analysis`](../misc/2026-09-18-allocator-guard-residue.md) ⛔ **The old idea is dead, and it is worth knowing why:** the aarch64 target builds with `-fomit-frame-pointer` (`phoenix-rtos-build/target/aarch64.mk:20`), and the shipped `free()` proves it — its prologue is `stp x30, x19, [sp, #-64]!`, with **no frame record** (`stp x29, x30` + `mov x29, sp`). So `__builtin_return_address(1)` and any x29 walk have nothing to walk: level 0 is the only reliable caller. Getting the real site needs either frame pointers across libstdc++ and the port, or `.eh_frame` unwinding — neither is a one-line change. Do not re-propose "record one more return address" (measured 2026-09-18).  [`analysis`](../misc/2026-09-17-stk-rate-and-allocator-guard-fires.md) The GPU-side hypothesis (a BO freed while still mapped) was re-scored 2026-09-17 on the current tree — `V3D_BO_TRACE=1` over an STK race: **0 overlapping reuses in 2 827 events**, all 120 address reuses after the previous owner closed. ⚠ A bound, not a clearance. |
| ~~`q3-qvm-recipe`~~ · `shader-cache-speckle` · `xterm-resize-artefacts` | ✅ **closed** — Quake III's QVM pak IS reproducible from source (§3d) · known cosmetic tell · unreproduced drag artefact | **Yes**, all three. |

Single source of truth: [`docs/KNOWN-ISSUES.md`](../KNOWN-ISSUES.md). ⓘ Reconciled against it 2026-09-17: `q2-sdl-openaudio-hang` and `allocator-double-free` were missing here, and `atexit-null-head` was listed as an open bug although it is root-caused, fixed in-tree, has 0 events in the whole log archive and no row there — dropped.

**Publication pass (2026-09-17):** README's two stale public claims fixed — SuperTuxKart "still faults
intermittently in its own code" (falsified: 0 in 131 runs) and Quake III's "the QVM build recipe is not
yet in this repo" (it has been since 09-03, verified byte-wise). STK's frame rate on the README now
comes from the page-flip counter, not the game's on-screen number. A read-only audit of the rest of the public docs found **11 more**, all fixed:
the README carried a *second* SuperTuxKart paragraph asserting the **opposite** of the measurement
("fill-rate/bandwidth bound", plus the retracted fault claim and HUD frame rates), and the
**hardware-support matrix** — which the README calls authoritative — still had **#67 open**, "the
wall torches are still missing", the closed QVM reproducibility gap, and SD-write work marked
"HW-blocked (no card)". `PHOENIX-RTOS-RPI4-CHANGES` still said SD boot was unverified and that
KNOWN-ISSUES *deletes* resolved rows (it strikes them through). Two HUD-sourced frame rates replaced
with page-flip figures.

## 5. Upstream

🔧 **One sibling was invisible to every sweep.** `phoenix-rtos-usb`'s master had no upstream tracking
ref, so `git-siblings.sh` printed a quiet `?` for ahead/behind — ten sweeps have said "all siblings 0
behind" without ever comparing it. It *is* 0 behind (41 ahead, checked directly), so nothing was
missed; but nothing was checking. The column now says **NO UPSTREAM — SWEEPS SKIP THIS REPO** with
the repair command, and tracking is set. Same cannot-fail shape as the graders fixed on 09-17.


✅ **Three SD images were cut today; only the last matters** (§1, sha `6012dd0d…`) — **prepared, NOT
flashed**, your card is untouched pending call #1. `--variant sd` overwrites the TFTP `loader.disk`
(no `nfs;/`) every time, so the netboot loader was rebuilt straight after each cut and the lane
re-verified with a real cycle **three times**, graded by the root arg (`nfs;/…;takeover`) rather than
by `bcm2711-emmc`, which starts in both lanes.


✅ **10th sweep ADOPTED (2026-09-17, 27 commits) — all six masters moved, everything 0 behind.**
The risky one: signal handling **moved into the kernel**, **`signalPost` removed**, `threadsinfo()`
reshaped. Resolved on branches first, master untouched until every gate passed. libphoenix `434f6de`
· kernel `15fdea32` · utils `ce472cb` · tests `c7ccfc8` · project `fe60daf` · ports `dd3628a`;
manifest `2026-09-17-w38-upstream-sweep-10.md`; nine conflict resolutions written up in
[`…-sweep-10-conflict-census.md`](../misc/2026-09-17-upstream-sweep-10-conflict-census.md).

**Verified before master moved** — full-clean build · stale census **0 of 355** · libc **20 suites /
1 088 tests / 0 failures** (incl. `signal` 32/0, upstream's own new tests against my resolution) ·
six-app gate **6/6**, torches present, frames within 1% of pre-sweep · `ps`/`pm` fields all populated
· endurance: X **26 min / 19 980 frames** (frame time 63.7 → 64.8 ms), vkQuake **28 min / 48 825
frames** (26.07 → 26.57 fps, 1 744 vs 1 726 frames/min pre-sweep), STK **6/6** — every one 0 faults.
⇒ post-fix STK streak now **0 crashes in 143 engine starts**.

✅ **The gap the endurance runs left is closed:** the 26-min soak proved the desktop *runs*, not that
it *exits* — and desktop exit is where the historical dead-Pi wedge lived, in the `vm/map.c` fault
path I hand-merged. Six `startx_gpu --quit-after 18 action` start+exit cycles on the merged tree:
**6 starts, 6 clean `session ended — shutting down X`, 0 faults.** ⚠ A bound, not a clearance — the
old wedge was ~1 in 6, so 6/6 is p ≈ 0.33 on its own; it sits on top of 20/20 pre-sweep.

⚠ **Two things it cost:** WindowMaker called `threadsinfo()` with the old 2-arg form (fixed with
`PH_THREADINFO_ALL` — *not* `flags = 0`, a legal call that fills **nothing** and would have emptied
window titles silently), and the nine hand-merged hunks. ⓘ **Behaviour change:** the default `umask`
is now **0**, not 022.

- **9th sweep (09-16): 17 commits merged, tree GREEN**, two regressions caught — a **syscall ABI
  break** (`phMutexLock` 1→3 args) and a cond-clock default flip, both fixed. 🔧 Lesson kept: run
  `check-stale-binaries.sh` after any merge touching libphoenix or a syscall signature, **before**
  believing a gate. Detail: [`2026-09-16-upstream-merge-detail.md`](../done/2026-09-16-upstream-merge-detail.md).
- 7th/8th sweeps (09-13, 09-14): nothing behind.
