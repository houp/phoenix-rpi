# Week 2026-W38 (Mon 2026-09-14 → Sun 2026-09-20)

Phoenix-RTOS RPi4 — weekly working log. Kept **short on purpose**: this is the file you read.
Analyses: `docs/misc/`. History: `docs/done/`. W37: `docs/done/WEEK-2026-W37.md`.
Working detail for this week: [`docs/done/2026-09-14-w38-detail.md`](../done/2026-09-14-w38-detail.md),
[`2026-09-15-w38-fixes-detail.md`](../done/2026-09-15-w38-fixes-detail.md),
[`2026-09-15-w38-detail-2.md`](../done/2026-09-15-w38-detail-2.md).

**★ OWNER GOAL (2026-09-06):** a stable build with the games and X11 good enough for a public
presentation / a screen recording published online.

---

## 1. STATE OF PLAY — is anything blocking? **No.**

✅ Nothing in flight, nothing blocking. Both deliverables are done, gated, and waiting only on you:

- **Reel** — `artifacts/hdmi-video/20260916-160506-phoenix-rtos-rpi4-showcase.mp4`, 239 s, 11 segments,
  all captured 2026-09-15/16 on the current build. `verify-demo-reel.py` **11/11**; bt709-tagged. (§2b)
- **Image** — `rpi4b-sd-2part-gated-b95e983a.img`, gated **12/12** across two showcase passes plus
  contents / `e2fsck` / boot-target checks. (§2) ↩ `da752ac1` (pre-merge) and `16ad56f9` are fallbacks.

⚠ **One state fact:** the netboot export is **ahead of every image on disk** — it carries the merged
tree *plus* the vkQuake demo-loop fix (gated 6/6 again, §3e), while `b95e983a` was cut before that fix
and `da752ac1` before the merge. Anything re-recorded over netboot comes from that newer tree, not from
the image you flash. The difference is inert for the image: it ships no `phoenix-demo.cfg`, so vkQuake
boots `map start`, which disarms the demo loop anyway.

## 2. ⏸ YOUR ONE ACTION — flash `b95e983a`

`artifacts/rpi4b/rpi4b-sd-2part-gated-b95e983a.img` — the most up-to-date image, as you asked. Cut from
the upstream-merged tree, gated 12/12 across two showcase passes, plus contents / `e2fsck` / boot-target
checks and the vkQuake fix verified inside its own ext2 with a negative control.
Manifest `manifests/2026-09-16-demo-image-b95e983a.md`.

```
udisksctl unmount -b /dev/sda1 2>/dev/null || true
sudo dd if=artifacts/rpi4b/rpi4b-sd-2part-gated-b95e983a.img of=/dev/sda bs=4M conv=fsync status=progress && sync
```

⛔ **There is no SD card in the lab — I checked properly this time, and it changes what you need to
bring.** The host has no `/dev/sda`, and a Linux netboot of the Pi shows **no `/dev/mmcblk*` at all**, so
there is no card in the Pi either. SD boot therefore cannot be exercised here for two independent
reasons, and you really are the first to boot this image from a card.
💡 **If you leave a card in the Pi, I can test SD boot unattended from now on.** The EEPROM was written
2026-05-21 with `BOOT_ORDER=0xf12` — **network first, SD second** — so a card does *not* block netboot;
SD boot is simply what happens when I bring the netboot server down. That plus a Linux netboot that can
`dd` to `/dev/mmcblk0` is a complete self-flash loop, no card swapping. (The `rpi4-run` skill said the
opposite — "card-in forces SD boot, remove it" — and has been corrected locally.)

★ **Warm each GPU app once during setup, never in front of the audience.** The first GL/VK run on a
fresh card compiles shaders and is slow — vkQuake's cold start is **~67 s of black screen** (67 shader
modules), which reads exactly like a hang. It announces itself: `v3d: shader cache COLD
(./.mesa-shader-cache/v1)`. The SD root is persistent, so this is **once per card, per app** — later
runs and later boots reuse it. If a GPU app is ever unexpectedly slow, grep the log for that line
before assuming a hang.
ⓘ The note here previously said "1 cold start in 12". I could not locate an artifact for that rate;
the benches that are retained show the cost exactly **once** — 2026-09-13: 24 trials, T1 cold (153
frames), T2–T24 warm (230–244); 2026-09-15: 12 trials, all warm, 1772–1939. Stated as found.
⚠ **You are the first to boot it from a card** — this host has no SD reader, so SD boot cannot be
exercised here; the gates are the closest stand-in.
↩ `da752ac1` (pre-merge) is kept as a fallback, as is `16ad56f9`.

## 2b. ✅ REEL — settled per your notes (2026-09-16)

`artifacts/hdmi-video/20260916-160506-phoenix-rtos-rpi4-showcase.mp4` — 239 s, 11 segments, 11/11 on
`verify-demo-reel.py`, bt709-tagged.

- **Caption raised** off the bottom edge (136–200 px), clear of the player's scrub bar and the games' HUDs.
- **Order unchanged**, **H.265 kept with your phone footage**, **vkQuake included** — your calls. The
  self-referential H.265 "alternative" I had built is deleted; it read as a game segment.
- **Every caption now carries a frame rate**, measured at the page flip with the winsys counter rather
  than read off a HUD — vkQuake **43 fps average**, SuperTuxKart **7–8**.
- ⚠ **vkQuake's number was re-derived after it disagreed with the screen.** The first "42" was a median
  over the whole capture, but vkQuake plays one demo then sits on a static console at a dead-flat
  42.2 fps — so that median measured the *console*. It was within 1 fps of right by luck. The demo
  itself is 2140 frames / 50.1 s = **42.7**; the caption says "average" because the engine's own counter
  is a ~1 s estimate swinging 23–65. STK checked the same way and is clean (race 7.95). Method is now
  documented above the segment table in `make-demo-reel.sh`.

## 3. ✅ Verified this week (detail: [`docs/done/2026-09-16-w38-verification-detail.md`](../done/2026-09-16-w38-verification-detail.md))

- **`vkq-tile-flicker` FIXED** (ports `7ed2949`) — vkQuake rendered into the buffer being scanned out;
  it now page-flips. Not a lightmap bug. The winsys already had the machinery; vkQuake never opted in.
- **Upstream merge adopted** — 17 commits, two regressions found and fixed (syscall ABI break; the
  `CLOCK_REALTIME` cond default). Merged tree **12/12** across two gate passes.
- **The desktop survives a talk** — `startx_gpu action` **31.7 min, 0 faults**, still *animating* at the
  end (tick-to-tick 4.32 late vs 5.95 mid; presence alone would not have shown that).
- **STK's ~8.5 fps is not fill-bound** — 720p gives 8.43–8.52 vs 1080p 8.40–8.53. No cheap win, left
  alone. [`docs/misc/2026-09-16-stk-fps-not-fill-bound.md`](../misc/2026-09-16-stk-fps-not-fill-bound.md)
- **1050 libc tests, 17 suites, 0 failures** · stale-binary census clean · netboot proven by a real boot.
- 🔧 New: `scripts/check-stale-binaries.sh`, `scripts/verify-demo-reel.py`, `tools/demo-apps/webroot/`.

## 3c. ✅ vkQuake now LOOPS its demos (HW-verified)

vkQuake played **one** demo (~50 s) then dropped to console while QuakeSpasm looped. It was **one line**:
the bring-up patch in `Host_Startdemos_f` set `cls.demonum = -1`, which permanently disarms Quake's demo
loop, so when the port's `playdemo` ended `Host_EndGame` disconnected instead of calling `CL_NextDemo`.

**HW-verified** (`vkq-loop2`): `demo2 → demo1 → demo2 → demo3 → demo1` — four self-driven transitions,
including the wrap. Flip rate stays in the variable 25–86 fps band with **no flat ~42 fps console block**.
Fork `c55d7d8`, ports `b0fc510`. Only applies when `id1/phoenix-demo.cfg` is staged; the shipped image has
none and boots `map start`, which disarms the loop by itself — so **the default image is unchanged**.

⚠ **Attempt 1 failed on hardware, and the cause was my own fix.** It armed `demonum` and then inserted
`menu_main`; `M_Menu_Main_f` stashes `demonum` in `m_save_demonum` and sets it to **-1**, restoring it only
from `M_Main_Key`'s ESCAPE case — a keypress that never arrives on a headless board. So the menu call
silently disarmed the loop it had just armed. Worth keeping: the old hack's explicit `-1` was only ever
redundant with the menu's own effect. It was caught in one cycle because the test demands ≥2 ordered
`Playing demo from …` lines and `flipstat-summary.sh --seq` shows the console as a flat block.
Both attempts are on `publish`: `c55d7d8`/`b0fc510` is the verified one, `abd06c4`/`8169648` the
intermediate hardware rejected — history, not a rollback target.

## 3d. 🐞 FIXED — every rebuild was silently wiping the Pi's shader cache

`sync-netboot-tree.sh` keeps the shader cache unless the GPU driver changed, keyed on a fingerprint file
at the buildroot root. **`prepare-buildroot.sh`'s `rsync --delete` deleted that fingerprint on every
prepare** — so the next sync took its "(or first run)" branch and wiped the cache regardless. The
2026-09-08 optimisation had been inert since the day it landed. Fixed, coord `4bfdfb135`.

Proven with both controls, then end-to-end: `prepare-buildroot.sh` → `sync-netboot-tree.sh` now reports
**`KEPT — 127 entries`**, where that exact sequence used to report `cleared`.

**Why it matters:** a cold cache is **~67 s of black screen for vkQuake**, misread as a hang twice
(2026-09-09 cost three cycles; 2026-09-13 left a trial with 3 frames).
⚠ **Correction, mine:** the cache does *not* save SuperTuxKart ~35 s — a controlled probe shows 50
shaders "compiling" with zero new cache entries, all hits, run still 49.8 s. STK's real saving is
~5.5 s. Corrected in `KNOWN-ISSUES.md`, the README, and the script comment that spawned it.
👁 Watch item: the cache now genuinely persists, so invalidation rests entirely on the fingerprint.
Green speckle over a valid frame after a driver change would point there first.

Full map (paths per boot variant, what it is worth per app, the traps):
[`docs/misc/2026-09-16-shader-cache-map.md`](../misc/2026-09-16-shader-cache-map.md).

## 3e. ✅ Gate re-run on the post-loop-fix tree — 6/6, 0 faults, torches PRESENT

Two things had landed since the last gate — the vkQuake port (`b0fc510`) and the build tooling — and the
vkQuake change touches `Host_Startdemos_f`, which runs on **every** boot including the shipped
`map start` path that #67's torch check grades. So it needed re-gating rather than assuming.

**6/6 mechanical** (rc 0, prompt, 0 faults, launched, 23–24 frames each) · **#67 torches PRESENT**
(20/20 at-viewpoint frames) · **6/6 content** by eye: vkQuake on the START map with both torches lit,
QuakeSpasm 48 fps, Quake II 38.8, Quake III 27 on q3dm1 with a bot in frame, STK racing lap 2/2 at
Hacienda, and the X desktop with all five clients drawn (GL pinwheel, XBill, xclock, Life, top).
Manifest `manifests/2026-09-16-w38-postloop-gate.md`.

## 3f. ✅ Stability soak — 10× SuperTuxKart, all clean

Everything is gated and waiting on your flash, so the remaining lever is the standing goal's first word:
**stable**. Ran 10 back-to-back STK races (`stksoak2`) — the app the "page reads back zero" crash family
hits hardest — with the 2026-09-15 guards in place.

**10/10 raced. 0 faults, 0 allocator events, 0 guard fires.** Boot stages 10/10 (kernel, fbcon, psh,
lwIP, link, IP). Frame counts tight at **1607–1672** (±2%), 5.6–8.4 fps — no outlier, so no trial
silently degraded. The cache stayed warm across all ten boots (0 cold reports, 10/10 syncs `KEPT`);
note the bench does not run `prepare` between trials, so that is a warm-use observation, not a second
proof of this morning's fingerprint fix.

⚠ Worth keeping honest: 10 clean runs **bound** an intermittent, they do not clear it. At the historic
~1-in-10 rate this is roughly a coin-flip's worth of evidence. The family stays open and livable (§4).

## 4. Open bugs — all rated livable

| bug | what it does | live with it? |
|---|---|---|
| "page reads back zero" family — `libc-uninit-main`, `freebin-corruption`, `stk-highbits-pointer` | an app occasionally dies at startup or mid-run | **Yes.** Relaunch it. No fault from this family since 2026-09-12. |
| `V3D-binner-wedge` | GPU binner stalls, auto-recovers via reset | **Yes.** 9 runs / ~85 700 frames, 0 wedges; last seen 2026-08-22. |
| `atexit-null-head` | a `.data` word read back NULL at load | **Yes.** Root-caused and fixed in-tree; 0 events in ~3645 processes. |
| `q3-qvm-recipe` · `shader-cache-speckle` · `xterm-resize-artefacts` | reproducibility gap · known cosmetic tell · unreproduced drag artefact | **Yes**, all three. |

Single source of truth: [`docs/KNOWN-ISSUES.md`](../KNOWN-ISSUES.md).

## 5. Upstream

- **2026-09-16 sweep (9th): 17 commits merged, all 16 siblings 0 behind, tree GREEN and shippable.**
  Two regressions found and fixed: a **syscall ABI break** (`phMutexLock`/`phCondWait` 1→3 stack args ⇒
  old binaries got EINVAL from `pthread_mutex_lock`, nondeterministically) and the default cond clock
  flipping to `CLOCK_REALTIME`, unusable here (reverted, libphoenix `c283f2d`).
  ✅ Verified in the right order — census → `test-libc-pthread` 28/0 → apps: **12/12 across two gate
  passes**, 0 faults, torches PRESENT. Stale binaries 333 → 13 (diagnostic probes only, none shipping).
  🔧 Run `scripts/check-stale-binaries.sh` after **any** merge touching libphoenix or a syscall
  signature, **before** believing a gate — apps can pass on luck, the census cannot.
  Detail: [`2026-09-16-upstream-merge-detail.md`](../done/2026-09-16-upstream-merge-detail.md) ·
  [`2026-09-16-upstream-mutex-abi-break.md`](../misc/2026-09-16-upstream-mutex-abi-break.md) ·
  manifests `2026-09-16-w38-fullclean-gate.md`, `2026-09-16-demo-image-b95e983a.md`.
- 2026-09-13 / 09-14 sweeps (7th, 8th): nothing behind, nothing merged.
