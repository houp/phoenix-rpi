# Known issues and limitations

Open items in the Phoenix-RTOS Raspberry Pi 4 port, as of 2026-09-12. This is
the user-facing summary; the exhaustive engineering registries are:

- [docs/pi4-hardware-support-matrix.md](pi4-hardware-support-matrix.md)
  — per-peripheral status with evidence.
- [docs/TEMPORARY-FIXES-AND-FUTURE-CLEANUP.md](TEMPORARY-FIXES-AND-FUTURE-CLEANUP.md)
  — the `TD-NN` transitional-debt registry (its bottom "Tracking Checklist" is
  authoritative; most `TD` items are already resolved).

**This file is the single source of truth for open issues in this port.** Nothing else should carry
its own list — [docs/PHOENIX-RTOS-RPI4-CHANGES.md](PHOENIX-RTOS-RPI4-CHANGES.md) links here rather
than duplicating, because the two had already drifted. Resolved items are **removed** from this file
as soon as they stop being issues, so it shrinks over time; the git history keeps the record of what
was fixed, and `docs/done/` keeps the analyses.

> **Editing note.** Each row below must be **one line with exactly three `|`
> cells**, and the table must contain **no blank lines** — a blank line ends the
> table and everything after it renders as plain text. Long analysis belongs in
> `docs/misc/` or `docs/done/`, linked from the row, not inlined here.

## Application bugs

These affect the showcase apps, not the base system.

| ID | Symptom | Status / workaround |
|---|---|---|
| ~~#67~~ | **vkQuake wall torches (`progs/flame.mdl`) — no intermittency survives a correct measurement.** The historical reports were never scored against a controlled viewpoint: the check had 0 at-viewpoint frames because our own port glue ran `playdemo start` instead of booting the map (ports `6a639ab`). ⚠ **Never close this on a screenshot** — it was declared fixed five times historically, each on a single frame, and one "proof" was a moving lavaball mistaken for a wall torch. Verify only with a pass *rate*: `./scripts/test-cycle-bench.sh 6 <label> -- "vkquake"` then `./scripts/check-torch-rois.py --rate <label>`. ⚠ **Bare `vkquake`, NOT `+map start`** — this port has **no argv path at all**; the boot map comes from the hand-staged `id1/phoenix-map.cfg` (default `start`), and `id1/phoenix-demo.cfg` must be **absent** or a demo overrides it. Refuted: submit serialization, the alpha/scanout theory, `blend==0`, a stale shader cache, non-deterministic GPU archives. History: [`docs/misc/2026-09-03-quake-torch-regression-archaeology.md`](misc/2026-09-03-quake-torch-regression-archaeology.md). | **Closed 2026-09-15 — 6/6 by rate, twice.** `torch67`: **6 of 6 gradeable trials PRESENT, 0 absent, 0 inconclusive**, 16–19 at-viewpoint frames per trial, **599/553 lit px against a threshold of 8**, 0 faults in all six. That repeats the 09-14 6/6 on a different build, so **12 trials across two builds, no absence**. The torch ROI check runs inside `run-showcase-gate.sh` on every gate, so a regression is caught without anyone remembering to look. |
| ~~#64~~ | "SD-side filesystem stack pressure under load (deep fs call chains)". **The SD/ext2 half was already fixed and is the reference fix**: `#120` raised the storage pool to `storage_run(2, 16*_PAGE_SIZE)` = 64 KB, and the 2026-06-08 pool-thread audit ([`docs/done/2026-06-08-pool-thread-stack-audit.md`](done/2026-06-08-pool-thread-stack-audit.md)) re-checked every Pi 4 server against it. ★ That audit left exactly **one RISK row unapplied — the USB msg and status threads at 2 KB**, running msgRecv/status → enumeration → in-process class drivers (usbkbd/usbmouse) → DMA pool, on adjacent `.bss` with no guard page, so an overflow silently clobbers the neighbour instead of faulting (the `#120` shape). | **Closed 2026-09-15.** The last open row is applied: USB thread stacks **2 KB → 32 KB** (usb `f9f5723`), the top of the audit's 16–32 KB range, ~60 KB of `.bss`. Verified on hardware: `usbkbd` and `usbmouse` both enumerate (`/dev/kbd0`, `/dev/mouse0`), 0 faults, unix-socket 38/0, quakespasm `Host_Init` 2.228 s, every non-debug boot-stage row green. |
| ~~#66~~ | Stale `/tmp/.X0-lock` prevented the X server restarting after an unclean exit. **Not a broken liveness probe** — the new `test-libc-signal` `liveness` group proves `kill(pid, 0)` answers ESRCH for a reaped pid and 0 for a live one without delivering anything (5/5 on hardware). It is **PID REUSE**: the kernel hands out the *lowest* free pid, so the pid a dead X server released is likely already held by something else when the next launch reads the lock file, and Xorg's `LockServer()` then refuses with "Server is already active". | **Fixed.** `xlaunch` removes a leftover lock before starting the server (coord `ce5f50873`) — it is the only launcher, on a board with one display, about to start a fresh server. Verified: with a planted lock it logs `removed leftover /tmp/.X0-lock`, X comes up; with no lock the unlink is a silent no-op and two back-to-back sessions in one boot both start. |
| V3D-binner-wedge | Intermittent V3D binner wedge on long GPU runs, auto-recovered by a GPU reset that reports loudly (`RENDER TIMEOUT` / `DROPPED job`). Seen under both GL and Vulkan; `q3dm7` is the documented reproducer, and it is workload-specific (q3dm1's 1942 faces never wedge, q3dm7's 5823 do). ★ **It is NOT an MMU fault** — with the real fault-report registers instrumented (`MMU_VIO_ADDR` 0x1234, `MMU_VIO_ID` 0x122c, offsets confirmed against Linux `v3d_regs.h`), a wedged trial reported `vio_addr=0`, `vio_id=0`, `int_sts=0`, no binner overflow, and `ct0ca` parked **inside its own valid BCL BO**: a binner front-end pipeline stall with zero error status. ⚠ The earlier "`mmu_ill=0x8000886x` ⇒ stale PTE" reading was a red herring — `MMU_ILLEGAL_ADDR` is the scratch-page redirect *we program*, so reading it back only echoes `scratch_pa`. `r_mergeLightmaps 0` is **not** a fix (it wedged with the cvar set, after one lucky clean run). One contributing cause was found and fixed in the interim: `MAP_CONTIGUOUS` returns **non-zeroed** DRAM, so unpopulated binner-overflow-pool slots held cold-boot garbage that CT1 followed as a wild pointer; the pool is now zeroed. | Open; a reset+drop mitigation ships. Not reproduced recently — **9 q3dm7 runs, ~85 700 frames, 0 wedges** (6 more on 2026-09-15: 58 123 frames, 0 wedges, 0 faults), plus 18 six-app gate runs at 0; last actually observed 2026-08-22. ⚠ Kept open deliberately: clean runs also predate the pool fix, so for an intermittent this bounds the rate rather than proving it gone. ★ **That diff is DONE (2026-09-15) and the previous "named next step" was already stale** — [`docs/misc/2026-08-22-q3dm7-binner-stall-analysis.md`](misc/2026-08-22-q3dm7-binner-stall-analysis.md) had run it on 08-22 and gone further. Re-verified independently against today's tree: **`TILE_BINNING_MODE_CFG` cannot differ — we emit no field of it**; the packet comes from upstream Mesa (`v3dx_draw.c:86-103`) and no Phoenix commit in `external/mesa` touches `v3dx_draw.c`/`v3dx_job.c`/`v3d_job.c`/`v3d_util.c`. **tile_alloc/tile_state use Mesa's own formula** (`v3d_util.c:272-313`, called at submit time with the real draw count) and its headroom term **saturates at 512 KiB by ~16 draws**, so q3dm1 and q3dm7 get the *same* ~570 KiB — face-independent by construction, and refuted anyway by the wedge dump (`bpcs=0x7f000`, i.e. 520 KiB still free, `bpoa=bpos=0`, `ovf_armed=0`). **The CT0 kick order matches Linux** (`PTB_BPOS`, QMA/QMS, QTS, QBA, QEA last). **No unflushed CPU→GPU write exists**: every binner-input BO is Normal-NC (`V3D_CREATE_BO_CACHEABLE` has no user anywhere in Mesa's `src/`), the per-frame BCL and the late in-place `TILE_BINNING_MODE_CFG` rewrite are drained by `dsb sy` before the first MMIO poke, and `9bac0f7`→`6cdf597` are consistent because `pmap.c:314-315` clean+invalidates any page mapped `MAIR_IDX_NONCACHED` at map time. ⓵ **One real divergence, new:** we write `CTL_MISCCFG = 0x05` (`V3D_QRMAXCNT=2`, `OVRTMUOUT`) at init (`v3d_gpu.c:553-555`); **Linux writes MISCCFG only for ver < 41, i.e. never on 4.2**, and our own cold probe reads the silicon default as `0x06` (QRMAXCNT=3, OVRTMUOUT=0). `QRMAXCNT=2` was A/B-tuned; the default 3 has never been benched, and the "OVRTMUOUT required by our Mesa build" comment is unverified. ⚠ **Do not change it now** — the bug has not reproduced, so an A/B would destroy the only signal there is. ⏭ **Next step is an OBSERVATION, not a change: at the next recurrence, read `CL@ct0ca`.** Foreign content ⇒ BO aliasing survived the three fixes that landed after the last sighting (handle recycling `6502f67`, stale dirty lines `9bac0f7`/`6cdf597`, single-owner mappings `7a1e3db` + mesa `274ee5abea9`) — then re-run `V3D_BO_TRACE=1` + `scripts/analyze-bo-trace.py` and look for *temporal* overlap. A valid draw item with `FDBGS` stall bits and `int_qpu=0` ⇒ a genuine front-end starve, and *then* MISCCFG parity (`V3D_QRMAXCNT = -1`) is the A/B, scored by the q3dm7 `BIN TIMEOUT` rate. |
| freebin-corruption | **The allocator is the victim, not the origin — mechanism now identified (2026-09-12).** A free bin ends up holding a pointer to memory that is no longer what it thinks, and a live heap header's `size` field reads back with its **high 32 bits overwritten** (`0x80000001_0000d000` where the true size is `0xd000`). The process then dies computing `heap + heap->size`. The corrupting values decode as **RGBA8 pixels** (`A=128`, exactly 50% alpha, twice), and the identified route is a BO's CPU mapping having **two live owners**, so a page the GPU still uses is handed to `malloc`. ★ **Fixed and now MEASURED, not inferred (2026-09-15).** The pair devices `7a1e3db` + mesa `274ee5abea9` made the winsys the single owner of a BO mapping, and a `V3D_BO_TRACE=1` SuperTuxKart run on the current build scores **0 overlapping reuses in 2880 BO events** (`scripts/analyze-bo-trace.py`). ⚠ **Read the right number:** the same run shows **117 CPU addresses reused by more than one handle**, and that is *expected* once closed BOs are really unmapped — the historical "56" was measured with unmapping OFF, where any reuse meant two live owners. Only *temporal overlap* is a defect. Contained meanwhile: `heap->size` is now bounds-checked from above (libphoenix `11286c0`), and both bin hand-out paths validate their result. Eliminated, do not re-walk: insertion of a heap base, writes into a poisoned freed chunk (8 trials, 0 fires), intra-struct overflow, STK's drive-graph index, and a userspace buffer overflow (11/11 page-aligned siblings, p≈1e-27). | **Believed fixed, watching.** The last signature-A fault in the archive is 2026-09-12 16:38; the ownership fix landed the same day at 20:51, and **23 SuperTuxKart runs since show 0 faults** — which is evidence for *this* fix, not for the font patch 0019 (see `stk-highbits-pointer`). Not closed: the mechanism check is one run, and signature B (the stale-bin hand-out, `hbase?=0`) still fires ~2 runs in 23 and is a separate, contained thing. Re-score any suspicious build with `V3D_BO_TRACE=1` + `scripts/analyze-bo-trace.py`. Detail: [`docs/misc/2026-09-12-two-corruption-signatures.md`](misc/2026-09-12-two-corruption-signatures.md). |
| stk-highbits-pointer | SuperTuxKart dies ~1 run in 10 with `far = 0x80000001_<valid heap address>` — a pointer whose **upper 32 bits are garbage** (bit 63 set is never a valid aarch64 userspace pointer). Symbolised to `FontWithFace::render`, and the signature **predates and survived** the drive-graph guards (seen 09-08, 09-09, 09-11, 09-12), so it is distinct from the fixed AI crash. The faulting `{_M_start,_M_finish}` pair is **self-consistent** (finish−start equals the element count, both words share the same bogus high half), so the pointer *arrived* bad rather than being overwritten in place. | Open. ⛔ **The STK font path is CLEARED as the origin (2026-09-15, source review).** The faulting `ldrb` is `font_with_face.cpp:787`, in the **collect** loop (630-805); the loops ports `b5ce28f` patches start at **834**, so that fix cannot have caused or cured this. There is **no out-of-bounds 4-byte store anywhere in `render()` or its callees** — every indexed store was checked and is in bounds, and `grab()/drop()` write an 8-aligned `ReferenceCounter`, not "8-aligned +4". The `m_cached_gls` dangling-reference theory is dead too (no `erase` anywhere, no re-entry into `getCachedLayouts`). ⇒ If a font-path store produced signature A, the store was **legitimate and the buffer was wrong** — the allocator handed the same memory to two owners, which also explains the self-consistent `{_M_start,_M_finish}` pair (another live object's coherent 16 bytes). ⚠ **23** STK runs since `b5ce28f` show 0 faults, but that is only **p ≈ 0.09** against the old rate and, per the above, would not be evidence even if it were significant. ★ Settled empirically too: `STK-FONT-OOB` — 0019's own guard — has fired **0 times in 28 post-fix STK logs**, so the fallback-bank bug it fixes was never live in this workload. 0019 is a correct fix for a real bug and is unrelated to this crash. ⚠ The "RGBA8 pixel" reading of `0x80000001` is **unconfirmed** — `SColor(alpha=128,0,0,0)` is `0x80000000`; nothing in the path produces `b=1`. **Same root cause as `freebin-corruption`** — and that cause is now identified and measured: a BO's CPU mapping with **two live owners**, fixed by devices `7a1e3db` + mesa `274ee5abea9` on 2026-09-12 at 20:51. ★ The 23 clean STK runs attach to **that** fix, not to the font patch: the last fault in the archive is 2026-09-12 **16:38**, four hours before it landed. ⏭ Make the next occurrence decisive: extend the 0016 guard to re-read `dfv[0]/dfv[1]` through a **`volatile`** pointer (a plain re-read is optimised away) and dump `gl[i-1..i+1]` plus `&gl[0]`, `size()`, `capacity()`. Neighbours foreign ⇒ whole buffer aliased; only the post-page-boundary half foreign ⇒ page-level. Review: [`docs/misc/2026-09-15-stk-font-path-cleared.md`](misc/2026-09-15-stk-font-path-cleared.md). Analysis: [`docs/misc/2026-09-12-stk-highbits-pointer-analysis.md`](misc/2026-09-12-stk-highbits-pointer-analysis.md). |
| allocator-double-free | libphoenix's allocator reports a double free and the process exits **70** (`EX_SOFTWARE`). **Not AF_UNIX-specific**: the report appears in 21 logs from 2026-06-28 → 09-09 across xcalc (×7), Dillo, vkQuake (×2), the glamor desktop (×5) and `test-libc-unix-socket` (×4). A generic heap-corruption symptom with several causes, some already fixed separately (the `vasprintf` overflow behind glib2/mc/Dillo; `free()` of `.rodata` in X11). ⚠ Only **one** occurrence in the whole archive captured block detail, so inferences from it rest on a sample of one — and two readings this row used to assert were wrong and have been corrected against the allocator source. | Open, rare; **no field occurrence since 2026-09-09**. The report now names its caller plus both footers, which discriminate the mechanism on sight: `foot == size & ~3` means the allocator wrote the header (a genuine double free); a mismatch means an overrun or a planted footer. Resolve `caller=` with `addr2line` against the unstripped `prog/` copy. |
| libc-uninit-main | **Two globals set by libc init were both unset at once** (one observation, `rpi4b-uart-20260915-001244-opbench-T9.log`). Previously filed as an "ntpclient Data Abort, stack-overflow shape" — that reading is **wrong**. Exact diagnosis: `far=0x30`, `esr=0x92000007` (read), `pc` resolves to `mutexLock(stream->lock)` in `fwrite`, libphoenix `stdio/file.c:777`; `0x30` is the `lock` offset in `FILE`, so the stream pointer was **NULL**. The stack dump carries the formatted string verbatim (`psh: ntpclient: unknown command\n`), which is `psh.c:211` `fprintf(stderr, ...)`. ⇒ **two** globals were unset at once: `stderr` (assigned only in `_file_init`) and psh's applet list (filled only by `__attribute__((constructor))` entries, one of which is `ntpclient` itself — `/bin/ntpclient` is the same 229 264-byte psh binary). Neither `_file_init()` nor the constructors had run. ★ **2026-09-15, from the ELF: the two zero globals share ONE 4 KiB page.** `psh_common` is at `0x438198` and `stderr` at `0x4383c8` — both in page `0x438000` of `.bss` (`stdin`/`stdout` too). And the fault's own register file was working on it: `x24=0x438198` (exactly `psh_common`), `x25=0x438000` (exactly that page base). ⇒ **one page reading back zero explains both at once**, which is a far smaller claim than "libc init never ran" — `_file_init()` and the constructors write into that page, so a zero read means those writes are not there. ⛔ **2026-09-15, source review: the mapping reading gets a CLEAN NEGATIVE, and this is NOT the same bug as `atexit-null-head`.** The two globals are not even on the same *kind* of page: psh's RW `PT_LOAD` splits into a file-backed COW page (`0x436000–0x437000`, where `.data` and `atexit_common` live) and a 3-page anonymous demand-zero mapping (`0x437000–0x43a000`, where `psh_common` and `stderr` live) — different populate paths, different data sources. And no kernel path was found that loses a write to that anon page: `process->lazy` is 0 on MMU builds so exec eagerly `_map_force`s every page; `pmap_switch` precedes `process_load`, so the BSS memset writes through the final mapping; every fault serializes on `map->lock` and a populated amap slot returns early, so two faults cannot yield two pages; there is no pageout/reclaim to drop a filled slot; and `_pmap_cacheOpAfterChange`'s flush-by-VA only fires for non-cached/executable descriptors, never for a cached RW data page. ⇒ **"those writes definitely execute" is the assumption the next occurrence must TEST, not inherit** — crt0 / `_libc_init` ordering is back on the table. ⓘ `atexit-null-head` is separately attributed **in-tree and the attribution holds**: `vm/object.c:285-309` records that `object_fetchCluster` silently zero-filled on a short/EOF NFS read and names `atexit_common.head` as the casualty; the fix added bounded retries plus the reporter that has seen 0 events in ~3645 processes. | Open, **one sighting**, no demo impact (ntpclient only sets the clock). ⏭ A recurrence now arrives **labelled**: psh's unknown-command path reports via `write(2)` instead of `fprintf` — it could previously die on the error itself — and says explicitly when the applet list is empty, which means lost `.bss` rather than a genuinely unknown command (utils). The era it comes from is the `premain-hang` bench, now fixed, so also re-check whether it recurs at all. ⓘ Hardened on the way past: `_file_init()` never checked its three `calloc()`s and dereferenced them immediately, so an allocation failure there was an instant NULL-deref — libphoenix now builds each standard stream through one checked helper. |
| ~~premain-hang~~ | **FIXED 2026-09-15 — and it was never a libc/exec/vfork problem.** `socket()` (kernel `posix/inet.c`) and libphoenix's `socksrvcall()` (reached via `getaddrinfo`) resolved `/dev/netsocket` with a **path lookup on every call**. A path lookup is answered by the filesystem that owns `/` — which `nfs-fs` **is**, and it is single-threaded — so its own loop thread sent the lookup to the port it was supposed to be servicing and deadlocked. libnfs opens a fresh socket whenever it must reconnect, so a dropped server stopped the whole filesystem and every process blocked on its first request; a launch hung while opening its own binary. ⓘ **Netboot/nfsroot only** — the SD demo image has an ext2 root and no `nfs-fs`. ⓘ An **old** defect, made reachable by the NFS-root takeover (2026-06-28) and turned from rare into periodic by the NFSv4 lease keepalive thread (2026-08-03); not a regression from the recent merges. | **Closed.** Fixes: kernel `7c021702`, libphoenix `47dde32` (cache the oid), ports `a44623a` (libnfs sync-call deadline, defence in depth), filesystems `427aabd` (reclaim on `-ETIMEDOUT`). Gate: **`./scripts/test-nfs-recovery.sh`** — restarts the host nfsd under a running Pi and grades reclaim + relaunch; **PASS** on the clean build (`noticed=1 reclaimed=1 launched=1 wedged=0 faults=0`), where it wedged 2/2 before. Manifest `manifests/2026-09-15-premain-hang-fixed.md`. ⚠ The withdrawn rate/regression claims recorded here earlier stand withdrawn; the method that worked was a `volatile` mark re-reported by a watchdog thread, not a pass-rate bench. |
| q3-qvm-recipe | Quake III's `pak1.pk3` (three QVMs built from **ioquake3**, needed because the free demo's 1999 QVMs report UI API 3 while quake3e requires 6) is **staged from `assets/quake3-qvm/` rather than rebuilt from source** — the QVM build recipe is not yet in the repo. The game itself needs no retail content and no CD key; see `assets/quake3-qvm/README.md`. | Open (reproducibility gap, not a runtime bug). |
| shader-cache-speckle | A **stale Mesa shader-cache blob** renders as green speckle over an otherwise-valid frame. Phoenix has no ELF build-id, so the on-disk cache keys on shader source only and a host toolchain change invalidates nothing: after the gcc-16.2 promotion the cache still held gcc-14-era blobs, the GPU executed stale QPU binaries, and SuperTuxKart's loading screen came up speckled. ⓘ Useful diagnostic tell — coloured speckle **over a valid frame** means stale-blob execution, whereas a real GPU wedge drops whole frames and logs one. | Known; bump `V3D_PHX_CACHE_VERSION` or clear the cache on any clean rebuild (`sync-netboot-tree.sh` does, which is why the first GL run after a re-stage is slow). |
| xterm-resize-artefacts | Owner-reported artefacts while dragging an xterm resize on the GPU X desktop. **Not reproduced** by a sound measurement; resize *correctness* is verified (server and client agree once settled). Most likely intermittent frames during a fast drag — this stack has no compositing and presents by GPU readback. | Open, unreproduced; re-check with the drag *released*. |

## System-level limitations

| Area | Limitation | Status |
|---|---|---|
| Board portability (TD-06) | The DTB parser assumes a single interrupt controller; only the 4 GB Pi 4B is validated (1/2/8 GB models untested). | Known limitation. |
| Early-console alias — cross-board port (B5) | The generic aarch64 early-console path (`hal/aarch64/generic/console.c`) hardcodes the UART at the fixed VA alias `0xffffffffffe00000`, because it must print *before* the DTB-discovered base is available. On the Pi 4B this alias **equals** the discovered pl011 base, so it is correct and intentionally left as-is. It only matters when porting to another aarch64 board whose console is elsewhere. | By design for the Pi 4; a to-do only for a future cross-board port. |
| SError masked (TD-10) | Asynchronous SError is masked in early kernel paths because a live PCIe/VL805 USB external-abort SError is not yet root-caused; unmasking regresses boot. A dump-and-halt handler is implemented and armed for when the abort is fixed. ⚠ This is why an MMIO read of an unclocked block **hangs silently** instead of aborting. | Known, HW-gated. |
| `hal_memset` DC-ZVA (TD-20) | The `dc zva` fast path is disabled on the Cortex-A72 pending proof of the EL2 DC-ZVA trap state (does not reproduce in QEMU). Performance-only; correctness-safe. | Known limitation. |
| Stack-exhaustion signal DoS | A userspace process that **exhausts its 1 MiB main-thread stack** takes down the kernel: fault-signal delivery (`hal_cpuPushSignal`, aarch64) writes the signal frame to the now-unmapped user stack and double-faults at EL1, with no recovery — so any program overflowing its stack crashes the box (register-confirmed via `tools/stack-bomb/`). Fix = a guarded signal-frame push, minding the demand-paged-stack case. **Attended**: highest-blast-radius kernel path, and invisible to a green-boot smoke. | Known; turnkey work-order in [docs/done/2026-08-22-signal-push-stack-exhaustion-dos-workorder.md](done/2026-08-22-signal-push-stack-exhaustion-dos-workorder.md). |
| Windowed GL in X11 is bandwidth-bound (~10 fps), by architecture | A GL application drawing into a **window** copies its pixels GPU → CPU → socket → CPU → GPU every frame. Measured on the 640×480 demo window: **96 ms/frame (10.4 fps)**, of which `draw` is 2.7 ms and the rest is the round trip (`glReadPixels` 12.5 ms, packing 6.7 ms, `XPutImage` **58 ms** pushing 1.23 MB through the X socket). Full-screen GL is unaffected — the games bypass X and render straight into the scanout buffer (QuakeSpasm ~37–48 fps, Quake III 25–46). **Root cause:** no DRI3/DMA-BUF equivalent, so a client can only hand the server pixels, never a buffer reference. Cheaper mitigations all measured and closed: a larger AF_UNIX ring (+12%), poller contention (refuted), a cached readback alias (refuted), deferred present (no effect). | **By design for now — owner decision, 2026-09-09.** A point fix was designed and half-proven on hardware but closed deliberately: its ceiling was only ~1.7× (≈17 fps) because the DDX present survives the change, and it would leave the port with no general buffer-sharing story. The right investment, if ever needed, is a Phoenix-specific DRI/DRM layer. Reference: [docs/misc/2026-09-09-gl-window-buffer-sharing-work-order.md](misc/2026-09-09-gl-window-buffer-sharing-work-order.md). |
| SD boot of the current image is unverified on this bench | The host card reader is **detached** (it shares the USB-C port with power/eth) and there is no card in the Pi, so the shipped image cannot be flashed or SD-booted here — the owner is the first to boot it from a card. Substitutes: the contents gate asserts the FAT boot partition (the six files the firmware needs, `arm_64bit=1`, and that every file `config.txt` names exists) and runs automatically inside `--variant sd`, plus a QEMU structural boot check. All other verification is netboot/NFS. | Known bench limitation; mitigated by gates, not by a boot. |
| Ethernet link IRQ (TD-Eth-LinkIRQ) | The PHY's `INT_B` line is not routed to a GIC SPI on the Pi 4 board, so link state is MDIO-polled at 1 Hz (as Linux and U-Boot also do). | By design for this board. |
| SD write throughput | Reads use UHS-I **DDR50** (1.8V) + **SDMA** + multi-block CMD18 (~38 MB/s, ~86% of Linux on the same card); writes are multi-block CMD25 but **PIO** (~17 MB/s DDR50 / ~13 MB/s HS50 — a BCM2711 controller quirk corrupts DMA writes, so they stay PIO). Multi-block writes verified 0-corruption. The 1.8V/DDR50 switch is reliable on netboot and best-effort on SD-boot (falls back to HS50). | Reads fast; writes correct but PIO-bound. Remaining (attended) levers: DMA writes, reliable DDR50 on SD-boot. |
| Audio | PWM audio over the 3.5 mm jack works with a streaming DMA ring and a Quakespasm mixer backend; an audible end-to-end sign-off on real headphones is still pending. | Partial. |
| Netboot NFS-root: one narrow boot-order race | A command issued *before* the NFS-root `takeover` completes can still hit the pre-takeover RAM root and report `not found`; the clean fix is a `plo` boot-order gate. The test harness already covers it with `--inter-cmd-secs`. ⓘ The broader "NFS root is unreliable, prefer SD" framing this row used to carry is **not supported by measurement**: across **297 NFS-root boots on 2026-09-11/12, 297 reached the psh prompt (100%)** and there were **zero** `ERANGE` / `exec -12` / `NF4ERR_EXPIRED` lease-reclaim failures. (A host-side `nfs-server` restart is still the fix if a stale export does appear after days of uptime.) | Known, narrow; NFS root is the routine lab configuration and is reliable in practice. |

## Partial / not started

- **WiFi (BCM43455) — data plane proven at FULL MTU in both directions; the
  lwip netif is the remaining integration step.** The driver joins a real
  WPA2-PSK access point, completes the 4-way key handshake, **and carries real
  traffic over the air**: it obtains a full DHCP lease (DISCOVER → OFFER →
  REQUEST → ACK, confirmed by the AP's `DHCPACK`) via `tools/wifi-probe
  jointxcnt`. Beyond that lease the frame path carries **full-size ethernet
  frames both ways**, verified byte-for-byte: the Pi sends 400/1000/**1472**-byte
  UDP payloads that a host socket receives bit-exact, and reads back a host-sent
  1472-byte tagged probe unchanged. Two fixes were needed — SDIO byte-mode CMD53
  caps a transfer at 512 bytes (larger requests silently wrapped the 9-bit count
  field, so 525..2048 was a corruption band), and function 2's block size had
  never been programmed, which made block mode unusable on the data path. The
  driver exposes `/dev/wifidata` (write a frame / read a frame) as the seam for
  an lwip netif. That netif is **not** wired up, so arbitrary sockets do not use
  WiFi — for everyday networking use wired Ethernet (fully working).

  *Measurement note for anyone debugging this:* `tcpdump` on the host's AP
  interface does **not** see frames sent by the Pi — not even the DHCP that the
  host's own dnsmasq answers — so it is useless as an egress detector on this
  rig and has twice produced a false "no egress" conclusion. Use an
  application-level detector (a UDP socket, or dnsmasq's own logs);
  `scripts/wifi-air-monitor.sh` wraps the ones that work. (The earlier "TX
  reaches the firmware but not the air" report was a measurement artifact — a
  link-layer counter that does not count broadcast/pre-lease frames.) The
  proprietary Cypress firmware blobs are **not vendored** here
  (copyright/EULA hygiene); `scripts/stage-bcm43455-firmware.sh` stages them
  into a gitignored `.firmware/`.
- **Bluetooth (BCM43455) — driver-level bring-up only.** `/dev/hci0` comes up,
  the firmware patchram loads (323/323), a real BD_ADDR is read, and an HCI
  Inquiry completes. There is **no host Bluetooth stack** — no pairing,
  profiles, or audio — so it is not usable for real Bluetooth work yet.
- USB mass storage, I²C / SPI / PWM general-purpose drivers, and camera (CSI-2) /
  DSI display are not implemented.

| `vkq-tile-flicker` | **vkQuake: single-frame screen-space black-tile flicker.** One frame (1/30 s) shows hard-edged, screen-axis-aligned black rectangles; the frames either side, at the same viewpoint, are clean. **Not a lightmap bug** — a band crosses four surfaces with independent lightmaps at identical screen rows, and the blocks do not follow the walls' perspective. V3D is tile-based and the blocks are tile-shaped ⇒ suspect the tile-store/present path. Control: QuakeSpasm on the same demo/day/capture card is clean over 72/72 consecutive frames. ⚠ **No rate figure is trustworthy yet** — seven automatic detector variants have failed (they fire on fast camera motion, or miss a frame confirmed by eye), so every verdict so far is contiguous-window visual review at 30 fps. Reported present on 09-08 at lower amplitude (unverified), so likely pre-existing with a changed rate rather than new. Evidence: `artifacts/vkq-lightmap-dating/`. | **Open, 2026-09-15.** Next step that settles it: record demo1 at 30 fps on a rolled-back 09-08 manifest and compare glitch frames per minute. |
