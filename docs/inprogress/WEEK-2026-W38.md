# Week 2026-W38 (Mon 2026-09-14 → Sun 2026-09-20)

Phoenix-RTOS RPi4 — weekly working log. Kept **short on purpose**: this is the file you read.
Analyses: `docs/misc/`. History: `docs/done/`. W37: `docs/done/WEEK-2026-W37.md`.
Working detail for this week: [`docs/done/2026-09-14-w38-detail.md`](../done/2026-09-14-w38-detail.md),
[`2026-09-15-w38-fixes-detail.md`](../done/2026-09-15-w38-fixes-detail.md),
[`2026-09-15-w38-detail-2.md`](../done/2026-09-15-w38-detail-2.md).

**★ OWNER GOAL (2026-09-06):** a stable build with the games and X11 good enough for a public
presentation / a screen recording published online.

---

## 1. WHERE THINGS STAND — two calls are yours, nothing is blocking

**Everything is green on the current tree**, including the 10th upstream sweep adopted tonight (§5):
six-app gate 6/6 · libc 20 suites / 1 088 tests / 0 failures · endurance X 26 min, vkQuake 28 min /
48 825 frames, STK 6/6 — all 0 faults. **The card is a separate, older artifact** and still boots
`b95e983a` (§2). SD-lane evidence table from the overnight run: archived in
[`2026-09-17-w38-sd-lane-and-fixes.md`](../done/2026-09-17-w38-sd-lane-and-fixes.md) — headline: SD
boot 6/6, showcase **17/18** over three passes, shader cache survives a reboot, three endurance runs
clean.

✅ **Call #1 is now cheap: the image is cut and verified, your card untouched.**
`artifacts/rpi4b/rpi4b-sd-2part.img`, sha256 **`1060fa27…4e22548a`**, cut **16:22 from the gated
tree** — sweep + PWM pacing + vkQuake demo loop + audio bound **+ today's audio containment**, so it
does *not* carry the ~350 s `q2-sdl-openaudio-hang` your card still has. Contents check **PASSES**,
QEMU boot-check **PASS** (plo → kernel entry, `nfs;/` refs=0 / `bcm2711-emmc` refs=4, i.e. a real SD
blob). If you say "re-flash", it is a 3-minute `dd`. ⓘ The 12:45 image (`564a3473…`) was cut *before*
the containment and is superseded — do not flash it. If you say "re-flash", it is a 3-minute `dd`, not a 40-minute build. ⏳ As expected,
`--variant sd` overwrote the TFTP `loader.disk`; the netboot loader was rebuilt and the lane
**re-verified with a real cycle** (13:09, `nfs;/…;takeover` in the syspage list, psh + lwip + 0
faults) — the bench is back exactly as I found it.

⏸ **Your two calls, neither urgent:**
1. **Re-flash the card?** It carries `b95e983a`, which now predates the vkQuake demo loop, the audio
   bound, *and* the whole upstream sweep. The demo works as-is; I have not touched your verified card.
   ⓘ Cutting a fresh SD image costs ~20 min and temporarily breaks the netboot lane (§5), so it waits
   for your word rather than being done speculatively.
2. **TD-19 — add the `isb` to the TLBI helpers?** The doc's `dsb; isb` claim was false and is
   retracted; per ARM ARM the sequence is arguably incomplete. I deliberately did **not** touch it: a
   barrier on every TLBI is a global change to a correctness-sensitive path and wants you watching.

ⓘ **Leave the card in.** Bench is on the **netboot** default; both lanes are one command apart
(`netboot-server-down.sh` ⇒ SD boot, `-up` ⇒ netboot).

## 2. ✅ CARD + REEL — both settled, nothing left for you here

**Card:** you put it on `/dev/sda`, I wrote `b95e983a` (read-back byte-identical, `e2fsck` clean) and
you put it in the Pi. It has since booted 6/6 and run the showcase three times (§1). **Leave it in** —
both lanes stay available (`netboot-server-up.sh` ⇒ netboot, `-down` ⇒ SD boot). Fallback images
`da752ac1` and `16ad56f9` in `artifacts/rpi4b/` were verified intact 2026-09-17. 🧹 That directory
holds 20 GB across 13 images, 9 superseded — your artifacts, so I have deleted nothing.
⚠ **If Quake II hangs on the card, REBOOT — do not relaunch.** The card's image predates the audio
bound, so `/dev/audio0` stays unopenable for up to ~350 s (§4).

**Reel:** `artifacts/hdmi-video/20260916-160506-phoenix-rtos-rpi4-showcase.mp4` — 239 s, H.264
High@L4.0 1080p30 bt709, **faststart already set**: upload as-is, no remux. ⊕ **Re-verified 2026-09-18
with the HARDENED checker** (the one that now fails on an empty frame window instead of grading it
"ok"): **11/11 segments, PASS**, file intact (`ff6c3037…`, 146.8 MB). ⓘ It was recorded on the
pre-sweep build; the current tree renders the same scenes at the same rates, so it stays
representative — I have not re-recorded it without your say-so. Captions carry measured frame rates (vkQuake 43 fps average, STK 7–8), method documented above
the segment table in `make-demo-reel.sh`.
📦 **Publish kit ready** — [`2026-09-18-reel-publish-kit.md`](../misc/2026-09-18-reel-publish-kit.md):
exact chapter timestamps (straight cuts, no fades, so they are exact not approximate), a title, a
description, and the three caveats worth carrying (STK's 7-8 fps is the honest number, the reel has
no audio track, Quake III's camera is a spectator). Publishing is a paste job whenever you want it.

ⓘ Detail for both — provenance, the blank-card retraction, the vkQuake fps re-derivation:
[`docs/done/2026-09-17-w38-sd-lane-and-fixes.md`](../done/2026-09-17-w38-sd-lane-and-fixes.md).

## 3. ✅ This week — fixes, verification, publication hygiene

Detail: [`2026-09-17-w38-sd-lane-and-fixes.md`](../done/2026-09-17-w38-sd-lane-and-fixes.md) ·
[`2026-09-16-w38-verification-detail.md`](../done/2026-09-16-w38-verification-detail.md) ·
[`2026-09-17-stk-rate-and-allocator-guard-fires.md`](../misc/2026-09-17-stk-rate-and-allocator-guard-fires.md)

**Fixed:** `vkq-tile-flicker` · vkQuake **loops its demos** · **every rebuild silently wiped the shader
cache** · **`q2-sdl-openaudio-hang`** bounded ~350 s → ~10 s and now names which half stalled
(devices `d13778d`) · upstream merge adopted (17 commits, two regressions caught).

**Verified on the current build:** libc **20 suites / 1 131 tests / 0 failures** · six-app gate **6/6**,
0 faults, torches present, real flipstat frames on every game · X desktop **31.7 min** netboot and
**~33 min** on the card · STK soak 10/10. ⚠ The GL window is bounded at 20 000 frames (~27–33 min) and
exits normally — relaunch it.

**★ THE HEAP GUARD NOW DIAGNOSES ITSELF** (libphoenix `8659311` + `e4f7c65`; manifests
`…-malloc-why-gate`, `…-malloc-heap-provenance`). "corrupt chunk header" never said **which** of eight
checks rejected the block nor **what** its heap pointer was; it now prints `why=1..8` plus
`lheap?`/`freed?` — released heap vs stale-pointer-address-reused vs smashed `->heap`. All 8 codes
covered host-side (13/13); re-gated 6/6, libc 383/0 then 93/0 + 8/0, stale census 0 of 355.
🔎 **Where the bug is NOT, from ~8 M operations:** the real `malloc_dl.c` survived 2.4 M
single-threaded ops, then 3.2 M concurrent ops across 8 threads once its mutex stub was made a **real**
lock (plus ASan), then 2.5 M on hardware via `bin/mtstress` — **0 violations, 0 tag mismatches**
everywhere. Two new libc tests show the pthread mutex **and** the raw kernel mutex both serialize
under SMP (the suite had one for **spinlocks only**). ⇒ neither the allocator's logic nor the mutex
explains it; what is left is a writer **outside** the allocator, or something Phoenix-specific about
`munmap` of a released heap. Detail:
[`2026-09-17-stk-rate-and-allocator-guard-fires.md`](../misc/2026-09-17-stk-rate-and-allocator-guard-fires.md).
🔧 Both test-cycle scripts now **grade their own log** — nothing ever ran `uart-summary.sh` on the four
runs whose guards fired, which is the real reason they sat unnoticed for two days.

**★ GRADER + DOC AUDIT (2026-09-17):** **13 graders that could not fail** and **14 stale public-doc
claims**, all fixed. Worst was the six-app gate's `frames` column counting HDMI *snapshots*, so the
one column meant to catch a hung app could never be 0; and the README asserting the *opposite* of a
measurement in one paragraph while carrying the correction in another. All 83 archived gate logs
re-graded — no published claim changed. Full list: the done file above.

**★★ THE AUDIO STALL: contained, and its SHAPE is now known** (2026-09-18). Full record — every
register, null and citation: [`2026-09-18-audio-dma-stall-captures.md`](../misc/2026-09-18-audio-dma-stall-captures.md).
✅ **CONTAINED — the one thing a live demo would notice is gone.** The driver called a **parked**
channel "started"; it now grades by **progress** (healthy 1 784-2 352 ring words per 20 ms, threshold
64), re-arms the PWM up to 3×, and otherwise degrades `/dev/audio0` to a **paced null sink** — the app
runs silent instead of blocking. **Gated 6/6 twice** (`fracgate`, `mashgate`; the loader now on TFTP
is one commit later — the additive `ARMTRIALS` ioctl, boot-checked 0 faults, **not** gated) + a **10/10 negative
control**; manifests `…-audio-progress-guard.md` and `…-audio-armguard-gated.md`.
✅ **Four real defects fixed on the way:** a bus error latched on **100% of boots**; a clock start that
changed the source mux and the enable on **one bus cycle**; the firmware's **MASH divider** inherited
over an exact integer divide; and a re-arm progress check that **could not fail**.
★★ **And the shape changed: it is per-BOOT, not per-arm.** ~**14 000 trials, 0 failures** across three
probes — 7 000 PIO PWM starts, 1 000 DMA arms and 2 000 clock-cycled arms on PWM0/DREQ 5/ch 6, plus
**4 000 arms of the REAL PWM1/DREQ 1/ch 5 path** via a new `RPI4AUDIO_ARMTRIALS` ioctl. The arm
sequence is reliable on both instances, on a settled clock and a freshly cycled one. ⊕ The audio
driver's own stream even survived 2 000 stops of the shared generator. ⚠ *Corrected before it
hardened:* I first wrote that **two** things are true once per boot. There are **four** — a generator
that has never run since reset, boot-time contention, the **first-ever CB fetch** (the trials reuse
the control block the first arm allocated), and **the pre-fix code path itself**: every trial ran
*after* today's BERR/MASH/clock fixes, which the three captures did not have. ⏳ **Rate run in
flight** — 25 boots on the frozen tree, launched 18:26. That beats another timing tweak: the tree
already has ~100 boots at 0 stalls and every change perturbs this fault; if one fires, the re-arm
print — now trustworthy — decides whether a parked channel is re-armable.
↩ Two corrections banked: `DMA_CS=0x21` is the **healthy** state (9/10 boots read it while streaming),
so the capture record's headline evidence was worthless; and `CM_GATE` does not read back on this
clock.
📊 Rate on the frozen pre-containment build: **60 boots, 0 stalls** (p ≈ 0.015 vs 3-in-44) — a
**bound, not a cure**.
✅ **Also fixed:** picocom kills its process group — that had been killing every boot-only bench at
trial 1 *and* hiding the netboot stage table (capture `a7514505f`).
📣 **Public docs caught up:** the README's on-stage advice still said the worst case was "~10 s and a
relaunch works". On a 09-18 build it does not block **at all** — the app starts and runs silently.
Rewritten per build vintage (09-18+ / 09-17 / older incl. the card), and the hardware matrix's audio
row now carries the containment.

**📊 A clean day, measured not assumed:** all **238 UART logs / 56 MB** from 2026-09-18 — two gates,
three benches, four probe cycles, ~50 boots — through the full fault regex: **0 fault patterns**
(positive controls fire on known-bad archive logs, so the detector is not vacuously clean).
ⓘ Quake III's gate frames moved 10 918 → 9 498 today; **not** a regression — fourteen archived gates
span 8 993-10 918 with everything else flat.

✅ **Same method, applied to the binner wedge** (devices `560c398`): KNOWN-ISSUES has named the same
next step for that row since 09-17 — *"at the next recurrence, read `CL@ct0ca`"* — and nothing printed
it, so a recurrence would still have told us nothing. Now a wedge says whether `ct0ca`/`ct1ca` parked
**inside the submitted CL**, which BO owns that address, a hexdump around it and the opcode there
(`0x00` = CLE HALT), plus the front-end debug registers. Diagnostic-only, inside the timeout branch,
so it is unreachable on a healthy run; it never dereferences an address it has not proven mapped.
↩ **But it landed on the wrong path, and that is worth knowing:** `rpi4-v3d` is built and **not
auto-launched** — the shipping games link the **in-process winsys**, whose wedge prints
`v3d-winsys:`. The winsys does dump a CL, with a **measurement defect**: it reads the opcode at
`ca & ~3`, not at `ca`, so every historical `op=` is only trustworthy when `ct0ca & 3 == 0` — and the
whole block is gated off after three wedges per process. Being fixed on the live path now.

★ **A missing BARRIER found by reading, not by hunting** (devices `b9414c3`). `rpi4-audio` built its
DMA control block and ring in **Normal-NC** memory and kicked the engine through **Device** registers
with **no barrier anywhere** — and aarch64 does not order those against each other, so the engine can
fetch the control block before our stores land. `MAP_CONTIGUOUS` memory is **not zeroed**, so what it
would follow is garbage; a garbage `PERMAP` alone parks the channel waiting for a DREQ that never
fires — the captured signature **exactly**. The window is **once per boot** (a re-arm rewrites no CB),
which is precisely the shape the 14 000 probe arms pointed at. ⚠ **Not claimed as the cure:** syscalls
between the CB write and the arm may drain it incidentally on most boots. Landed because the V3D
driver has carried this barrier and its reasoning all along and this one never did.
⏳ **A port-wide audit of the same class is running** — genet descriptors, xHCI rings, SDHCI SDMA, the
V3D winsys: who else writes a DMA descriptor and kicks an engine with nothing in between.

⏳ **Nothing built yet, deliberately:** a rebuild overwrites the TFTP `loader.disk` and the 25-boot
rate run is measuring the *frozen* build. Everything above builds when it finishes.

**Same method, next defect — no capture yet:** 10 long STK races on the frozen build (6-lap profiles,
**31 806 frames**, the workload 3 of the 4 archived heap-guard fires came from) — **0 fires, 0
faults**. A bound with its exposure stated, not a clearance. What is banked: the shipping build is now
sampled with `why=`/`lheap?`/`freed?` in place, so the next fire says in one line whether a block
outlived its heap, a stale pointer's address got reused, or `->heap` was smashed.

## 4. Open bugs — all rated livable

| bug | what it does | live with it? |
|---|---|---|
| "page reads back zero" family — `libc-uninit-main`, `stk-highbits-pointer` | an app occasionally dies at startup or mid-run | **Yes.** Relaunch it. ⊕ **STK re-measured 2026-09-17: 0 crashes in 143 engine starts** since the 2026-09-12 BO fix, against **30 in the 242 runs before** (identical detector both sides, p ≈ 3e-8) — the "~1 run in 10" figure is historical. ⚠ *Corrected 2026-09-17:* this cell used to say "no fault since 2026-09-12", which its own membership contradicts — `libc-uninit-main`'s sighting is **2026-09-15**, and it now has a second, earlier one (2026-09-07, identical signature). The `far=0x8000…` signature proper is clean since 09-12. |
| `V3D-binner-wedge` | GPU binner stalls, auto-recovers via reset | **Yes.** Hunted 2026-09-17 on its own reproducer (`q3dm7`): **0/6, 37 057 frames, 0 faults**. Cumulative 15 runs / ~123 k frames, 0 wedges; last seen 2026-08-22. ⚠ A bound on an intermittent, not a clearance. |
| `q2-sdl-openaudio-hang` | Quake II hangs at startup in `SDL_OpenAudio` | **Yes.** Relaunch it. 2 of 136 Quake II runs (~1.5%), both lanes. **Mitigated, not fixed** (devices `efac488`): the stall is bounded ~350 s → ~10 s and now announces itself; the DMA stall itself is untouched and the abort path has never fired on HW. |
| `allocator-double-free` + `freebin-corruption` | a heap guard fires; the fatal variant exits 70 | **Yes.** ⊕ **2026-09-17 census:** 0 of the 1 239 non-probe logs since the BO fix carry the fatal double-free signature — but **4 fired a containment guard** (3× corrupt chunk header in STK, 1× chunk-handed-out-twice in vkQuake) and kept rendering to the end, so nothing looked wrong. ↩ `uart-summary.sh` would have flagged all four (23/1/5/71) — none of them was ever run through it, including one I reported on the same morning from its BO trace alone. Three callers symbolise into **Bullet physics** teardown; the 71-fire run is the same one that scored **0 BO overlaps**, so the double-ownership route does not explain the residue. ⏭ the guard records `operator delete`, not the site. ⛔ **That idea is dead, and it is worth knowing why:** the aarch64 target builds with `-fomit-frame-pointer` (`phoenix-rtos-build/target/aarch64.mk:20`), and the shipped `free()` proves it — its prologue is `stp x30, x19, [sp, #-64]!`, with **no frame record** (`stp x29, x30` + `mov x29, sp`). So `__builtin_return_address(1)` and any x29 walk have nothing to walk: level 0 is the only reliable caller. Getting the real site needs either frame pointers across libstdc++ and the port, or `.eh_frame` unwinding — neither is a one-line change. Do not re-propose "record one more return address" (measured 2026-09-18).  [`analysis`](../misc/2026-09-17-stk-rate-and-allocator-guard-fires.md) The GPU-side hypothesis (a BO freed while still mapped) was re-scored 2026-09-17 on the current tree — `V3D_BO_TRACE=1` over an STK race: **0 overlapping reuses in 2 827 events**, all 120 address reuses after the previous owner closed. ⚠ A bound, not a clearance. |
| ~~`q3-qvm-recipe`~~ · `shader-cache-speckle` · `xterm-resize-artefacts` | ✅ **closed** — Quake III's QVM pak IS reproducible from source (§3d) · known cosmetic tell · unreproduced drag artefact | **Yes**, all three. |

Single source of truth: [`docs/KNOWN-ISSUES.md`](../KNOWN-ISSUES.md). ⓘ Reconciled against it 2026-09-17: `q2-sdl-openaudio-hang` and `allocator-double-free` were missing here, and `atexit-null-head` was listed as an open bug although it is root-caused, fixed in-tree, has 0 events in the whole log archive and no row there — dropped.

**Publication pass (2026-09-17):** README's two stale public claims fixed — SuperTuxKart "still faults
intermittently in its own code" (falsified: 0 in 131 runs) and Quake III's "the QVM build recipe is not
yet in this repo" (it has been since 09-03, verified byte-wise). STK's frame rate on the README now
comes from the page-flip counter, not the game's on-screen number. A read-only audit of the rest of the public docs found **11 more**, all fixed:
the README carried a *second* SuperTuxKart paragraph asserting the **opposite** of the measurement
("fill-rate/bandwidth bound", plus the retracted fault claim and HUD frame rates), and the
**hardware-support matrix** — which the README calls authoritative — still had **#67 open**, "the
wall torches are still missing", the closed QVM reproducibility gap, and SD-write work marked
"HW-blocked (no card)". `PHOENIX-RTOS-RPI4-CHANGES` still said SD boot was unverified and that
KNOWN-ISSUES *deletes* resolved rows (it strikes them through). Two HUD-sourced frame rates replaced
with page-flip figures.

## 5. Upstream

🔧 **One sibling was invisible to every sweep.** `phoenix-rtos-usb`'s master had no upstream tracking
ref, so `git-siblings.sh` printed a quiet `?` for ahead/behind — ten sweeps have said "all siblings 0
behind" without ever comparing it. It *is* 0 behind (41 ahead, checked directly), so nothing was
missed; but nothing was checking. The column now says **NO UPSTREAM — SWEEPS SKIP THIS REPO** with
the repair command, and tracking is set. Same cannot-fail shape as the graders fixed on 09-17.


✅ **An SD image carrying the sweep is cut and verified** (see §1) — `rpi4-sd-2part.img`, sha256
`1060fa27…4e22548a`, contents check PASSES + QEMU boot-check PASS. **Prepared, NOT flashed**: your
card is untouched pending call #1. The known cost was paid and undone twice today — `--variant sd`
overwrites the TFTP `loader.disk` (no `nfs;/`), so the netboot loader was rebuilt straight after each
cut and the lane re-verified with a real cycle both times (`nfs;/…;takeover` back in the syspage
list, psh + lwip + 0 faults, audio streaming on arm 1).


✅ **10th sweep ADOPTED (2026-09-17, 27 commits) — all six masters moved, everything 0 behind.**
The risky one: signal handling **moved into the kernel**, **`signalPost` removed**, `threadsinfo()`
reshaped. Resolved on branches first, master untouched until every gate passed. libphoenix `434f6de`
· kernel `15fdea32` · utils `ce472cb` · tests `c7ccfc8` · project `fe60daf` · ports `dd3628a`;
manifest `2026-09-17-w38-upstream-sweep-10.md`; nine conflict resolutions written up in
[`…-sweep-10-conflict-census.md`](../misc/2026-09-17-upstream-sweep-10-conflict-census.md).

**Verified before master moved** — full-clean build · stale census **0 of 355** · libc **20 suites /
1 088 tests / 0 failures** (incl. `signal` 32/0, upstream's own new tests against my resolution) ·
six-app gate **6/6**, torches present, frames within 1% of pre-sweep · `ps`/`pm` fields all populated
· endurance: X **26 min / 19 980 frames** (frame time 63.7 → 64.8 ms), vkQuake **28 min / 48 825
frames** (26.07 → 26.57 fps, 1 744 vs 1 726 frames/min pre-sweep), STK **6/6** — every one 0 faults.
⇒ post-fix STK streak now **0 crashes in 143 engine starts**.

✅ **The gap the endurance runs left is closed:** the 26-min soak proved the desktop *runs*, not that
it *exits* — and desktop exit is where the historical dead-Pi wedge lived, in the `vm/map.c` fault
path I hand-merged. Six `startx_gpu --quit-after 18 action` start+exit cycles on the merged tree:
**6 starts, 6 clean `session ended — shutting down X`, 0 faults.** ⚠ A bound, not a clearance — the
old wedge was ~1 in 6, so 6/6 is p ≈ 0.33 on its own; it sits on top of 20/20 pre-sweep.

⚠ **Two things it cost:** WindowMaker called `threadsinfo()` with the old 2-arg form (fixed with
`PH_THREADINFO_ALL` — *not* `flags = 0`, a legal call that fills **nothing** and would have emptied
window titles silently), and the nine hand-merged hunks. ⓘ **Behaviour change:** the default `umask`
is now **0**, not 022.

- **9th sweep (09-16): 17 commits merged, tree GREEN**, two regressions caught — a **syscall ABI
  break** (`phMutexLock` 1→3 args) and a cond-clock default flip, both fixed. 🔧 Lesson kept: run
  `check-stale-binaries.sh` after any merge touching libphoenix or a syscall signature, **before**
  believing a gate. Detail: [`2026-09-16-upstream-merge-detail.md`](../done/2026-09-16-upstream-merge-detail.md).
- 7th/8th sweeps (09-13, 09-14): nothing behind.
