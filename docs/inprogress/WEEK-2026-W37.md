# Week 2026-W37 (Mon 2026-09-07 → Sun 2026-09-13)

Phoenix-RTOS RPi4 — weekly working log. Kept **short on purpose**: this is the file you read.
Detail: `docs/done/2026-09-10-w37-detail-snapshot-{3,4,5,6,7}.md` and
`docs/done/2026-09-10-w37-allocator-investigation.md`. Analyses: `docs/misc/`. Analyses: `docs/misc/`.
W36: `docs/done/WEEK-2026-W36.md`.

**★ OWNER GOAL (2026-09-06):** a stable build with the games and X11 good enough for a public
presentation / a screen recording published online. Everything below is ranked against that.

---

## 1. DECISIONS NEEDED FROM WITOLD — none open

★★★ **AF_UNIX UPSTREAM RE-PORT: DONE, VERIFIED ON HARDWARE, PUSHED.** Your rule (prefer upstream,
re-apply ours on top, never reject) is carried out end to end. kernel `114ce7d3` · tests `2a627bf` ·
project bumped `e301d6d` · manifest `2026-09-13-afunix-upstream-verified.md`. **All 16 siblings are
now 0 behind upstream.**
**Verified, not assumed:** socket suite **38 tests / 0 failures / `OK`** on hardware (baseline was
**27/0**; upstream's 36 + our 2) — against the previous attempt's **6 EL0 Data Aborts and 10 of 27**.
X11: **0 faults, 0 exceptions**, content **89.7% non-black** (baseline 89.4–90.2) and **11.64 fps vs
10.45 pre-merge** — so the readiness-`poll()` fix holds and the desktop is if anything slightly
faster, not the 100 ms-per-round-trip regression that taking upstream as-is would have caused.
★ **3 of our 5 local fixes retired** — upstream had fixed those bugs itself, the socket-id one more
thoroughly than we had. Only the readiness-`poll()` queue and the `SO_RCVBUF` ceiling were
re-applied.
⏳ **Six-app gate running on the merged kernel** — the socket suite and X11 are verified, but the
games are not, and `master` now carries AF_UNIX so the next image will include it. ⓘ The shipped
image `afd534a7` was built **before** the merge, so what you flash is unaffected either way.
ⓘ The predicted link hazard did **not** fire precisely because the poll fix was re-applied properly:
`posix_poll()` merged with no conflict marker while calling a function upstream deleted.

Optional only: **D4** (the "genuine tool" boundary) and **D2** (moving Mesa into
`phoenix-rtos-devices`). Nothing is blocked on either.

## 2. ⏸ YOUR ONE ACTION — flash the refreshed gated image `afd534a7`

`artifacts/rpi4b/rpi4b-sd-2part-gated-afd534a7.img` · SHA256
`afd534a73b5fbd2d388c7d2efda2323e0843e52ed3c73f6c07bec69df42e0239` · manifest
`manifests/2026-09-13-demo-image-afd534a7.md`

```
udisksctl unmount -b /dev/sda1 2>/dev/null || true
sudo dd if=artifacts/rpi4b/rpi4b-sd-2part-gated-afd534a7.img of=/dev/sda bs=4M conv=fsync status=progress && sync
```

**New since `1f493117`:** the Mesa/winsys **BO-ownership pair fix** — the STK memory corruption is
root-caused. Mesa's GL path was `munmap`ing the **winsys's own** mapping, returning pages to the
kernel behind its back while the GPU PTEs stayed valid; `malloc` then took those pages, so a
4-byte pixel write landed on allocator metadata. Fixed as a pair (Mesa stops unmapping, the winsys
becomes the single owner and unmaps at `GEM_CLOSE`). Plus the libphoenix `heap->size` guard and STK
port patches through `0019`.

**Gates — three, all passing.** Contents + FAT safe-to-flash · **QEMU structural PASS** · **provenance
read from the image's own ext2 root** (heap guard in every binary, diagnostic startup trace absent
everywhere including `psh`).

**Evidence:** six-app gate **6/6**, content-graded from HDMI, 0 faults and 0 corruption events ·
**31 clean STK bench runs**, 0 corruption events of any kind (0 abandons, 0 high-word records,
**0 guard fires** — so the corruption did not occur, rather than being contained).
⚠ Honest limit: that is **Fisher p = 0.125**, not significance. ~50 clean runs would be needed.
Strong in practice; not claimed as proven.

★ Before presenting or recording: run each GPU app once to warm up (a cold first run costs ~35 s).
↩ Fallbacks, newest first: `-gated-1f493117.img`, `-gated-2940cc70.img`, `-gated-aa176f38.img`,
`-gated-80a1cb88.img`, `-gated-1afa724b.img`.
ⓘ Don't flash `rpi4b-sd.img` (FAT-boot-only) or the rolling `rpi4b-sd-2part.img`.
⚠ **You are the first to boot this from a card** — the host SD reader is absent, so SD boot cannot be
exercised here; the contents + QEMU gates compensate.
ⓘ Full hunt detail: [`docs/done/2026-09-13-w37-corruption-hunt-and-upstream.md`](../done/2026-09-13-w37-corruption-hunt-and-upstream.md).


## 2b. ✅ Demo repeatability, the V3D clock race, and STK's AI crash — all settled

**3 full gates, 18 cycles, 17 clean**, all six apps rendering every time (content-graded from HDMI).
The one blemish was `/bin/ntpclient`, not the app. The intermittent GPU-app start stall was a **V3D
clock race** — fixed and shipped in `1f493117` (`MBOX_FAIL` ~9% → 0%). SuperTuxKart's AI crash was
an `assert()` compiled out under NDEBUG — guarded by port patches 0014/0015, **0 crashes in 12
trials** against a 1-in-3 baseline. ⚠ `premain-hang` stays **reopened**: the V3D fix does not explain
a launch that printed nothing.
Full text: [`docs/done/2026-09-12-w37-settled-sections.md`](../done/2026-09-12-w37-settled-sections.md).

## 3. ✅ Settled today — the POSIX option macros (your go-ahead)

libphoenix advertised `_POSIX_VERSION 200809L` with **no option macro defined**, so portable
`#if _POSIX_TIMERS > 0 && ...` guards were always false. **Eleven now claimed** (`070ceda`),
contract-tested both ways (`98e7014`, `03cbc97`), **HW 4/4, suite 212/0**, ports rebuilt, showcase
re-gated. Detail: [`docs/done/2026-09-10-w37-detail-snapshot-6.md`](../done/2026-09-10-w37-detail-snapshot-6.md).

## 4. ✅ libc bugs found and fixed this week — all HW-verified, all with tests

- **`strncmp`/`strncasecmp` read one byte past `n`** (libphoenix **`f2d2789`**, tests **`a82e0e4`**
  + **`7c80324`**). The bound was tested *after* `*p`, so a string ending exactly at a page boundary
  faulted — it was killing SuperTuxKart in AngelScript's tokenizer. `strncasecmp` had **no** test
  coverage at all. Negative control: the same pattern faulted on the old code, so the test is not
  vacuous. Manifest `2026-09-10-strncmp-over-read-fix.md`.
- **`getaddrinfo` leaked and lied** (**`75bf050`**, test **`fc3dd0f`**): on a failed `realloc` it
  leaked the old buffer *and* returned 0 with `*res == NULL`. It had zero coverage; the new test is
  DNS-free so it runs anywhere.
- **`_atexit_init` dereferenced NULL** (**`160e916`**): the list head came from a file-scope compound
  literal, leaving no named storage to recover to; it is now a named static. This was the "2070
  exceptions in ntpclient" report — ntpclient was innocent (it is `psh` under an applet name).
  ⚠ The ELF is *correct* (`.data` holds `0x4373e0`), so a runtime NULL meant **`.data` did not reach
  the process intact** — a load-time failure that would hit **any** statically initialised pointer.
  ✅ **2026-09-12: the recovery no longer masks it, and it is not happening now.** The fix recovered
  **silently**, hiding the only evidence the corruption still occurred; `_atexit_init` now reports it
  (libphoenix **`94a2d8e`**). Measured with the reporter live: **~3645 processes across three boots**
  (2500 + 1000 + 85 launches plus boot processes), every child reaching `main()`, **0 reports, 0
  faults**. ⚠ Not proof it is fixed — the check covers **one word**, and kernel `6cd3adec`'s
  premature-EOF diagnostic never fired either — but it bounds the current rate, and a recurrence can
  no longer pass unnoticed. Tracked as `atexit-null-head`.
- ✅ **Regression-checked twice: 541 libc tests, 0 failures**, with zero spurious instrument fires.


## 5. Allocator free-bin corruption — SUPERSEDED by §2; allocator is the victim

Kept only as a pointer: the hardening (validator cannot fault, release never abandons a linked
chunk, coalesce boundary from the trusted heap) is committed and HW-clean, and the free-bin entries
are **stale pointers into `mmap`-recycled regions**. Insertion is excluded (25 abandons, 0 catches)
and the poison bench closed the last internal route (8 trials, 0 fires). The live thread is §2.
Full text: [`docs/done/2026-09-12-w37-settled-sections.md`](../done/2026-09-12-w37-settled-sections.md).

## 5c. ✅ The banked GPU wedge did not reproduce on the shipped build

`q3dm7` is the documented reproducer for the intermittent V3D binner wedge. **3 runs, 27605 frames,
0 wedges, 0 faults** — plus 12 six-app gate runs at 0. Last actually observed **2026-08-22** (2
events); every sampled run since 2026-08-27 is clean. A root cause was found and fixed in between:
`MAP_CONTIGUOUS` returns **non-zeroed** DRAM, so unpopulated binner-overflow-pool slots held cold-boot
garbage that CT1 followed as a wild pointer. ⚠ **Kept open, not closed** — clean runs also predate
that fix, so for an intermittent this bounds the rate rather than proving it gone. The recovery
reports loudly (`RENDER TIMEOUT` / `DROPPED job`), so any recurrence is visible in any gate log.

## 5b. ✅ X11 re-verified on the current build

The desktop had not been exercised since two allocator commits landed, and X is the heaviest AF_UNIX
**and** malloc consumer here — a workload shape the 541-test suite does not cover. One cycle:
**0 exceptions, 0 corruption events, 2430 frames at a steady 10.3 fps**, content-graded **89.5%
non-black / 8679 colours** against the 90.2% / 8856 baseline. No regression.

## 5d-0. ★★★ WITOLD'S CALL: `premain-hang` IS ALMOST CERTAINLY A REGRESSION (2026-09-13)

You said the system was stable in the 09-09 showcase videos and that you'd bet on a regression from
the recent risky merges. **The timeline says you are right and I had it framed wrong** — I filed this
as a new open bug and never tested the regression hypothesis.

| when | what |
|---|---|
| 2026-09-09 16:13 | last showcase video recorded — system stable |
| 2026-09-09 14:49 | kernel `4ea46701` **"!proc: increase the number of priorities from 8 to 64"** (upstream) |
| 2026-09-09 19:03 | merged as `0c768551` **"8 → 64 priorities (ready bitmask)"** |
| 2026-09-10 05:00 | **first silent-launch artifact in the whole repo** (`stkcap-T4`) |
| 2026-09-11 | filed as `premain-hang` — as a new bug, not a regression |

The same 09-09 window also landed `ce445804` + `f0e94a9b` (dangling `thread->process`), `3c8009b7`
(`process_getName`), `ac40dcf1` (kmalloc zone links) and `6cd3adec` (vm/object premature EOF).
★ **The symptom fits the prime suspect:** the child is created, the parent blocks in `proc_spawn`,
and the child never executes a **single** instruction — no output, no fault, no exception. That is
what a thread enqueued ready but never marked in the scheduler's **ready bitmask** looks like.

⚠ **Two corrections to my own reasoning.** (1) I called today's 17% and the earlier ~4% "not
comparable"; both used a 300 s no-output detector, so they **are** — the rate got ~4× **worse** over
the last days, and I read a rising-rate signal as a measurement artifact. (2) On aarch64 the exec
path is **eager**, not demand-paged (`process->lazy = 0`, `vm/map.c:619` forces every PT_LOAD page at
exec time), so the "slow cold NFS paging" framing was weaker than I treated it.

★ **Method change, per your point about the debug facilities:** stop grinding trials. Code review of
the priority change first; A/B against kernel `137ec58f` (pre-window) to confirm the regression
outright; and the hang leaves the board **alive with networking up**, so a live scheduler-state dump
beats counting boots. Rollback snapshot: `manifests/2026-09-13-pre-regression-ab.md`.

## 5d-1. ★★★ DEBUG_THREADS WAS OFF — and turning it on found a real defect in one run

★ **The scheduler's own assertions have been compiled out of every build we have ever run.**
`4ea46701` (the 8 → 64 priority merge) replaced a live `LIB_ASSERT` on ready-list membership with
`LIB_ASSERT_THREADS`, which is gated on **`DEBUG_THREADS` — a macro defined NOWHERE in the tree**.
So from 2026-09-09 onward, ready-list / bitmask divergence has been undetectable at runtime. That is
a regression in *observability* regardless of what else is wrong, and it lands exactly in your window.

✅ **Added `KERNEL_DIAG`** (kernel `a95189f9`) — a `-D` hook for the kernel build only, with the same
knob-change invalidation the libc trace needed. Enabled `-DDEBUG_THREADS`. Board boots clean.

🐞 **FIRST RUN, REAL DEFECT, DETERMINISTIC:**
`kernel (_proc_lockClear:2154): lock: user.mutex, pid: 25, tid: 60, unlock on not locked lock`
— QuakeSpasm unlocks a userspace mutex that **is not locked**, **once in every single run** (26 of 28
instrumented runs so far; the 2 misses are runs that ended early). It fires at **exit**, right after
`-loadbench set -> exiting after load`. Deterministic and cheap to chase — no statistics needed.

★ **The scheduler orphaned-queue mechanism is real but NOT firing.** `_readyRemove()` on a thread that
is not on the queue NULLs that queue's head (`lib_listRemove` takes its singleton branch without
checking membership) and then clears the ready bit because the head is now NULL — orphaning every
thread at that priority, silently. That is the one mechanism matching the symptom. But with the
assert live: **0 fires in 28 runs, including 3 silent launches** ⇒ it is not what is happening.
ⓘ The bitmask arithmetic itself is **correct** — 64-bit type, no `1 << prio` on a `u32`, no
`ctz`/`ctzll` mismatch, set/clear correctly paired, no stale `& 7`. My first hypothesis was wrong.

🐞 **My own instrument was on a dead path and its silence meant nothing.** The first watchdog went in
`proc_spawn()` — but **psh runs external commands through `execve()`**, so `proc_spawn()` is never on
that path. Replaced with a marker at the **last kernel statement before the EL1→EL0 hand-off**
(`process_exec`, which all three exec paths converge on), positive-controlled at **42 fires across 8
boots** before being read. It splits the fault cleanly: marker printed and no libc marker ⇒ the hang
is in **crt0**; marker absent ⇒ still **kernel-side**.
⏳ No silent launch has occurred yet on that build (0 in 14; 20 more running), so the split is open.
⚠ Rates across builds: **5/30** (libc trace only) · **3/14** (+DEBUG_THREADS) · **0/14** (+exec marker).
0 vs 3 is **p ≈ 0.22** — not significant, so instrument masking is again *suspected, not shown*.

## 5d-2. ★★★ THE RACE IS AT THE EXEC → USER-MODE HAND-OFF (2026-09-13, significant)

**One `lib_printf` immediately before the EL1→EL0 `eret` makes the fault disappear.**

| build | silent | clean |
|---|---|---|
| libc trace only / + `DEBUG_THREADS` | **8** | 36 |
| + `-DEXEC_ENTRY_TRACE` (one print before the hand-off) | **0** | **33** |

**Fisher two-sided p = 0.0091.** Unlike the earlier masking suspicions (p = 0.37), this one is
significant. The marker sits in `process_exec()` *before* `hal_cpuDisableInterrupts()`, so it adds a
short preemptible window — and a delay there closes the window the bug needs. That is a **positive
localisation**, not merely a failed reproduction: whatever is wrong happens at or immediately around
the transition into user mode, on a 4-core SMP board.
⚠ The cost is that the instrument suppresses what it was built to split, so the "kernel-side vs crt0"
question is still open — no silent launch has occurred *with* the marker live to read.
★ Next, and it fits the `vfork` work in your suspect window (`6d8f40a5` "do not free a kernel stack a
vfork child is still running on", `20802d61` "stop vfork from freeing the parent's stack and map"):
psh launches through **`vfork` + `execve`**, which shares stack and map between parent and child
across exactly this hand-off. A probe that records state without adding a delay (a ring buffer read
out afterwards, not a print) is the way to keep the bug alive while observing it.

✅ **Tree restored to non-diagnostic and HW-verified**: `EXEC-ENTRY`, `SPAWN-WATCHDOG`, ready-list
asserts and libc markers all **0** in the loader and in every staged and live binary (kernel objects
and `init.o` deleted on the knob change, ports relinked, export re-synced). Boot test:
**unix-socket 38/0 `OK`, 0 exceptions**, quakespasm `Host_Init` in **2.238 s**. Manifest
`2026-09-13-post-regression-hunt-clean.md`.

## 5d. ★★★ `premain-hang` LOCALISED — the process never reaches libc at all

**The fault is BEFORE `_libc_init`**, i.e. in **exec / program loading (kernel side)** or psh's spawn
path — not in a libc initialiser, which is where three weeks of this hunt had been pointed.

**Evidence (sound this time).** 30 cold `quakespasm -loadbench` launches, one per boot, on a build
carrying the one-marker `LIBC_STARTUP_TRACE=min` instrument (verified in the **live NFS export**, not
just the buildroot — `--scope core` does not relink ports). **25 reached readiness in ~2 s; 5 produced
literally nothing in a 300 s window** — no app output and **zero `libc-init: enter` markers after the
command echo**, against exactly 1 in every one of the 25 healthy trials. 300 s clears the documented
~68 s cold-exec class by 4×, so this is a hang, not a slow load.

⚠ **RETRACTION, and it is a big one.** The **"cold boot + heavy launch" trigger grid** (p = 4.4e-04)
and the **"reproduced on QuakeSpasm, 2 in 23"** claim are **withdrawn**. Every silent-shaped trial in
the entire artifact history — five, across four benches — ran a **~45 s** post-command window, which
is *below* the ~68 s cold-exec class, so "printed nothing" and "had not finished loading" were the
same observation. My own memory carried that warning (`--idle-secs` ≥ 240) and I ran 45 s anyway.
ⓘ Two long-window observations are **not** affected and still stand: the original event (idle 240 /
max 300) and the spawn-storm stall (≥218 s).
ⓘ Rate is now **5/30 ≈ 17%**, well above the ~4% quoted before; the two are not comparable, since the
old detector could not separate a hang from a slow start.

🐞 **Four measurement defects found and fixed** — all of them would have produced a confident wrong
answer, in both directions:
- the window was read from arrival stamps, which cannot measure a wait in which nothing arrived — a
  genuine 300 s hang would have scored *inconclusive*. `psh-interact` now writes
  `*** capture-window ended after N.Ns` into the log itself, and `check-capture-complete.py` ignores
  that line (counting it would mark every silent run "CAPTURE OK").
- `--stamp` splits the capture mid-token, so a healthy trial's `libc-init: enter` arrived as `libc` +
  `-init: enter` and graded as reaching neither libc nor main.
- markers were counted over the whole log — a boot prints ~20 of them, which would have scored a
  silent launch as "reached libc" on the strength of the boot's own output.
- a trial whose command was never **echoed** never launched anything; counting it as an event is what
  made a 2-in-23 bench read as 3-in-24.
✅ All of it is now in `scripts/classify-premain-trials.sh`, controlled **both ways** against real logs
(`c76187c48`, `edf8f71e6`, `0c2a7ef8b`, `ed1bb3856`, `c8df8c79f`).
⏳ Confirmation bench running with the window marker live, so the proof sits in the artifact rather
than in my reading of a side log.
Detail: [`docs/misc/2026-09-13-premain-hang-condition-grid.md`](../misc/2026-09-13-premain-hang-condition-grid.md).

## 6. ✅ The merged AF_UNIX kernel passes the full showcase too

Six-app gate on the **merged** kernel: **6/6 rc=0, prompt, 0 faults**, and **0 corruption events of
any kind** on every app. Content-graded per run (pre-merge → post-merge): X **89.7 → 89.6%**,
QuakeSpasm 62.0 → 61.6%, Quake III 43.8 → **70.1%**, Quake II 47.2 → 47.2%, vkQuake 62.2 → **82.8%**,
STK 99.5 → **99.9%** (114614 colours). No regression anywhere.
🐞 **Fixed a grader flaw found doing this** (`check-hdmi-content.py`): it keyed frames on the **label
only**, so every re-run of a gate pooled its frames with the previous one and reported the best across
both. That is why three apps first graded *identical to the digit* with the pre-merge gate — the
winning frame came from the older run. It now splits runs on a >120 s gap, and the numbers above are
the first genuinely per-run ones.

## 7. ✅ UPSTREAM — all 16 siblings are 0 behind

The AF_UNIX cluster is **done and verified on hardware** (kernel `114ce7d3`, tests `2a627bf`, project
`e301d6d`). Your rule — prefer upstream, re-apply ours on top, never reject — carried out end to end:
**3 of our 5 local fixes retired** because upstream had fixed those bugs itself, one more thoroughly
than we had. Socket suite **38/0 `OK`** on HW (baseline 27/0); X11 **11.64 fps vs 10.45** pre-merge,
so the readiness-`poll()` re-apply holds. Detail:
[`docs/misc/2026-09-11-afunix-upstream-report-scope.md`](../misc/2026-09-11-afunix-upstream-report-scope.md).

Game forks: quakespasm / yquake2 / quake3e **0 behind**; **vkquake 42 behind by your rule**. All four
generated port patches **OK**.
- **2026-09-13 sweep (6th). Nothing behind, nothing merged.** All 16 siblings **0 behind** — and this
  time it is genuine, not last sweep's branch artifact: kernel and tests are back on `master` with the
  AF_UNIX merge landed. Forks unchanged; all four patches OK.


## 8. Also settled this week

One line each, with full detail in `docs/done/2026-09-10-w37-detail-snapshot-{3,4,5,6,7}.md`: the
libstdc++/`std::chrono` fix (~30 features off, not one) and its promotion · libphoenix
`clock_getres` · the allocator large-bin abandon fix · the readlink clamp · the nfs-fs
attribute-cache fix · CPython's restored `dir_fd`/`sched_*` · the Window Maker `nice()` collision
that had made the X11 showcase unbuildable, **and the stale docs that would have re-introduced it**
· the yquake2 upstream sync · MicroPython's `ticks_ms`/`ticks_us` were the wall clock, now monotonic
· a sweep of all 16 port shims against libphoenix's 994 symbols (one overlap, removed) · the public
docs re-synced · your four reported bugs, all addressed.
