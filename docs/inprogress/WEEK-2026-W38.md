# Week 2026-W38 (Mon 2026-09-14 → Sun 2026-09-20)

Phoenix-RTOS RPi4 — weekly working log. Kept **short on purpose**: this is the file you read.
Analyses: `docs/misc/`. History: `docs/done/`. W37: `docs/done/WEEK-2026-W37.md`.
Working detail for this week: [`docs/done/2026-09-14-w38-detail.md`](../done/2026-09-14-w38-detail.md),
[`2026-09-15-w38-fixes-detail.md`](../done/2026-09-15-w38-fixes-detail.md),
[`2026-09-15-w38-detail-2.md`](../done/2026-09-15-w38-detail-2.md).

**★ OWNER GOAL (2026-09-06):** a stable build with the games and X11 good enough for a public
presentation / a screen recording published online.

---

## 1. STATE OF PLAY — is anything blocking? **No.**

Both deliverables you asked for are done, gated, and waiting on you:

- **The regenerated showcase reel** — `artifacts/hdmi-video/20260915-194618-phoenix-rtos-rpi4-showcase.mp4`,
  239 s, **all 11 segments captured 2026-09-15** on the current build. Gated by
  `scripts/verify-demo-reel.py`: **PASS on all eleven** (live signal, content, motion where expected).
- **A new demo SD image** — `da752ac1`, gated **6/6** on hardware (§2).

All three things you flagged on the clips are fixed: **Quake II** now plays the demo (30× the motion of
the clip you rejected), **Quake III** orbits (`+map` silently resets the `CVAR_CHEAT` orbit cvars — it
needs `+devmap`), **vkQuake** is back on demo2/the Grisly Grotto *and* its flicker is root-caused and
fixed (§3).

## 2. ⏸ YOUR ONE ACTION — flash `da752ac1`

`artifacts/rpi4b/rpi4b-sd-2part-gated-da752ac1.img` · SHA256
`da752ac1b8a902420feca8d85a03c2d3416e2062a5844ddb353062fc6358d857` · 1.6 GB ·
manifest `manifests/2026-09-15-demo-image-da752ac1.md`

```
udisksctl unmount -b /dev/sda1 2>/dev/null || true
sudo dd if=artifacts/rpi4b/rpi4b-sd-2part-gated-da752ac1.img of=/dev/sda bs=4M conv=fsync status=progress && sync
```

**Supersedes `16ad56f9`, which you had not flashed.** One delta: the vkQuake page-flip fix. `16ad56f9`
was cut before it, so a live vkQuake demo off that card would show the flicker.

**Gates — run against the image, not the build tree:** contents PASS (incl. the FAT boot partition) ·
`e2fsck -fn` clean (10513 files) · `loader.disk` boots `/dev/mmcblk0p2:ext2` with zero `nfs;/` refs ·
plo starts **usb** and **lwip** (no usb = no keyboard) · ★ the fix verified **inside the image's own
ext2** with `debugfs`, with a **negative control** (the old code's marker is absent, so it cannot be a
stale binary).

**Hardware gate on that tree (`w38-pageflip`): 6/6 mechanical · 6/6 content · 0 faults · #67 torches
PRESENT** (16/16 at-viewpoint frames). X 99.2% non-black · QuakeSpasm 69.0% · Quake III 59.3% ·
Quake II 47.8% · vkQuake 53.0% · STK 100.0%. Manifest `manifests/2026-09-15-w38-pageflip-gate.md`.
ⓘ Colour counts read lower than the previous gate across **all six** apps, including ones untouched by
the change ⇒ a capture/counting difference, not a regression. Flagged, not explained away.

★ Before presenting or recording: run each GPU app **twice** to warm up — 1 cold start in 12 spends its
whole window compiling shaders.
⚠ **You are the first to boot this from a card** — this host has no SD reader, so SD boot cannot be
exercised here; the gates above are the closest stand-in.
↩ Fallbacks, newest first: `-gated-16ad56f9.img`, `-gated-4099f657.img`, `-gated-afd534a7.img`.
ⓘ Don't flash `rpi4b-sd.img` (FAT-boot-only) or the rolling `rpi4b-sd-2part.img`.

## 2b. ⏸ DECISION — the H.265 segment uses your own footage

That segment decodes `IMG_8331-phoenix.265`, **your phone recording of your desk**. In the 09-15
capture a laptop screen is legible at **t≈80 and t≈129** showing an internal Claude Code transcript,
and the reel encodes at full **1920×1080 with no downscale**, so anything readable there is readable in
the published file. My window (`85|24`) avoids both and shows a file manager, the previous showcase
mp4, the Phoenix-RTOS page in Dillo and a keyboard — but the room and screens are still yours.
ⓘ The same footage is in the **currently shipped** reel too, so this is not new; publication is what
makes it matter.
✅ **A non-personal alternative is already recorded** (`hevc-play` on `showcase1080.265`). Say the word
and I swap it in — a 10-minute re-cut. Nothing is published either way until you say so.

## 3. ✅ Shipped today

- **`vkq-tile-flicker` FIXED** (ports `7ed2949`, port-only). vkQuake had no WSI swapchain: it rendered
  into ONE fb0-backed image and `storeOp=STORE` *was* the present, i.e. it wrote the buffer the display
  was scanning out. V3D is tile-based and stores tiles one at a time, so a refresh landing mid-store
  showed half-new frames — hard-edged, screen-axis-aligned blocks for one frame. **Not a lightmap bug.**
  ★ The winsys already had page-flip and the GL path already used it (which is why QuakeSpasm was the
  clean control); vkQuake just never called `scanout_init`, so `v3d_phoenix_flip()` was a silent no-op.
  HW: `3 buffer(s) TRIPLE-BUFFER+page-flip`, 0 faults, render unchanged. Escape hatch
  `VKQ_SINGLE_BUFFER=1`. ⚠ The flicker **rate is unmeasured** — mechanism gone by construction, but 7+
  detector variants failed, so no before/after percentage is quoted.
  Analysis: [`docs/misc/2026-09-15-vkq-tile-flicker-root-cause.md`](../misc/2026-09-15-vkq-tile-flicker-root-cause.md).
- **The reel**, 11/11 segments re-captured, plus `scripts/verify-demo-reel.py` to gate any future one.
- **`tools/demo-apps/webroot/`** — the page Dillo fetches, which nothing in the tree used to serve.

↩ **Retracted today, so you don't chase them:** `fbcon-freeze` (not a bug — a static screen caused by my
own harness pacing, `--idle-secs` > recording length; fixed with `REC_IDLE_SECS` + a warning), and
"python3/dillo/mc are missing from the image" (they are present; `ls a b` fails if *either* operand is
missing).

## 4. Open bugs — all rated livable

| bug | what it does | live with it? |
|---|---|---|
| "page reads back zero" family — `libc-uninit-main`, `freebin-corruption`, `stk-highbits-pointer` | an app occasionally dies at startup or mid-run | **Yes.** Relaunch it. No fault from this family since 2026-09-12. |
| `V3D-binner-wedge` | GPU binner stalls, auto-recovers via reset | **Yes.** 9 runs / ~85 700 frames, 0 wedges; last seen 2026-08-22. |
| `atexit-null-head` | a `.data` word read back NULL at load | **Yes.** Root-caused and fixed in-tree; 0 events in ~3645 processes. |
| `q3-qvm-recipe` · `shader-cache-speckle` · `xterm-resize-artefacts` | reproducibility gap · known cosmetic tell · unreproduced drag artefact | **Yes**, all three. |

Single source of truth: [`docs/KNOWN-ISSUES.md`](../KNOWN-ISSUES.md).

## 5. Upstream

- **2026-09-13 / 09-14 sweeps (7th, 8th). Nothing behind, nothing merged.** All 16 siblings **0 behind**
  upstream; all four game-fork patches OK.
